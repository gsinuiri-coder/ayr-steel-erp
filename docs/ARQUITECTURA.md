# AYR STEEL ERP — Documento vivo de Arquitectura, Análisis y Requisitos

> **Documento vivo.** Fuente de verdad del proyecto. El agente (Claude Code) lo actualiza al cerrar cada fase: nuevas decisiones van a §0.2, avances a `docs/PROGRESO.md`, requisitos nuevos o cambiados se editan aquí con su RF.
> Origen: `AYR-Steel-ERP-Arquitectura-2026-09-01.docx` (v1, 2026-09-01). Convertido y ampliado el 2026-09-02.

## 0. Control del documento

### 0.1 Reglas de edición

- Identificadores de código (variables, propiedades, columnas, funciones, archivos, rutas API) en **inglés**. Todo lo demás (UI, mensajes, comentarios, docs, commits) en **español**.
- Cada requisito lleva ID `RF-nn`. Un RF sin referencia a módulo/ruta que lo implemente = pendiente.
- Decisiones se registran como `D-nnn` en §0.2 con fecha, decisión y motivo. No se borran; se marcan `SUPERSEDIDA por D-nnn`.
- Preguntas abiertas viven en §5 hasta resolverse; al resolverse pasan a §0.2.

### 0.2 Bitácora de decisiones (ADR corto)

| ID    | Fecha      | Decisión                                                                                                                                                                                                                                                                                                                                                                                                                          | Motivo                                                                                                                                                                                                                                                  |
| ----- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D-001 | 2026-09-02 | Greenfield. **No existe data real** que migrar de la versión previa (Firebase). Toda carga histórica entra por importación masiva desde planilla (RF-12, RF-52, RF-71).                                                                                                                                                                                                                                                           | Confirmado por el dueño del proyecto.                                                                                                                                                                                                                   |
| D-002 | 2026-09-02 | Monorepo `pnpm` + Turborepo: `apps/api`, `apps/web`, `packages/shared`.                                                                                                                                                                                                                                                                                                                                                           | Tipos y esquemas Zod compartidos API↔Web.                                                                                                                                                                                                               |
| D-003 | 2026-09-02 | API: NestJS + Prisma. Dinero, pesos (kg) y medidas (mm) en columnas `NUMERIC` → tipo `Decimal` de Prisma. Prohibido operar montos/pesos con `number`.                                                                                                                                                                                                                                                                             | Precisión exacta para kardex, costeo, IGV.                                                                                                                                                                                                              |
| D-004 | 2026-09-02 | Web: Next.js App Router + shadcn/ui + Tailwind + TanStack Query/Table + React Hook Form + Zod.                                                                                                                                                                                                                                                                                                                                    | Densidad de ejemplos para agentes; shadcn vive en el repo.                                                                                                                                                                                              |
| D-005 | 2026-09-02 | DB: Neon Postgres 17, proyecto `ayr-steel-erp`, región `aws-us-east-2`. Ramas: `main` (prod), `dev`, `ci`.                                                                                                                                                                                                                                                                                                                        | Wake ~300 ms; branching para tests.                                                                                                                                                                                                                     |
| D-006 | 2026-09-02 | Colas/jobs: **pg-boss** sobre Postgres. Sin Redis.                                                                                                                                                                                                                                                                                                                                                                                | Una pieza menos; suficiente para SUNAT, PDFs, importaciones.                                                                                                                                                                                            |
| D-007 | 2026-09-02 | Storage de archivos: Cloudflare R2 (API S3), bucket `ayr-steel-erp-docs`.                                                                                                                                                                                                                                                                                                                                                         | XML de facturas, planillas, PDFs.                                                                                                                                                                                                                       |
| D-008 | 2026-09-02 | Hosting API: Google Cloud Run `us-central1` (proyecto GCP `ayr-steel-erp`), deploy `--source`. Web: Vercel.                                                                                                                                                                                                                                                                                                                       | Sin sleep, free tier.                                                                                                                                                                                                                                   |
| D-009 | 2026-09-02 | Facturación electrónica vía Nubefact (sandbox hasta validación del cliente).                                                                                                                                                                                                                                                                                                                                                      | Proveedor ya conocido; cubre factura, boleta, NC/ND, GRE.                                                                                                                                                                                               |
| D-010 | 2026-09-02 | Auth propia: email+password (argon2), JWT access corto + refresh en tabla `sessions`; cambiar rol o desactivar usuario invalida sesiones (RF-03).                                                                                                                                                                                                                                                                                 | Sin dependencia externa; RF-01..04.                                                                                                                                                                                                                     |
| D-011 | 2026-09-02 | Calidad: ESLint estricto + typecheck + unit (Vitest/Jest) + E2E Playwright en CI. SonarCloud solo si su plan gratuito cubre repo privado; si no, Semgrep OSS.                                                                                                                                                                                                                                                                     | —                                                                                                                                                                                                                                                       |
| D-012 | 2026-09-02 | Agentes: Claude Code principal (auto mode + `/goal`). Antigravity CLI `agy` secundario, solo lectura/opinión (revisión, auditoría, research).                                                                                                                                                                                                                                                                                     | Nunca dos agentes editando el mismo archivo.                                                                                                                                                                                                            |
| D-013 | 2026-09-02 | App móvil fuera de alcance. RF-39 (terminal de operario) = ruta web responsive `/planta`.                                                                                                                                                                                                                                                                                                                                         | Alcance = app web lista para cliente.                                                                                                                                                                                                                   |
| D-014 | 2026-09-02 | Entorno de desarrollo: Windows (cmd/PowerShell). Scripts del repo deben ser cross-platform (`pnpm` scripts, sin bash-isms).                                                                                                                                                                                                                                                                                                       | Máquina del dueño.                                                                                                                                                                                                                                      |
| D-015 | 2026-09-02 | El web consume el API por proxy same-origin `/api/*` → `API_URL` (mecanismo actualizado por D-022). Cookies httpOnly `SameSite=Lax` sin `domain`. CORS del API solo para `WEB_ORIGIN`. Detalle en `DECISIONES.md`.                                                                                                                                                                                                                | Evita cookies de terceros entre Vercel y Cloud Run.                                                                                                                                                                                                     |
| D-016 | 2026-09-02 | La rama de producción de Neon se llama `production` (no `main`, corrige D-005). `dev` y `ci` cuelgan de ella. Endpoint prod `ep-square-cherry`.                                                                                                                                                                                                                                                                                   | El proyecto Neon ya venía creado así.                                                                                                                                                                                                                   |
| D-017 | 2026-09-02 | Versiones fijadas sin `^`: NestJS 11.2, Next 15.5, Prisma 6.19, TS 5.9, ESLint 9, Zod 3.25, Jest 29 (API). Detalle en `DECISIONES.md`.                                                                                                                                                                                                                                                                                            | Reproducibilidad; NestJS 12/Next 16/Prisma 7/TS 7 existen pero cambian el tooling.                                                                                                                                                                      |
| D-018 | 2026-09-02 | Reset de DB de pruebas = `prisma migrate deploy` + `TRUNCATE` (`apps/api/prisma/reset-test-db.ts`), con bloqueo si la conexión apunta a `production`. Nunca `migrate reset`.                                                                                                                                                                                                                                                      | Prisma bloquea `migrate reset` invocado por agentes; el truncate es más seguro.                                                                                                                                                                         |
| D-019 | 2026-09-02 | Deploy web: build remoto en Vercel desde la raíz del monorepo con `rootDirectory=apps/web` (proyecto `ayr-steel-erp-web`, ligado al repo GitHub → auto-deploy en push a `main`).                                                                                                                                                                                                                                                  | `vercel build` local falla en Windows (symlinks).                                                                                                                                                                                                       |
| D-020 | 2026-09-02 | Auth: access token JWT 15 min con `sid`; el guard consulta la sesión en cada request (una lectura indexada) para que revocar sea inmediato (RF-03). Refresh 7 días, rotado en cada uso.                                                                                                                                                                                                                                           | Simplicidad y revocación inmediata sobre rendimiento marginal.                                                                                                                                                                                          |
| D-021 | 2026-09-02 | SonarCloud analiza desde CI (`sonarqube-scan-action`, con cobertura lcov del API); Automatic Analysis del proyecto desactivado. Semgrep OSS solo si `SONAR_TOKEN` está vacío.                                                                                                                                                                                                                                                     | Ambos modos a la vez fallan; CI permite cobertura y bloquear el pipeline.                                                                                                                                                                               |
| D-022 | 2026-09-02 | El proxy `/api/*` del web es un Route Handler (`apps/web/src/app/api/[...path]/route.ts`, fetch server-side en runtime Node), no un `rewrites()` de `next.config.ts`.                                                                                                                                                                                                                                                             | Vercel bloquea rewrites declarativos hacia el dominio por defecto de Cloud Run (`*.a.run.app`) con `DNS_HOSTNAME_RESOLVED_PRIVATE`, falso positivo de su protección SSRF; un `fetch` normal no pasa por ese chequeo.                                    |
| D-023 | 2026-09-02 | `scripts/gcp-secrets.mjs` otorga explícitamente a la service account de Compute por defecto: `roles/secretmanager.secretAccessor` por secreto, y `roles/{storage.objectViewer,cloudbuild.builds.builder,artifactregistry.writer,logging.logWriter}` a nivel proyecto.                                                                                                                                                             | `roles/editor` (rol por defecto de esa cuenta) no basta para que Cloud Build lea el zip fuente de `gcloud run deploy --source` ni para que la revisión de Cloud Run lea Secret Manager.                                                                 |
| D-024 | 2026-09-02 | Los E2E de escritura contra producción corren solo vía `pnpm e2e:prod`, que crea un ADMINISTRADOR efímero (`e2e-admin@ayr.test`, contraseña aleatoria en memoria) y borra en `finally` todo usuario `e2e-...@ayr.test`. La cuenta real del dueño nunca se usa ni se modifica; `audit_log` no se toca.                                                                                                                             | Verificar RF-03 de punta a punta en producción exige crear y desactivar usuarios; hacerlo con la cuenta real obligaría a cambiar su contraseña, y dejar cuentas de prueba ensuciaría `/usuarios` del cliente.                                           |
| D-025 | 2026-09-03 | (P-02) El ERP emite comprobantes directo a SUNAT vía Nubefact desde la venta. RF-71 (importación de comprobantes) queda como ruta de histórico/contingencia, no como flujo normal.                                                                                                                                                                                                                                                | Emisión en línea es el flujo real del negocio; importar es solo para datos previos o caídas de Nubefact.                                                                                                                                                |
| D-026 | 2026-09-03 | (P-03) Una cotización confirmada genera una `production_orders` separada, con FK a `quotes`. Permite fabricar en parciales y reprogramar sin tocar la cotización original.                                                                                                                                                                                                                                                        | Separar la orden de la cotización evita que un cambio de programación de planta reabra el compromiso comercial ya aceptado por el cliente.                                                                                                              |
| D-027 | 2026-09-03 | **SUPERSEDIDA por D-037.** (P-04) La venta directa de bobina (sin transformar) crea un producto en la línea `trading`, con SKU `BOB-{finishCode}-{thicknessMm}-{widthMm}`, unidad kg; la venta referencia la bobina específica en el kardex.                                                                                                                                                                                      | Mantener todo lo vendible como `product` unifica el catálogo y el reporte de ventas; la referencia a la bobina concreta conserva la trazabilidad física que pedía P-04.                                                                                 |
| D-028 | 2026-09-03 | (P-05) Valorización de kardex por promedio ponderado, calculado por producto y por línea de negocio.                                                                                                                                                                                                                                                                                                                              | Más simple que PEPS, aceptado por SUNAT, y suficiente para el tamaño de operación.                                                                                                                                                                      |
| D-029 | 2026-09-03 | (P-06) Compras y ventas en PEN o USD. Se calcula un equivalente en PEN automático con el tipo de cambio SUNAT del día vía apis.net.pe (`APIS_NET_PE_TOKEN`), con caché en `exchange_rates` y fallback al último tipo de cambio conocido, editable a mano. Cada registro guarda `exchangeRate`, `exchangeRateSource` (API\|MANUAL) y `exchangeRateDate`. PEN es la moneda por defecto en ventas; USD permitido con la misma regla. | Evita depender de que el usuario tipee el TC en cada operación, sin bloquear el flujo si la API externa falla.                                                                                                                                          |
| D-030 | 2026-09-03 | (P-07) Módulo de compras completo, sin contabilidad: proveedor → compra tipada (`COIL`\|`FINISHED_GOOD`\|`SERVICE`\|`EXPENSE`) → recepción → cuenta por pagar → pagos. Las compras `EXPENSE` no generan movimiento de kardex. El formulario varía según el tipo; hay una lista central filtrable por línea de negocio. Se construye en Fase 2 junto con bobinas; sus RF se numeran al iniciar esa fase.                           | v1 no necesita asientos contables, pero sí necesita saber cuánto se debe a cada proveedor y de qué compra viene cada kilo o unidad que entra a inventario.                                                                                              |
| D-031 | 2026-09-03 | (P-08) Las secciones faltantes del documento origen eran clientes/proveedores y reportes: §4.7 = clientes y proveedores (RF-80..RF-89), §4.8 = reportes (RF-90..RF-94), agregadas en esta versión con su alcance mínimo.                                                                                                                                                                                                          | Confirma la hipótesis de P-08; sin estas secciones el catálogo de requisitos quedaba incompleto frente al docx original.                                                                                                                                |
| D-032 | 2026-09-03 | (P-09) Precio sugerido de venta = costo promedio ponderado del kardex × (1 + margen%). El margen (y el margen mínimo) vive por línea de negocio en `pricing_settings`, editable solo por ADMINISTRADOR. VENDEDOR puede subir el precio libremente pero no bajarlo del margen mínimo; bajar del mínimo requiere ADMINISTRADOR.                                                                                                     | Da un precio de partida objetivo sin congelar al vendedor a una lista fija, y pone un piso que protege el margen sin pasar por contabilidad.                                                                                                            |
| D-033 | 2026-09-03 | (P-10) El corte tercerizado admite varios proveedores de corte a la vez, marcados con `providesCuttingService` en `suppliers`. El costo por kg del servicio se ingresa al momento de recibir los flejes, no antes.                                                                                                                                                                                                                | El costo real de corte varía por proveedor y por lote; fijarlo de antemano no reflejaría lo que realmente se pagó.                                                                                                                                      |
| D-034 | 2026-09-03 | Se reordenan las fases de construcción (§3.7): Fase 1 = maestros (líneas, acabados, catálogo, clientes, proveedores, precios, tipo de cambio) + importación desde planilla; Fase 2 = compras + bobinas completas + kardex. Las fases 3 en adelante no cambian de contenido, solo de número relativo.                                                                                                                              | Bobinas y compras comparten el mismo kardex y las mismas cuentas por pagar; construirlas en la misma fase evita mockear compras para poder probar bobinas.                                                                                              |
| D-035 | 2026-09-03 | (P-11) El costo de producción de un producto terminado = materia prima consumida (costo promedio del kardex) + servicios directos imputados (corte tercerizado, flete) + un `overheadPerKg` por línea de negocio (campo `Decimal` nuevo en `pricing_settings`, editable solo por ADMINISTRADOR).                                                                                                                                  | v1 no lleva contabilidad de costos: un overhead por kilo configurable por línea absorbe la fábrica sin exigir centros de costo, y deja el costo del kardex comparable entre líneas.                                                                     |
| D-036 | 2026-09-03 | (P-08) Los reportes de v1 son exactamente cinco: inventario valorizado por línea, kardex por producto/bobina, ventas por período, cuentas por pagar por proveedor y cola de producción. §4.8 se reescribe como RF-90..RF-94 con esa lista; se elimina cualquier RF de reportes fuera de ella.                                                                                                                                     | Acotar los reportes a los cinco que el negocio pide de verdad evita construir un generador genérico en v1; exportar a Excel/CSV pasa a ser una propiedad de cada reporte, no un requisito aparte.                                                       |
| D-037 | 2026-09-03 | **Supersede D-027.** El SKU de la bobina para venta directa es `BOB{finishCode}{thicknessMm}`, sin ancho y sin guiones, uno por `typeKey` (RF-14). El ancho no entra al SKU porque no cambia el tipo de material vendido.                                                                                                                                                                                                         | Un SKU por acabado+espesor hace que el catálogo de `trading` no explote con una entrada por cada ancho comprado, y coincide exactamente con el `typeKey` de RF-14 que ya agrupa el inventario de bobinas.                                               |
| D-038 | 2026-09-03 | El costo con el que una bobina entra al kardex es su valor de compra **sin IGV**. El IGV se guarda por separado en la compra (`purchases.igv`) y nunca forma parte del `unitCost` del movimiento de inventario.                                                                                                                                                                                                                   | El IGV de compra es crédito fiscal, no costo del material; incluirlo inflaría el costo promedio y, con él, el precio sugerido de D-032.                                                                                                                 |
| D-039 | 2026-09-03 | Las cuentas por pagar admiten **pagos parciales** (`supplier_payments`, N pagos por compra). El saldo de una compra = total − pagos aplicados; el estado de cuenta por proveedor lista sus compras con saldo, su antigüedad y el total adeudado.                                                                                                                                                                                  | El negocio paga a sus proveedores en armadas; un modelo de pago único obligaría a partir compras artificialmente para reflejar la realidad.                                                                                                             |
| D-040 | 2026-09-03 | La merma es un movimiento de kardex de salida (`OUT`, `refType=MERMA`) valorizado al **costo promedio vigente** del ítem en el momento de registrarla. Anular una merma emite un movimiento inverso que referencia el original (`reversalOfId`); nunca se borra la fila.                                                                                                                                                          | Trata la pérdida física como cualquier otra salida de inventario, así el promedio ponderado y el valorizado por línea siguen cuadrando sin lógica especial.                                                                                             |
| D-041 | 2026-09-03 | La Fase 2 se ejecuta en dos sesiones: **2a** = kardex base, compras y alta de bobinas (3 vías); **2b** = partido de bobina, merma, cierre, anulación, edición y vistas de inventario de bobinas. §3.7 se actualiza para reflejar las dos sub-fases.                                                                                                                                                                               | El alcance completo de Fase 2 no cabe en una sesión con la calidad exigida (revisión, auditoría, E2E, deploy); partirla por dependencia técnica —2b necesita el kardex y las bobinas que crea 2a— mantiene el cierre de fase verificable en cada mitad. |

| D-042 | 2026-09-03 | El kardex se lleva **siempre en soles**: `inventory_movements.unitCost` e `inventory_balances.avgCost` guardan el costo ya convertido con el tipo de cambio de la operación. El documento (compra, bobina) conserva su moneda original y su `exchangeRate`. | Sin esto, comprar el mismo ítem una vez en USD y otra en PEN promediaba dos escalas distintas y el inventario valorizado (RF-90) sumaba dólares con soles. Detectado por el `revisor` al cerrar los puntos 1-3 de Fase 2a. |
| D-043 | 2026-09-04 | (P-12) **Landed cost.** Una compra `SERVICE` con `serviceKind` `FREIGHT`, `CUSTOMS` o `INSURANCE` puede vincularse a una compra `COIL` por `relatedPurchaseId`. Al recibirla, su costo **sin IGV** (en soles, D-042) se prorratea **por kilo** entre las bobinas de la compra vinculada: cada una recibe un movimiento `ADJUST` de **costo, no de cantidad** (`qty` = 0 lógico: el movimiento lleva la cantidad vigente y solo mueve `avgCost`) y su `unitCostPerKg` efectivo se actualiza. Default por recomendación del agente; el dueño puede revertirlo antes de Fase 3. | Sin esto, el flete y la aduana de una importación de bobinas quedan como gasto suelto y el costo promedio del acero sale por debajo del real, arrastrando el precio sugerido de D-032 y el costo de producción de D-035. El prorrateo por kg (y no por valor) es la práctica habitual cuando el servicio se contrata por peso transportado. |
| D-044 | 2026-09-04 | RF-22 (cancelar el plan de corte de una bobina) se implementa en **Fase 3**, junto con el plan de corte tercerizado (RF-40..42), no en 2b. | En 2b no existe todavía el plan de corte: no hay nada que cancelar. Anotado en §4.2. |
| D-045 | 2026-09-04 | RF-20: editar la **moneda o el tipo de cambio** de una bobina solo se permite si la bobina no tiene movimientos posteriores a su `IN` inicial. El cambio **recuesta** el ingreso vía reversa del `IN` original más un nuevo `IN` al costo corregido; nunca se hace `UPDATE` sobre un movimiento. Los campos que no afectan al kardex (ancho, notas) se editan libremente mientras la bobina esté `OPEN`. | El kardex es append-only (§3.2) y el promedio ponderado de D-028 es acumulativo: recostear un ingreso ya consumido por una salida o un partido reescribiría hacia atrás un promedio que ya valorizó otras operaciones. Reversa + nuevo movimiento deja la corrección visible en el kardex, que es justo lo que RF-95 pide. |
| D-046 | 2026-09-04 | **Quién anula qué** (precisa §3.4). SUPERVISOR_PLANTA puede deshacer lo que él mismo registra en planta: revertir un partido (RF-16) y anular una merma (RF-18). ADMINISTRADOR es el único que anula una **bobina** (RF-21), anula una **compra** y edita moneda, tipo de cambio o costo (RF-20, D-045). | Una merma mal tipeada o un partido con el ancho equivocado se detectan en el turno, y obligar a un administrador para corregirlos empuja a la planta a "arreglarlo" con otra merma compensatoria, que es peor para el kardex que la reversa. Anular una bobina o una compra, en cambio, toca el documento de compra y la cuenta por pagar, que no son de planta. Lo señaló el `revisor` como ambigüedad entre §3.4 y el controlador. |
| D-047 | 2026-09-02 | (P-13, cierra) **Consumo de producción.** El kg que consume una corrida de producción es el **kg teórico** calculado desde las dimensiones × `densityFactor` del acabado (drywall: kg/metro de perfil desde el fleje; coberturas: ancho × espesor × largo × densidad), con **override** de kg real que el operario puede escribir a mano. La diferencia entre el teórico (u override) y lo que el kardex de verdad tenía disponible se registra automáticamente como merma de proceso (`SCRAP`). Aplica a las dos líneas de transformación (drywall y coberturas). | Sin un cálculo teórico, cada corrida obligaría al operario a pesar y tipear a mano; con un teórico + override, el caso normal no exige nada extra y el caso real (la plancha pesó distinto de lo calculado) queda igual de auditable que cualquier otra merma (D-040), sin inventar un tercer mecanismo de ajuste. Se implementa en Fase 4. |
| D-048 | 2026-09-02 | **Reorden de fases por dependencia RF-31** (coberturas exigen cotización, §4.3). Fase 4 = producción drywall + terminal `/planta`. Fase 5 = cotizaciones + `production_orders` + producción de coberturas + ventas. §3.7 se reescribe con este orden. | RF-31 prohíbe producción suelta de coberturas: necesitan una cotización confirmada, que es de Fase 5. Meter coberturas en Fase 4 obligaría a mockear cotizaciones para poder probar producción, exactamente el problema que D-034 evitó entre compras y bobinas. Drywall no tiene esa dependencia y puede salir antes. |
| D-049 | 2026-09-02 | **Los flejes son bobinas.** `coils` gana la columna `kind COIL\|STRIP` (default `COIL`); un fleje es una fila `kind=STRIP` con `parentCoilId` a la bobina madre (comprada o, en corte tercerizado, la bobina enviada). Reusa el partido (RF-15/`planCoilSplit`), el código RF-13, el `typeKey` RF-14 y el kardex tal cual. El stock de flejes (RF-42) se agrupa por `typeKey` + `widthMm`, a diferencia del inventario de bobinas (RF-51), que agrupa solo por `typeKey`. | Un fleje no es una entidad nueva: es una bobina angosta con otro origen. Modelarlo como fila de `coils` evita duplicar código, kardex y trazabilidad; la única diferencia real con una hija de partido interno es el `refType` del movimiento (`CUTTING` en vez de `SPLIT`) y que el ancho sí importa para su stock, porque el drywall consume fleje por ancho exacto. |
| D-050 | 2026-09-02 | **Envío a corte no saca del kardex.** La bobina enviada a un tercero pasa a `status = IN_THIRD_PARTY` (nuevo valor de `CoilStatus`) sin movimiento de inventario: la mercadería sigue siendo propiedad de la empresa, solo cambió de ubicación física. Mientras está `IN_THIRD_PARTY` queda excluida de producción y del partido local (RF-15). Recién al **recibir** los flejes se emite la salida `OUT refType=CUTTING` de la madre y las entradas `IN` de los flejes, igual que un partido. | Un movimiento de kardex por un envío que puede no volver nunca (el proveedor de corte puede tardar semanas) inflaría el kardex con salidas que no son verdad —la empresa no perdió el material— y complicaría la reversa de RF-22 (cancelar antes de recibir) sin ganar nada: el kardex real ocurre en la recepción, que es cuando el peso y el ancho reales se conocen. |
| D-051 | 2026-09-03 | **Fase 3b se intercala entre Fase 3 y Fase 4.** Se agrega `CuttingService.reverse()`: deshace la recepción de una bobina de una orden de corte (RF-41 a la inversa), simétrico a RF-16. §3.7 gana la fila "3b". | Fase 3 quedó documentada con el hueco explícito ("no existe una reversa de recepción de corte") y dejó 3 bobinas madre de prueba en producción sin poder anularse por falta de esta reversa. Cerrarlo antes de Fase 4 evita que producción (que va a consumir flejes) herede el mismo hueco, y sigue el mismo criterio de secuenciación que D-041 (partir una fase grande en sub-fases con cierre verificable propio). |
| D-052 | 2026-09-03 | **Guardrails de `reverse()` (Fase 3b).** Además del criterio "todo o nada" de RF-16 sobre los flejes (ninguno con movimientos posteriores a su ingreso), la bobina madre debe estar `OPEN` o `CLOSED` (nunca `IN_THIRD_PARTY` de OTRO envío, nunca `CANCELLED`) y sin movimientos de kardex posteriores a la salida que se revierte. Si ambos guardrails pasan, el envío queda vivo por construcción: la fila vuelve a `SENT` y la bobina madre siempre a `IN_THIRD_PARTY`, nunca a un "disponible" ambiguo — si algo hiciera ambiguo ese estado, ya bloqueó antes. El mismo guardrail de `IN_THIRD_PARTY` se agregó retroactivamente a `revertSplit` (RF-16), que tenía el mismo hueco sin haberlo necesitado nunca hasta D-050. | RF-16 no necesitaba chequear `IN_THIRD_PARTY` porque antes de D-050 una bobina no podía "irse" sin dejar rastro de kardex; `send()` sí lo permite (D-050), así que una bobina madre partida o recibida puede reenviarse a otra orden de corte sin que ningún movimiento lo delate. Bloquear entero ante cualquier ambigüedad es más seguro que decidir en silencio un estado final "disponible" que no se puede verificar contra el kardex — mismo criterio conservador que ya usan RF-16 y RF-21. |
| D-053 | 2026-09-03 | **Fix del historial de migraciones (Sesión M-1).** La carpeta `20260903031603_fase3_corte_flejes` se renombró a `20260904125000_fase3_corte_flejes` (solo el nombre; el `.sql` no se tocó) para que el orden de las carpetas coincida con el orden real en que se aplicaron en cada rama de Neon (verificado contra `_prisma_migrations.started_at` antes de tocar nada: `fase1 → fase2a → fase2b → fase3 → fase3b`). Se actualizó `_prisma_migrations.migration_name` a mano en las **tres** ramas — `dev`, `production` y `ci` (verificado que `id`/`checksum` no cambiaron); `ci` necesitó además resolver un intento de `migrate deploy` fallido que el primer push a CI dejó a medias, porque esa rama tiene su propio historial persistente que un reset de datos (D-018) no toca. `prisma migrate status` queda limpio y `prisma migrate dev` reconstruye el shadow database sin errores en las tres. | Fase 3b había documentado que `prisma migrate dev` rompía el shadow database con `type "CoilStatus" does not exist`: la migración de Fase 3 hace `ALTER TYPE "CoilStatus" ADD VALUE`, pero `CoilStatus` se crea recién en la migración de Fase 2a, cuya carpeta ordena alfabéticamente **después** de la de Fase 3 aunque en la realidad se aplicó antes. Renombrar la carpeta (nunca el SQL ya aplicado) es la única forma de arreglar el reproceso desde cero sin tocar una migración ya viva en `production`. |
| D-054 | 2026-09-03 | (P-15, cierra) **Modelo de coberturas, cotización y reserva (Fase 5).** Coberturas se venden siempre a través de una cotización (RF-31); **cotizar no reserva** stock — es solo una simulación de precio. Cuando el vendedor confirma la cotización, una **transacción atómica** crea el pedido y una **reserva** por producto/kg. La reserva vive en un **ledger propio, aparte del kardex** (no es un movimiento de `inventory_movements`), con estados `ACTIVA`/`CONSUMIDA`/`LIBERADA`. Invariante que se aplica en todo el sistema: `disponible ≥ reservado`siempre — cualquier operación que rompería esa invariante (anular una compra o bobina, registrar merma, enviar a corte, o que otra venta consuma el mismo stock) queda bloqueada mientras la reserva esté`ACTIVA`. Una orden de producción (OP) consume la reserva (la deja `CONSUMIDA`); cancelar el pedido la deja `LIBERADA`. Sin vencimiento automático: una reserva vieja solo se libera a mano, con una alerta que avisa cuándo conviene hacerlo. Fase 4 deja la OP preparada para consumir contra una reserva **opcional** (drywall no depende de cotización, D-048); el módulo de ventas de Fase 5 es quien implementa el ledger de reservas completo. | Reservar en el kardex mismo (un movimiento `OUT`fantasma) ensuciaría el promedio ponderado y la trazabilidad de RF-90..94 con salidas que todavía no ocurrieron de verdad — el mismo motivo por el que D-050 mantuvo el envío a corte fuera del kardex. Un ledger aparte deja al kardex como lo que siempre fue (movimientos reales) y a la reserva como una promesa comercial verificable contra él vía la invariante`disponible ≥ reservado`, sin inventar un segundo mecanismo de ajuste para cada operación que hoy ya bloquea por estado (compra, bobina, merma, corte) — solo se les agrega este chequeo adicional. Decisión del dueño; el agente puede ajustarla si el grill de Fase 5 revela un matiz no cubierto aquí. |

| D-055 | 2026-09-03 | **La unidad primaria del producto terminado de drywall son las PIEZAS.** El kardex del perfil se lleva en `NIU` (piezas), no en kilos: `inventory_movements.qty` de un reporte de producción son piezas y los kilos son derivados (`piezas × kgPerPiece` de la receta, D-059). El producto de catálogo tiene que estar en `NIU` para poder tener receta. | Un perfil de drywall se vende, se cuenta y se despacha por pieza; llevar su stock en kilos obligaría a convertir en cada consulta y a decidir un factor en cada punto del sistema. El kilo sigue existiendo donde de verdad importa —el consumo del fleje, que es lo que el kardex descuenta— y se deriva del maestro, que es un solo lugar. Mezclar unidades en un mismo saldo ya está prohibido por el kardex (`InventoryService.record`), así que la unidad del producto se fija de entrada. |
| D-056 | 2026-09-03 | **Costo de la pieza en v1 = (costo real de los flejes consumidos + los prorrateos que ese costo ya arrastra) / piezas buenas.** El costo del fleje ya incluye el landed cost de la compra de la bobina (D-043) y el prorrateo del servicio de corte (RF-41), así que la OP no vuelve a imputar nada por su cuenta. La merma de proceso (D-057) **la absorben las piezas buenas**, no se destruye valor. **Sin mano de obra ni overhead estándar**: `production_orders.overhead_cost_pen` existe y vale siempre `0` en v1, y `productionCost()` deja el término explícito como hook de D-035 (`pricing_settings.overheadPerKg`) para que Fase 5 solo tenga que llenarlo. | Es el costeo por absorción habitual y el único que no exige un módulo de contabilidad de costos, que §0.3 excluye de v1. Dejar el término del overhead escrito y en cero —en vez de no escribirlo— evita que activarlo más adelante obligue a rehacer el cálculo, la columna y el DTO. |
| D-057 | 2026-09-03 | **La merma de proceso sale sola al cerrar la OP, por diferencia:** `kg de fleje asignados − kg teóricos de las piezas buenas reportadas`. Se emite como `OUT refType=SCRAP` sobre cada fleje (D-040), y su costo se traslada a las piezas buenas con un `ADJUST` sobre el producto terminado (mismo mecanismo que el landed cost de D-043). Si esa merma supera el **10 %** del material asignado, cerrar exige **motivo escrito**, que va al kardex y a la auditoría junto con el ratio. La merma del cierre **no** se puede anular por RF-18: se deshace reabriendo la orden. | Es la aplicación de D-047 al caso drywall: el operario no tiene que pesar ni tipear nada, y la diferencia entre lo que montó y lo que salió en piezas queda registrada como cualquier otra merma, auditable y reversible. Que el ajuste de costo cierre contra el material que salió es lo que hace que el kardex cuadre exacto. El motivo por encima del umbral lo pidió la auditoría de seguridad: sin él, cerrar con un fleje entero sin convertir era una baja de inventario sin explicación, mientras que la merma equivalente de RF-17 sí exige motivo. Y anularla por RF-18 devolvía los kilos **y** el valor al fleje mientras las piezas conservaban el costo absorbido — valor creado de la nada. |
| D-058 | 2026-09-03 | **La OP reporta piezas en N eventos y se cierra explícitamente** (mismo patrón que la recepción parcial de RF-41). Estados `DRAFT` → `IN_PROGRESS` → `CLOSED` \| `CANCELLED`; `DRAFT` mientras no tiene ningún fleje tomado. Cada reporte emite su kardex completo (salida del fleje por el kilo teórico, entrada de las piezas), así que el stock del perfil está disponible desde el primer parcial. | Una corrida de perfilado dura turnos: obligar a un único reporte al final dejaría el stock invisible mientras tanto y perdería el rastro de quién produjo qué. El cierre explícito es lo que fija el momento en que el costo real se conoce (D-056) y la merma se puede calcular (D-057). |
| D-059 | 2026-09-03 | **La receta vive en el maestro de productos** (`product_boms`, una por producto): qué fleje consume el perfil y cuántos kilos se lleva cada pieza. El fleje **no se identifica por SKU** —D-049 lo hace una fila de `coils`, no un producto de catálogo— sino por **acabado + espesor + ancho**, que es exactamente como RF-42 agrupa el stock de flejes. `kgPerPiece` se sugiere desde la geometría × `densityFactor` (D-047) y admite override del maestro. La OP valida contra la receta: un fleje que no coincide en los tres campos se rechaza. En Fase 4 solo la línea **drywall** tiene receta. | Sin receta, cada corrida obligaría a tipear el kilo por pieza y nada impediría montar el fleje equivocado. Identificar el insumo por acabado/espesor/ancho en vez de inventar un SKU de fleje evita duplicar el catálogo con entidades que D-049 decidió no crear. Coberturas quedan fuera porque exigen cotización (RF-31, D-048) y su receta tiene otra forma (largo variable por pedido). |
| D-060 | 2026-09-03 | **Consumir un fleje en una OP es asignarlo, y no mueve kardex** (mismo criterio que D-050 con el envío a corte): el fleje sigue en el almacén y en el saldo hasta que un reporte lo consume. A cambio, un fleje con asignación viva queda **bloqueado** para merma (RF-17), anulación de merma (RF-18), partido (RF-15), reversa de partido (RF-16), cierre (RF-19), edición de costo o ancho (RF-20/D-045), anulación (RF-21), anulación de su compra, imputación de landed cost (D-043) o de costo de corte (RF-41), reversa de la recepción de corte (D-052) y consumo en otra OP. El chequeo **bloquea las filas de los flejes antes de mirar** (`FOR UPDATE`), porque `consume` toma ese mismo lock: sin eso quedaba una ventana en la que el chequeo veía la lista vacía y un consumo concurrente se colaba. **Las tres reversas van en esta misma fase:** revertir un reporte de piezas (solo el último vigente; bloqueado si las piezas ya salieron o si otro cierre las recosteó), reabrir una OP cerrada (deshace merma y costeo) y anular la OP (solo sin reportes vigentes; libera los flejes sin tocar el kardex). | La asignación sin kardex es lo que permite que anular una OP no tenga nada que revertir y que el operario monte un rollo sin comprometer el inventario. Su precio es que ninguna de las reglas "sin movimientos posteriores" que protegen al resto la ve — el mismo hueco que D-050 abrió y que Fase 3 tuvo que tapar a mano en cuatro sitios—, así que el bloqueo se aplica explícitamente en todas las rutas que tocan un fleje. Las reversas van acá y no en una "Fase 4b" por la lección de D-051: cerrar el hueco después cuesta una sesión entera y deja residuos en producción mientras tanto. |
| D-061 | 2026-09-03 | **Sesión M-2.** Anular un pago a proveedor (cierra el supuesto que D-039 dejó pendiente desde Fase 2b: "anular un pago se resuelve junto con el resto de anulaciones", nunca construido). `SupplierPayment` gana `reversedAt`/`reversedById` (append-only, mismo criterio que `CoilSplit`/`CuttingOrderCoil`: la fila nunca se borra). `purchaseBalance`/`paidAmount` filtran los pagos anulados en el único lugar donde se suman, así que ningún llamador puede olvidarlo. Guardrail: un pago ya anulado no se vuelve a anular (409, idempotencia); la compra no puede estar `CANCELLED` (defensivo). Solo ADMINISTRADOR, mismo rol que `addPayment`/`cancel` (D-046). | El guardrail "aguas abajo" que de verdad importa ya existía en `cancel()` pero estaba roto: contaba **cualquier** pago, vivo o no, así que una compra con un pago —aunque ya no significara nada— quedaba bloqueada para anular **para siempre**. El fix es filtrar ese conteo a pagos vigentes; con la reversa nueva, ese bloqueo por fin se puede destrabar. |
| D-062 | 2026-09-03 | **Sesión M-2.** El `deny` de `Read(./.env*)`/`Read(**/.env*)` en `.claude/settings.json` (regla dura 5 de `CLAUDE.md`: nunca imprimir `.env.setup`) es **permanente y no removible por un agente**. Se había encontrado eliminado del árbol de trabajo al _cerrar_ Fase 4 (junto con `Bash(sed:*)` agregado al `allow`), de origen desconocido y anterior a esa sesión; se restauró entonces. Al abrir la Sesión M-2 sobre esta misma fase se verificó que seguía intacto — no volvió a faltar. | Documentado para que ningún agente futuro interprete la ausencia del `deny` como una configuración deliberada a respetar: si vuelve a faltar, restaurarlo es la acción correcta por defecto, no preguntar si "quizás se quitó a propósito". Queda pendiente que el dueño confirme si el cambio original fue intencional; si no lo fue, evaluar si `.env.setup` estuvo expuesto y si conviene rotar las credenciales que contiene. |
| D-063 | 2026-09-03 | **Permisos de diagnóstico y comandos desde la raíz.** `.claude/settings.json` suma al `allow` los comandos de solo lectura que el agente usa para orientarse (`grep`, `rg`, `head`, `tail`, `ls`, `find`, `wc`, `git log`, `git diff`, `git show`), y `CLAUDE.md` gana la regla dura 8: **nunca prefijar un comando con `cd ... &&`** — todo se corre desde la raíz del repo con rutas relativas — y **ningún comando de diagnóstico apunta a `.env*`**. El `deny` de `Read(./.env*)`/`Read(**/.env*)` (D-062) queda **intacto**: ampliar el `allow` de Bash no lo toca ni lo esquiva. | Cada `grep`/`head` pedía aprobación y el flujo se cortaba decenas de veces por sesión, sin que ninguno de esos comandos pueda cambiar nada. Los dos límites de la regla van juntos por un motivo: un `Bash(grep:*)` permitido es exactamente la puerta por la que un `.env*` podría leerse esquivando el `deny` de `Read`, y un `cd apps/api && …` mueve el directorio de trabajo bajo los pies del comando siguiente, que es como en Fase 0 una migración terminó aplicada contra la rama equivocada de Neon. |
| D-064 | 2026-09-03 | **El dominio comercial va únicamente en soles (PEN).** Cotización, pedido, precios de lista y PDF no tienen selector de moneda ni tipo de cambio. El USD existe solo en compras, con conversión obligatoria al TC de la fecha (D-042, que ya lleva el kardex en soles). | Decisión del dueño. La empresa vende en soles; un selector de moneda en ventas obligaría a decidir con qué TC se congela un precio cotizado, qué pasa si el TC se mueve entre la cotización y el pedido, y en qué moneda se emite el comprobante — tres preguntas sin respuesta de negocio que solo existirían para soportar un caso que no ocurre. Si algún día se vende en dólares, el campo se agrega junto al TC congelado en la cotización, igual que en compras. |
| D-065 | 2026-09-03 | **Un solo flujo cotización → pedido → reserva para todas las líneas**, con un flag en la línea de negocio (`business_lines.quotation_required`) que decide si la cotización es **obligatoria** (coberturas, RF-31) u **opcional** (perfiles, trading: admiten `POST /sales/orders` directo). El pedido es una copia congelada de la cotización; las dos comparten la resolución de líneas (`sales-lines.ts`), así que un pedido directo nunca admite lo que una cotización rechaza. Estados del pedido: `CONFIRMED` → `IN_PRODUCTION` \| `FULFILLED` \| `CANCELLED` (despacho real y cobranza son Fase 5b). | Construir dos flujos paralelos —uno "con cotización" y otro "sin"— duplicaría la validación de líneas, el cálculo de totales y la creación de reservas, y es exactamente por ahí por donde se esquivaría RF-31. Con un flag, la regla vive en un dato y no en dos ramas de código. |
| D-066 | 2026-09-03 | **El ledger de reservas y la invariante `disponible ≥ reservado`.** Una reserva apunta al ítem con el **mismo par `(itemType, itemId)` que usa `inventory_balances`**, así que se comprueba bajo el mismo lock de saldo que ya toma el kardex. La invariante se aplica en **dos formas**: (a) **cantidad**, dentro de `InventoryService.record` (salidas) y `reverse` (anulación de un ingreso) — el único punto por el que pasa toda salida de stock (§3.2), y con eso quedan cubiertas merma, partido, consumo de producción, anulación de compra o de bobina y cualquier ruta futura; (b) **custodia**, `assertNotReserved` como función suelta en las rutas que se llevan el ítem entero **sin mover kardex**: envío a corte (D-050), asignación a una OP ajena (D-060) y cierre de bobina (RF-19). La OP nacida del pedido es la excepción: monta su propia reserva. | La forma (a) sola no alcanza —enviar a un tercero o cerrar una bobina no mueve un gramo de kardex y aun así rompe la promesa—, y la (b) sola tampoco —una merma parcial sí mueve kardex y la custodia no la ve—. Es el mismo hueco que D-050 abrió y que Fase 3 tapó a mano en cuatro sitios, y que D-060 volvió a abrir con las asignaciones: la novedad de esta fase es reconocer que son **dos** clases de ruptura y no una, y ponerle a cada una su guardrail. |
| D-067 | 2026-09-03 | **Consulta de RUC/DNI contra apis.net.pe** (`GET /customers/lookup`), el **mismo proveedor** que ya sirve el tipo de cambio SUNAT (D-029): un solo token y un solo tercero del que depender. Es **opcional de punta a punta**: sin token, con la API caída o sin match, responde `found:false` con el motivo y el formulario sigue aceptando la captura manual. Nunca lanza. Throttle propio (20/min), igual que la subida de XML de compras. | Mismo criterio de fallback que D-029: un maestro de clientes que no se puede dar de alta porque un tercero está caído no es una opción, y el alta manual es el camino que siempre tiene que funcionar. El throttle propio existe porque cada llamada sale con nuestro token: un formulario en bucle consumiría la cuota compartida con el tipo de cambio. |
| D-068 | 2026-09-03 | **Precios, numeración y PDF.** Precio de lista **único por producto** en el maestro (`products.list_price_pen`, sin IGV y en soles); el vendedor puede editarlo en la línea, y la cotización guarda **los dos** (`listPricePen` vs `unitPricePen`), que es lo que hace auditable el descuento. Listas múltiples o por cliente quedan diferidas: entran como tabla aparte que sobreescriba ese valor, sin tocar lo construido. IGV fijo del 18 % calculado sobre el subtotal ya redondeado, con subtotal e IGV separados en el documento. Numeración correlativa por `serial` de Postgres: `COT-nnnnnn` y `PED-nnnnnn` (mismo criterio que `OP-nnnnnn`, D-058). PDF de la cotización a R2 al emitir, descargable por `GET /sales/quotations/:id/pdf`; la subida va **fuera** de la transacción y su fallo no impide emitir. | Guardar solo el precio cotizado perdería la referencia contra la que se concedió el descuento; guardar solo el de lista perdería lo que de verdad se le prometió al cliente. El correlativo por `serial` evita el contador propio con lock que ya costó una corrección en el código de bobina (RF-13). Que el PDF no bloquee la emisión sigue el criterio de D-030 con el XML: el hecho de negocio es emitir, el archivo es un adjunto regenerable. |
| D-069 | 2026-09-03 | **Vigencia y vencimiento de la cotización.** Estados `DRAFT` → `EMITTED` → `CONFIRMED` \| `EXPIRED` \| `CANCELLED`; vigencia por defecto de 7 días, editable al crearla. Un job diario de pg-boss (05:00 UTC = medianoche en Lima) marca `EXPIRED` las emitidas cuya fecha pasó, y `POST /sales/quotations/expire` (ADMINISTRADOR) hace lo mismo bajo demanda. **El job no es la regla**: `confirm()` revalida la vigencia por su cuenta y rechaza una cotización vencida aunque su estado siga diciendo `EMITTED`. Anular el pedido devuelve la cotización a `EMITTED` si sigue vigente, y a `EXPIRED` si no. | El API vive en Cloud Run con escalado a cero (§3.6): una instancia dormida no ejecuta ningún cron, así que un vencimiento que dependiera solo del job sería un vencimiento que a veces no ocurre — y "a veces no ocurre" sobre una regla comercial es peor que no tenerla. El job pone al día la lista; la verdad la sostiene la validación de `confirm()`. El endpoint manual existe además para poder probarlo de punta a punta. |
| D-070 | 2026-09-04 | **Fase 5b se reasigna: facturación electrónica + despacho/GRE + cobranzas.** Decisión del dueño. 5b entrega el ciclo **fiscal y logístico** sobre el pedido que 5a dejó construido: comprobante (factura, boleta, nota de crédito), guía de remisión electrónica remitente, despacho que mueve kardex y cierra el pedido, y cobranza con su reversa. La **producción de coberturas** (RF-30, RF-31, RF-33) y la **venta directa desde punto de venta** (RF-60, RF-64, RF-73) pasan a **Fase 5c**; la Fase 6 original (facturación Nubefact) queda absorbida por esta y se reduce a la **importación de comprobantes** (RF-71, RF-72). | El pedido de 5a ya prometía material y no tenía forma de convertirse en nada: ni salía del almacén, ni se facturaba, ni se cobraba. Cerrar ese ciclo entero vale más que producir un producto más, porque el hueco no era de un tipo de producto sino de todo el tramo posterior al pedido. Además, agrupar comprobante + guía + cobranza en una sola fase es lo que permite que las tres compartan el mismo puerto de PSE, el mismo job de reintento y el mismo criterio de reversa; repartirlas habría obligado a diseñar el puerto dos veces. |
| D-071 | 2026-09-04 | **Puerto `ElectronicInvoicingProvider` en el dominio; `NubefactProvider` es una implementación y la única.** El módulo `invoicing` define el puerto (`issueDocument`, `issueDispatchNote`, `queryStatus`, `voidDocument`) con tipos propios del dominio y lo inyecta por el token `ELECTRONIC_INVOICING_PROVIDER`. **Ningún archivo fuera de `invoicing/providers/` importa nada de Nubefact**, y ninguna forma de su payload aparece en el modelo de datos. La respuesta cruda del PSE se guarda tal cual en `fiscal_documents.provider_response` (JSON) y **el dominio nunca la lee**: solo se archiva para soporte y para poder reconstruir qué contestó el PSE ante una discrepancia con SUNAT. `NullInvoicingProvider` cubre el caso "sin credenciales" devolviendo un error de envío, que es exactamente la ruta de contingencia de D-073. | Decisión del dueño. Un PSE peruano se cambia (por precio, por caída, por quiebra) y ese cambio no puede ser una migración de base de datos ni un rediseño del ciclo de venta. El costo de la abstracción es una interfaz de cuatro métodos; el costo de no tenerla es que el vocabulario de un proveedor (`sunat_ticket_numero`, `aceptada_por_sunat`, sus códigos de error) se filtre a las columnas, a los estados y a la UI, y que reemplazarlo obligue a tocar todo. Guardar la respuesta cruda sin leerla es la otra mitad: se conserva la evidencia sin que el dominio dependa de su forma. |
| D-072 | 2026-09-04 | **Series y correlativos: un punto de emisión, y el correlativo se asigna al ENVIAR.** Una tabla `fiscal_series` con una serie activa por tipo de documento: `F001` (factura), `B001` (boleta), `FC01`/`BC01` (nota de crédito, elegida **según el tipo del comprobante afectado**) y `T001` (guía de remisión remitente). El correlativo se toma con `UPDATE … SET correlative = correlative + 1 … RETURNING` —el mismo patrón atómico que `suppliers.coil_seq` (RF-13)— **en el momento de enviar el documento al PSE, no al crear el borrador**. Un borrador no tiene número; un documento rechazado **conserva el suyo** y la corrección se emite con un correlativo **nuevo**. | El correlativo es un recurso fiscal, no un identificador de aplicación: SUNAT exige que sea consecutivo y sin huecos por serie. Asignarlo al crear el borrador significa que todo borrador abandonado abre un hueco, y los borradores se abandonan todo el tiempo. Asignarlo al enviar significa que un número existe solo si hubo intento real de emisión. Que el rechazado conserve el número y la corrección tome otro es la regla de SUNAT y también lo honesto: el documento rechazado ocurrió, queda en el historial, y pretender reutilizar su número escondería un intento fallido que la administración ya vio. |
| D-073 | 2026-09-04 | **Contingencia: la operación nunca se detiene por el PSE.** Decisión del dueño. Enviar un comprobante hace tres cosas en este orden: (1) asigna correlativo y deja el documento en `ISSUED` —"emitido, pendiente de envío"— **y confirma la transacción**; (2) intenta el envío **fuera** de esa transacción; (3) según el resultado pasa a `ACCEPTED`, `REJECTED` o `SEND_ERROR`. Desde el paso (1) el documento **ya habilita el despacho**: la mercadería sale aunque el PSE esté caído. Un job de pg-boss (`invoicing.send`, con reintento y backoff) más un barrido cada 15 minutos reintentan los `ISSUED`/`SEND_ERROR`; `POST /invoicing/documents/:id/retry` los reintenta a mano. Los que pasan de `invoicing_settings.alert_after_hours` sin aceptación se marcan como alerta en la lista y en el contador del menú. `invoicing_settings.provider_offline` (solo ADMINISTRADOR) fuerza la contingencia sin tocar credenciales. | Es la misma lección de D-069, en un lugar donde duele más: el API escala a cero en Cloud Run (§3.6), así que un job **no puede** ser la única garantía de que algo ocurra. Por eso el intento inline del paso (2) es el camino normal —el caso feliz es inmediato y verificable de punta a punta— y el job es la red que recoge lo que ese intento no pudo. El orden importa: si el envío ocurriera **dentro** de la transacción, una caída del PSE revertiría el correlativo ya tomado y abriría el hueco que D-072 evita, o peor, dejaría a un camión esperando a que un tercero conteste. El interruptor manual existe porque durante una caída conocida del PSE reintentar cada 15 minutos solo agrega ruido. |
| D-074 | 2026-09-04 | **El despacho cierra el pedido; la facturación no.** Decisión del dueño. Un despacho cubre líneas del pedido (total o parcialmente) y **mueve kardex** como salida (`InventoryRefType.SALE`); cuando la suma despachada de **todas** las líneas alcanza lo pedido, el pedido pasa a `FULFILLED`, y mientras haya algo despachado y algo pendiente, a `PARTIALLY_FULFILLED` (estado nuevo). Emitir, aceptar o anular un comprobante **no cambia** el estado del pedido. Como el despacho es el que saca el material, es también el que **consume la reserva** (`markReservationConsumed` antes del movimiento de kardex) y su reversa la **restaura** (`restoreReservation`), exactamente igual que la orden de producción de D-060. | El ciclo físico y el fiscal corren a distinta velocidad y se pueden cruzar en cualquier orden: se despacha y se factura después, se factura por adelantado y se despacha en tres tandas, se anula una factura y la mercadería ya está en casa del cliente. Un pedido "atendido" describe un hecho del almacén, no uno de SUNAT, y atarlo al comprobante haría que anular una factura _desatendiera_ un pedido cuya mercadería ya salió. Que el despacho consuma la reserva sale del mismo razonamiento de D-060: la salida de kardex chocaría contra la invariante de D-066 con su propia reserva si no se marca consumida antes, y toda reversa aguas abajo tiene que devolver la promesa o el material queda desprotegido. |
| D-075 | 2026-09-04 | **Cobranzas: espejo exacto de los pagos a proveedor (D-039, D-052, D-061).** Un cobro (`customer_payments`) va **contra un comprobante**, no contra el pedido; el saldo se **recalcula, nunca se almacena**; el comprobante lleva condición de pago (`CONTADO`/`CREDITO`) y, en crédito, fecha de vencimiento derivada de `customers.credit_days`. La reversa de un cobro sigue el patrón de M-2: la fila **nunca se borra**, se marca `reversed_at`/`reversed_by_id`, el monto vuelve al saldo y el motivo queda en la auditoría. Registrar un cobro es de ADMINISTRADOR y VENDEDOR; revertirlo, solo de ADMINISTRADOR (D-046). Sin letras, sin retenciones y **sin cálculo de detracción**: si aplica, se captura como campo informativo (código, porcentaje y monto escritos a mano) y viaja al PSE tal cual. | El saldo pendiente ya está resuelto en compras y resolverlo distinto en ventas daría dos verdades sobre la misma pregunta. Que el cobro vaya contra el comprobante y no contra el pedido es lo que hace que el saldo cierre cuando hay facturación parcial o notas de crédito: el pedido no es un documento de cobro. La asimetría de roles con compras (donde registrar un pago también es de ADMINISTRADOR) es deliberada: compras es un módulo de planta al que VENDEDOR no entra, y la cobranza es parte del trabajo del vendedor. La detracción se difiere porque su cálculo depende del catálogo 54 de SUNAT y de reglas por tipo de bien que no se pueden adivinar; capturarla informativamente no impide emitir y no inventa un número. |
| D-076 | 2026-09-04 | **RF-85 cambia: VENDEDOR da de alta y edita clientes.** Decisión del dueño. VENDEDOR puede **crear** un cliente y **editar sus datos básicos** (razón social, dirección, correo, teléfono). Siguen siendo exclusivos de ADMINISTRADOR: la **baja lógica**, el cambio de **tipo y número de documento** y los **días de crédito**. Los proveedores no cambian: siguen siendo solo de ADMINISTRADOR. | 5a dejó al vendedor con el botón "Buscar RUC" (D-067) y sin ninguna puerta por la que meter el cliente que acababa de encontrar, así que cotizar a un cliente nuevo exigía interrumpir a un administrador. Los tres campos que quedan reservados son los que tienen consecuencia fuera del maestro: el documento es la identidad fiscal con la que sale el comprobante, los días de crédito definen la fecha de vencimiento de una cuenta por cobrar (D-075) y la baja lógica esconde al cliente de todo el sistema. |
| D-077 | 2026-09-04 | **Boleta "público en general" contra un cliente del sistema, sembrado e inmutable.** Decisión del dueño. La migración siembra un cliente `PÚBLICO EN GENERAL` con documento `00000000` y `customers.is_system = true`; la API rechaza editarlo o darlo de baja y la UI no ofrece ninguna de las dos. Solo se le emiten **boletas**; al PSE viaja con tipo de documento "sin documento". Pasado el tope de **S/ 700** (regla SUNAT para boleta sin identificar) el bloqueo es **suave**: la emisión se detiene pidiendo un cliente identificado, y **solo ADMINISTRADOR** puede forzar la excepción, que queda registrada en el comprobante (`generic_customer_override_by_id`) y en la auditoría. | La venta menor de mostrador existe y sin esto obligaría a inventar un cliente por cada una, ensuciando el maestro con filas irrepetibles. Sembrarlo como fila del sistema —en vez de tratarlo como un caso especial sin cliente— deja que todo el resto del ciclo (comprobante, cobranza, saldo) funcione sin ramas. El bloqueo suave con excepción registrada es el punto medio que pidió el dueño: la regla se aplica sola en el caso normal, y cuando alguien decide saltársela queda escrito quién y sobre qué documento, que es justo lo que un requerimiento de SUNAT necesita poder mostrar. |
| D-078 | 2026-09-04 | **GRE remitente con modalidad de traslado elegida por despacho.** Decisión del dueño. Cada despacho declara si el traslado es **privado** (placa del vehículo + nombre, documento y licencia del conductor) o **público** (RUC y razón social del transportista); la guía se arma con una rama u otra. La **GRE del transportista** queda fuera de alcance: solo se emite la del remitente. El **catálogo de vehículos y conductores frecuentes se difiere**; v1 captura texto libre y lo autocompleta con los valores usados en despachos anteriores (`GET /dispatches/transport-suggestions`), sin tablas ni ABM nuevos. | Subcontratar el flete es normal en el rubro y una modalidad fija habría dejado sin guía a la mitad de los despachos; que se elija por despacho y no en configuración es porque la modalidad la decide el envío concreto, no la empresa. El catálogo se difiere porque dos maestros con su ABM y su UI ensanchan la fase sin resolver nada que el autocompletado no resuelva: la sugerencia sale de los datos reales ya cargados, no cuesta una tabla, y si mañana hacen falta atributos propios del vehículo (capacidad, certificado) el catálogo entra sin migrar lo capturado. |
| D-079 | 2026-09-04 | **La guía de remisión no se da de baja por el puerto: se da de baja en el panel del PSE y el sistema la reconcilia.** El proveedor no reconoce una GRE aceptada en su operación de anulación (responde _"el documento no existe o no fue enviado"_), así que `voidDocument` no tiene camino para ella. El camino oficial es: dar la guía de baja en el panel del PSE y usar **«Consultar al PSE»** sobre ella, que la lee con `consultar_guia`, reconoce el marcador de anulado y la pasa a `VOIDED` — con lo que se desbloquea la reversa del despacho. El mensaje del bloqueo lo dice completo. **La asimetría es del proveedor, no del dominio**: el puerto define `voidDocument` para los cuatro tipos de documento y un PSE que sí soporte la baja de una GRE funciona sin cambiar nada. | Es la única operación de esta fase que no se puede completar desde el sistema, y esconderla habría sido peor que documentarla: una guía vigente bloquea la reversa del despacho —con razón, porque deshacer una salida que un documento vigente declara dejaría al kardex y a SUNAT contando cosas distintas—, así que sin salida el despacho quedaba trabado **para siempre**. Se eligió reconciliar en vez de debilitar el guardrail o inventar una operación del proveedor que no se pudo verificar. Queda anotada acá porque es exactamente el tipo de detalle que hay que revisar al cambiar de PSE (D-071): el contrato del puerto se cumple, lo que falla es una implementación, y descubrirlo costó una corrida completa contra el proveedor real. |
| D-080 | 2026-09-04 | **Producción se despliega sin credenciales del PSE.** Decisión del dueño. `deploy-api.mjs` no inyecta `NUBEFACT_URL` ni `NUBEFACT_TOKEN` y `gcp-secrets.mjs` no las sube a Secret Manager, así que Cloud Run ata `NullInvoicingProvider` y **toda emisión real cae en la contingencia de D-073**: toma correlativo, habilita el despacho y queda pendiente de envío. El pase a la cuenta real de Nubefact es una sesión propia con su checklist, en la que se agregan las dos líneas que los scripts dejan documentadas. | La única cuenta disponible hoy es la **demo**, y desplegarla a producción dejaría al sistema a un descuido de distancia de marcar como _aceptado por SUNAT_ un comprobante que SUNAT nunca vio — un dato que después se usa para cobrar, para el saldo por cobrar y para responder un requerimiento. Que el módulo funcione sin PSE no es una degradación: es el camino que D-073 diseñó y probó, así que producción queda operativa en todo lo demás (despacho, kardex, pedidos, cobranza, pantallas) y la facturación espera. La alternativa —no desplegar nada— habría dejado fuera de producción una fase entera por una credencial. |
| D-081 | 2026-09-04 | **`e2e:prod` nunca puede emitir contra el PSE real, ni por accidente de entorno.** Sesión M-3. `scripts/e2e-prod.mjs` fuerza `E2E_FISCAL_EMISSION: '0'` en el `env` que le pasa a Playwright, como última entrada del spread — gana pase lo que pase en el shell de quien invoca el script. Antes de esto, `E2E_FISCAL_EMISSION=1` en el entorno del operador se colaba sin que el script lo tocara. Con D-080 vigente (Cloud Run sin credenciales del PSE) el hueco era inofensivo porque toda emisión caía en contingencia; deja de serlo el día que se cargue la cuenta real (el pase de esa sesión sigue pendiente, ver D-080 y `docs/handoff/m-3.md`). `pnpm prod:purge-e2e` se auditó aparte y ya era seguro sin cambios: solo actúa sobre comprobantes cuyo cliente o notas empiezan con `E2E `. | Un correlativo real gastado en un comprobante de prueba no se puede deshacer sin dejar hueco en la numeración fiscal (D-072), y un `ACCEPTED` real contra un cliente inventado es un documento que SUNAT sí vio. El fix vive en el único lugar donde importa —el script que corre contra producción— en vez de depender de que cada operador recuerde no exportar la variable; así la protección no depende de la disciplina de quien teclea el comando. |
| D-082 | 2026-09-04 | **La producción de coberturas se numera Fase 6 y §3.7 se renumera.** Decisión del dueño. La fila `5c` de §3.7 pasa a llamarse **6**, "importación de comprobantes" pasa a **7** y "auditoría, reportes, hardening, UAT" a **8**. La fase entrega producción de coberturas metálicas contra pedido (RF-30..RF-33, RF-36) y el maestro de colores; **quedan fuera y pasan a Fase 7** la cola de producción y su indicador de menú (RF-37, RF-38) y el punto de venta directo (RF-60). RF-73 (venta directa de bobina) ya lo cubre D-037 desde Fase 2a, y la venta de UPVC es compra-venta pura (D-091). | El dueño abrió la sesión llamándola "Fase 6" y pidiendo el handoff en `docs/handoff/fase-6.md`: dos fases con el mismo número serían un rastro imposible de seguir en `PROGRESO.md`. Renumerar hacia adelante cuesta tres filas de tabla y deja la numeración otra vez densa; el alcance recortado respecto de la vieja `5c` es deliberado — la cola y el POS no dependen de coberturas y sí dependerían de decisiones de UI que esta fase no toca. |
| D-083 | 2026-09-04 | **Conviven dos tipos de producto terminado de cobertura, y una cotización puede mezclarlos.** Decisión del dueño. (a) **Plancha catálogo**: SKU con largo fijo, unidad `NIU`, stock general, línea de cotización simple (cantidad × precio por pieza), fabricada con el mismo flujo de la Fase 4. (b) **A medida**: producto base **sin largo**, unidad **`MTR`**, línea de cotización **compuesta** con subítems `{cantidad, largo}` que totalizan metros lineales y kilos teóricos; el precio se cotiza **por metro lineal**. El kardex del producto a medida se lleva en metros y los largos viven en las tablas de detalle (`quotation_item_pieces`, `sales_order_item_pieces`, `production_order_items`, `production_report_pieces`). | Es la realidad del rubro: una plancha de largo comercial se produce para stock y se vende como cualquier producto, y una cobertura a medida se corta al largo del techo del cliente. Llevar el kardex del producto a medida en **piezas** metería una plancha de 3 m y una de 9 m en el mismo saldo y en el mismo promedio ponderado, que es exactamente lo que D-055 evitó para drywall al fijar la unidad de entrada; en metros, el costo por metro es uniforme y el kardex no miente. La unidad en el maestro (`products.unit`) es lo que separa los dos tipos, y el kardex ya prohíbe mezclar unidades en un saldo, así que la separación se sostiene sola. |
| D-084 | 2026-09-04 | **La OP de coberturas nace SIEMPRE de un pedido, y sus subítems se copian automáticamente y son editables.** Decisión del dueño. `POST /production/roofing` exige `reservationId`: no hay corrida de coberturas sin pedido detrás (RF-31). Al crearla, los subítems `{cantidad, largo}` de la línea del pedido se copian a `production_order_items`, y planta puede ajustarlos antes y durante la corrida (el techo real se mide en obra y el largo cambia). Lo que se reporta son los largos **reales**, no los planificados. | RF-31 prohíbe la producción suelta de coberturas y D-048 reordenó dos fases enteras por esa dependencia: exigir la reserva en el schema del endpoint es la forma de que la regla no dependa de que alguien la recuerde. Copiar los subítems evita que planta retipee la lista y que un error de tipeo convierta el pedido en otra cosa; dejarlos editables reconoce que el plan de corte es una intención y la corrida es un hecho — el mismo motivo por el que D-047 dejó el kg real como override del teórico. |
| D-085 | 2026-09-04 | **El color es un maestro propio y un `colorId` estructurado; el SKU lo refleja pero nadie lo parsea.** Nueva tabla `colors` (`code`, `name`, `hexColor`, `isActive`), CRUD de ADMINISTRADOR con baja lógica y auditoría. `products.color_id` (nullable) y `coils.color_id` (nullable: las prepintadas lo llevan, las galvanizadas no). El código de SKU **refleja** el color para que se lea a simple vista, en el espíritu de D-037, pero **todo filtrado y todo matching es por `colorId`**, nunca leyendo el SKU. El emparejamiento entre bobina y producto es por **igualdad estricta**, con `NULL` incluido: un producto sin color solo monta bobina sin color. | Parsear un SKU es exactamente cómo se pierde el rastro cuando alguien renombra un código: D-059 ya había decidido identificar el fleje por acabado+espesor+ancho en vez de inventarle un SKU, y esta es la misma lección aplicada al color. La igualdad estricta —y no "si el producto tiene color, compara" — es lo que impide montar una bobina prepintada en un producto galvanizado sin decir nada; con `NULL` tratado como comodín, el único caso que el filtro dejaría pasar en silencio sería justo el que más caro cuesta deshacer. El `hexColor` existe para que el selector muestre la muestra real: en planta se elige el rollo por lo que se ve, no por el nombre. |
| D-086 | 2026-09-04 | **Selección de bobina en la OP de coberturas: abierta + espesor dentro de tolerancia + color igual, y montarla es custodia.** El filtro ofrece bobinas `kind=COIL`, `status=OPEN`, con saldo de kardex, de la misma línea de negocio, con `thicknessMm` dentro de `[espesorReceta − TOL, espesorReceta + TOL]` y `colorId` igual al del producto (D-085). `TOL` es una constante compartida (`ROOFING_THICKNESS_TOLERANCE_MM`, **0.02 mm**), con override por variable de entorno del API y **sin UI en v1**. Montar la bobina en la OP la asigna sin mover kardex y la deja bajo el mismo bloqueo transversal de D-060: no se puede mermar, partir, cerrar, editar, anular, enviar a corte ni montar en otra OP mientras la orden viva. | La bobina que compra el proveedor viene con el espesor nominal y una tolerancia de laminación: exigir igualdad exacta dejaría fuera del filtro rollos perfectamente válidos, y no filtrar nada dejaría montar un 0.45 en un producto de 0.30. Que la tolerancia sea una constante y no un campo de UI sigue el criterio de `MAX_SCRAP_RATIO_WITHOUT_REASON` (D-057): un número que la operación no cambia todos los días no necesita pantalla, y una pantalla lo convertiría en algo que se puede aflojar para esquivar el filtro. La custodia no se reinventa: D-060 ya la resolvió para el fleje y la roladora consume la bobina exactamente igual. |
| D-087 | 2026-09-04 | **Una sola OP con `kind`, dos servicios.** `production_orders` y `production_reports` ganan `kind DRYWALL \| ROOFING` y se comparten enteros: el correlativo `OP-nnnnnn`, los estados de D-058, `reservation_id`, la auditoría y el listado unificado de `/produccion`. La receta también es una sola tabla: `product_boms` gana `kind` y `input_width_mm`/`piece_length_mm`/`kg_per_piece` pasan a nullable (solo DRYWALL las usa). Lo que **no** se comparte es el servicio: `ProductionService` queda como está y la rama de coberturas vive en `RoofingProductionService`, con `lockOrder`, `recomputeStatus`, `restoreReservationIfIdle` y el mapeo a DTO extraídos a `production-shared.ts`. | Evaluado contra el código real de Fase 4, como pedía el dueño: `production.service.ts` tiene 1421 líneas y es drywall en cada método —`bom.kgPerPiece`, `theoreticalKg(pieces)`, match de fleje por acabado+espesor+ancho, `allocateStripKg`—, así que una rama `if (kind)` por método duplicaría cada guardrail en el mismo archivo y dejaría los dos flujos compartiendo bugs por accidente. Partir la **tabla** en cambio habría duplicado el correlativo, el ledger de reservas y el listado, que es lo que de verdad tiene que ser uno solo: una OP es una OP para el operario y para la auditoría, aunque la máquina sea otra. |
| D-088 | 2026-09-04 | **La reserva se traslada del insumo al producto terminado, y el despacho lee la reserva viva y no la línea congelada.** Al reportar largos, la OP de coberturas descuenta de la reserva de **bobina** los kilos que consumió (`consumeReservationQty`, D-074) y **crea una reserva nueva sobre el producto terminado** por los metros que acaban de entrar al kardex: las piezas a medida nacen reservadas para el pedido que las encargó. `reservations.sales_order_item_id` deja de ser único y pasa a `@@unique([sales_order_item_id, item_type, item_id])`, así que una línea puede sostener a la vez la promesa de materia prima que le queda y la de producto ya fabricado. El despacho toma las coordenadas de kardex (`itemType`/`itemId`/unidad) de la **reserva viva** de la línea, no de `sales_order_items.reserve_*`, que sigue congelado como el registro de lo que se prometió al confirmar. Una línea que se fabrica contra el pedido —la que trae largos— **no vuelve nunca al insumo**: sin producto terminado reservado, el despacho se rechaza en vez de sacar kilos de la bobina, y la reserva de producto se topa contra lo que la línea todavía debe, así que sobre-producir deja stock libre y no una promesa que nadie libera. | Es un **hueco real de Fase 5b**, encontrado leyendo el código y no por una prueba: `dispatches.service.ts` saca del kardex `orderItem.reserveItemType/reserveItemId`, que en una cobertura es la **bobina**. Como la OP ya sacó esos kilos al reportar, despachar los habría sacado por segunda vez — el kardex habría cerrado con material que nunca existió, y en perfiles y trading el defecto no se ve porque el ítem reservado es el propio producto. Trasladar la promesa en dos filas (una consumida de verdad, una nueva) y no mutando una sola es lo que mantiene el ledger append-only en el sentido del proyecto: la fila que se consumió describe un hecho que ocurrió, y la que nace describe uno nuevo. Es además lo que hace cierta la invariante `disponible ≥ reservado` sobre las piezas: mientras el pedido no las despache, ninguna otra venta, merma ni OP se las puede llevar. |
| D-089 | 2026-09-04 | **Merma por despunte: planta declara los kilos que la bobina consumió de verdad, y el saldo no consumido vuelve al almacén.** Al cerrar la OP de coberturas, el operario escribe `consumedKg` (por defecto, la suma de los kilos teóricos reportados, o sea merma cero). La diferencia `consumedKg − Σ kg teóricos` sale como `OUT refType=SCRAP` sobre la bobina, valorizada al costo promedio vigente (D-040), y su costo se traslada a los metros buenos con un `ADJUST` sobre el producto terminado, exactamente el mecanismo de D-057. Lo que quedó **asignado y no consumido** se libera sin merma: la bobina vuelve a estar disponible con su saldo intacto. Si la merma supera `MAX_SCRAP_RATIO_WITHOUT_REASON` (10 %) del material consumido, cerrar exige motivo escrito. Los kilos del despunte **también descuentan la reserva** antes de emitirse, por el mismo motivo que los del reporte: sin eso, una orden que reservó el rollo entero no se podía cerrar con merma, porque la propia promesa bloqueaba la salida. | El patrón literal de D-057 —"todo lo asignado que no llegó a ser pieza es merma"— es cierto para un fleje, que entra entero a la perfiladora, y **falso** para una bobina de coberturas, que se queda montada en la roladora y cuyo sobrante sigue siendo inventario: aplicarlo tal cual habría dado de baja rollos enteros al cerrar cada orden. Declarar el consumo real es la forma que D-047 ya había previsto para este caso ("override de kg real que el operario puede escribir a mano"), y deja la merma de despunte —los centímetros que se pierden en cada corte— como lo que es: una diferencia pequeña, auditable y con el mismo umbral de motivo que cualquier otra merma. |
| D-090 | 2026-09-04 | **El rolado es con máquina propia: el costo de la cobertura es material más lo que ese material ya arrastra.** Decisión del dueño. `costo del metro = (costo real de los kilos de bobina consumidos, con su landed cost de D-043 ya dentro, más la merma de despunte absorbida) / metros buenos`, que es D-056 sin cambios. **No hay servicio de rolado tercerizado** en v1, así que la OP no imputa nada por su cuenta. `production_orders.overhead_cost_pen` sigue existiendo y sigue valiendo `0`: el hook de mano de obra y overhead de D-035 queda diferido igual que en Fase 4. | El costeo por absorción de D-056 ya estaba escrito y probado para drywall y no necesita ni una variante: el insumo cambia (bobina en vez de fleje) pero la aritmética es idéntica. Reescribir el término del overhead ahora, sin módulo de contabilidad de costos que lo alimente (§0.3 lo excluye de v1), habría sido inventar un número; dejarlo en cero y explícito es lo que permite activarlo después sin tocar el cálculo, la columna ni el DTO. |
| D-091 | 2026-09-04 | **UPVC (`roofing`) es compra-venta pura en v1; no se construye producción de UPVC.** Decisión del dueño. La línea se modela con productos `FINISHED_GOOD` comprados (D-030) y vendidos con el ciclo comercial normal: cotización opcional, kardex sobre el propio producto, despacho y comprobante de Fase 5b. Esta fase solo **verifica** que el catálogo lo modele bien y lo cubre con un E2E de compra → venta → kardex. Si algún día se fabrica UPVC, reutilizaría la OP de coberturas de D-087 con otro `kind`, no una entidad nueva. | La línea vende perfiles y planchas de PVC importados: no hay bobina, no hay rolado y no hay nada que transformar, así que una OP de UPVC en v1 sería una tabla vacía con una pantalla que nadie abre. Dejar dicho por dónde entraría —el `kind` de la OP, que ya existe— es lo que evita que la próxima sesión que lo necesite crea que tiene que empezar de cero, que es la lección que D-051 dejó cara. |
| D-092 | 2026-09-04 | **La cola de Fase 7 solo cubre coberturas contra pedido; la reposición de catálogo/drywall queda diferida.** Decisión del dueño. v1 muestra en cola únicamente pedidos de la línea Metallic Roofing fabricados a medida o de catálogo contra pedido (Fase 6). Reponer stock de drywall o de plancha de catálogo sin un pedido detrás no entra: se deja el hook de origen de demanda (ver D-093) para que una fila de cola futura por "mínimo de inventario" no exija rediseñar la señal, solo una segunda forma de producirla. | Diseñar la cola para dos orígenes de demanda a la vez —pedido y reposición— antes de que exista reposición (§3.7 Fase 8+, fuera de esta sesión: "NO HACER" excluye mínimos de inventario) habría sido construir una abstracción sin segundo caso real que la valide. |
| D-093 | 2026-09-04 | **La cola es una vista derivada, no una tabla ni un estado nuevo del pedido.** Un pedido está `EN_COLA` cuando tiene una reserva `ACTIVE` sobre una bobina (`itemType=COIL`) del producto de la línea, ese producto tiene receta activa (`productBom.count({isActive:true}) > 0`, la misma señal de "se fabrica contra el pedido" que ya usa `resolveDispatchTarget`, D-088) y ninguna OP `DRAFT`/`IN_PROGRESS` cuelga de esa reserva; pasa a `EN_PRODUCCION` en cuanto existe esa OP; sale de la cola cuando la reserva se consume, se libera o el pedido se anula. Ningún campo nuevo en `sales_orders` codifica este estado: se recalcula en cada lectura a partir de `Reservation` + `ProductionOrder`, exactamente el "hook de origen de demanda" de D-092 (mañana, una reposición por mínimo de inventario sería otra función que produce la misma forma de fila, no una migración). | La señal ingenua `reserveItemType === 'COIL'` es **falsa** para RF-73 (venta directa de bobina, D-037): esa línea también reserva una bobina y no fabrica nada. La sesión de Fase 6 ya había resuelto exactamente esta ambigüedad para el despacho con el conteo de `productBom` activo; reusarla en la cola es la aplicación literal de "derivar, no duplicar" también a la lógica, no solo a los datos. |
| D-094 | 2026-09-04 | **Orden de la cola: prioridad manual > semáforo > FIFO por confirmación.** Decisión del dueño. El desempate final es `SalesOrder.createdAt` (el pedido nace exactamente al confirmar, no hace falta un campo `confirmedAt` nuevo). La prioridad es excepcional y de ADMINISTRADOR: marcarla exige motivo (`priorityReason`) y queda en `audit_log` además de en `priorityAt`/`priorityById` del propio pedido, con el mismo patrón que `cancelledById/At`; quitarla borra esos tres campos y el pedido vuelve a ordenarse solo por semáforo y FIFO. Entre dos pedidos priorizados, gana el que se priorizó primero (`priorityAt` ascendente) — misma lógica FIFO, aplicada al evento de priorizar. | Guardar la prioridad en el propio pedido (y no solo en `audit_log`) es lo que permite ordenar la cola con una consulta, sin releer el historial de auditoría en cada carga; el audit_log sigue siendo la fuente de verdad para "quién y cuándo" según RF-95, y el pedido solo cachea el último valor vigente, igual que ya hace con la cancelación. |
| D-095 | 2026-09-04 | **Planta jala: la cola es la pantalla de entrada de `/planta`, y crear la OP desde la tarjeta es el único punto de entrada al flujo de Fase 6.** El botón "Iniciar producción" de cada tarjeta sigue llamando a `POST /production/roofing` sin cambios; lo único que cambia es que ya no se elige a partir de una lista plana de reservas activas (`RoofingPickerCard`) sino de la cola ordenada por D-094, con sus subítems, kilos teóricos y semáforo visibles antes de decidir qué tomar. `/produccion` expone la misma cola en vista de administración, con los controles de prioridad y fecha que `/planta` no tiene. | Cambiar el flujo de creación de la OP (Fase 6, ya probado con 101 E2E en producción) para acomodar la cola habría sido el tipo de riesgo que este proyecto evita: se cambia el punto de entrada, no el contrato que ya funciona. |
| D-096 | 2026-09-04 | **`fechaEntregaPrometida` es opcional, del pedido, y solo el vendedor la fija al crearlo.** Se pasa como parámetro opcional a `confirm()`/`createDirect()`; una vez que el pedido existe, solo ADMINISTRADOR la cambia, por un endpoint dedicado (`PATCH /sales/orders/:id/promised-delivery-date`), sin pasar por `revokeAllSessions` porque no es una acción de cuenta (regla dura 4 no aplica). El semáforo de la cola es `VENCIDO` (fecha < hoy de Lima), `PROXIMO` (fecha es hoy o mañana — el proxy de calendario para "menos de 48 h" que una columna `DATE` sin hora puede dar), `A_TIEMPO` (después) o `SIN_FECHA` (nulo), calculado con `businessToday()` (D-069), nunca con UTC. | La cotización no tiene pedido todavía (nace recién al confirmar, D-054), así que no hay dónde guardar una fecha prometida antes de ese momento: "editable por el vendedor hasta confirmar" solo puede leerse como "en el acto de confirmar". Fijarla ahí y no en una edición posterior evita un estado intermedio (pedido confirmado sin fecha, luego editado) que nadie pidió y que hubiera exigido su propio permiso. |
| D-097 | 2026-09-05 | **Al cerrar una OP de coberturas, lo que sobra de la reserva de bobina del pedido se libera entero (`releaseRemainingReservation`), y la señal "se fabrica contra el pedido" de la cola exige `ProductBom.kind = ROOFING`.** Encontrado por `revisor` al cerrar Fase 7: `reserveKg` es una estimación del vendedor y casi nunca coincide exacto con lo que la corrida termina gastando (teórico + despunte), y D-086 encima permite rolar una bobina distinta a la reservada — en los dos casos la reserva original quedaba `ACTIVA` con saldo, sin nada que la tocara nunca, y el pedido no salía de la cola ni despachado entero. `close()` ahora libera ese saldo con `RELEASED` (no `CONSUMED`: nada de esa bobina se volvió producto por esta vía) al terminar, deliberadamente sin reversa en `reopen()` — reabrir no depende de esa reserva para nada. Aparte, `findProductionQueue`/`computeQueueStatus` exigían solo receta activa; ahora exigen receta activa **de coberturas**, para que una línea que reservó bobina para un producto con receta `DRYWALL` (posible a nivel de datos aunque el web nunca lo arma) no entre a la cola con la aritmética de otra línea. | El primer defecto es el mismo patrón que ya costó tres bloqueantes en Fase 6 (D-088): la promesa se mueve entre ítems y hay que revisar cada punto que la lee o la descuenta, y este punto —el cierre exitoso, el camino más común, no un borde— no se había revisado porque Fase 6 nunca tuvo un lector como la cola que hiciera visible una reserva viva sin dueño. `RELEASED` y no `CONSUMED` es la distinción correcta y no cosmética: `restoreReservationQty` ya rechaza restaurar una `RELEASED`, así que usar el estado equivocado habría dejado una reversa fingiendo que podía devolver un kilo que nunca se volvió producto. |
| D-098 | 2026-09-05 | **El mostrador vende stock del propio producto, y solo eso.** Decisión del dueño. `/pos` cubre la venta de mostrador: productos **en stock** (plancha de catálogo, perfiles, UPVC), cliente genérico o identificado, **contado** y entrega inmediata. Quedan fuera y no tienen forma de entrar: material a medida, cotización y cola de producción. La regla no se aplica con un flag sino con la **forma del contrato**: `posSaleItemInputSchema` no tiene `reserveFromCoilId` ni `pieces`, así que una línea de mostrador nunca puede prometer una bobina; encima, `createDirectInTx(..., { counterSale: true })` exige que **cada línea esté respaldada por su propio producto**, y el buscador excluye la unidad `MTR`, que es la marca de una cobertura a medida (D-083). A cambio, el mostrador **se salta `business_lines.quotation_required`** (D-065): una plancha de catálogo en stock se vende sin cotizar. | Lo que RF-31 protege no es "la línea metallic-roofing" sino "lo que hay que fabricar contra el pedido", y `quotation_required` es una aproximación de línea entera a esa idea. Exigir el respaldo por producto propio es la misma protección **más fina**: una línea que se fabrica contra el pedido no tiene su producto terminado en stock, así que no puede pasar el filtro, y una plancha que sí está en el almacén no tiene por qué exigir una cotización que nadie va a leer. Poner la regla en la **ausencia de campos** y no en una validación es lo que la hace difícil de romper por descuido: no hay nada que un llamador futuro pueda mandar de más. |
| D-099 | 2026-09-05 | **El POS no es un camino paralelo de stock: es UI rápida sobre los servicios que ya existían.** Decisión del dueño, no negociable. `PosService.sell` compone en **una sola transacción** `SalesOrdersService.createDirectInTx` + `DispatchesService.createInTx` + `InvoicingService.createInTx`/`assignInTx` + `ReceivablesService.addPaymentInTx`. Para eso, el cuerpo de esas cuatro operaciones pasa a aceptar la transacción del llamador (`*InTx`) y los métodos públicos siguen abriendo la suya. El POS **no importa `InventoryModule`**: no escribe kardex, no toca reservas y no comprueba disponible por su cuenta. Todos los guardrails e invariantes aplican tal cual, incluida `disponible ≥ reservado`: si al mostrador no le alcanza, la venta se rechaza con el mismo mensaje que cualquier pedido. El envío al PSE queda **fuera** de la transacción, exactamente como manda D-073. | La alternativa —un "camino rápido" que descuente stock por su cuenta— es el defecto que D-088 costó tres bloqueantes descubrir, multiplicado: cada invariante del proyecto tendría que volver a demostrarse en un segundo lugar, y la primera vez que las dos ramas divergieran el kardex mentiría sin que ninguna prueba lo notara. Que el módulo dependa solo de `sales` e `invoicing` es la prueba estructural de que no lo hace: si necesitara `InventoryService` sería porque estaría moviendo stock por fuera. El costo es la refactorización a `*InTx`, que además deja el patrón listo para el siguiente compositor de operaciones. |
| D-100 | 2026-09-05 | **Anular una venta de mostrador es la cadena de reversas que ya existe, en el único orden que funciona, y solo dentro del turno abierto.** Cobro → comprobante (baja o nota de crédito según `voidPathFor`, D-072) → despacho → pedido: el mismo orden que `prod:purge-e2e`. Solo ADMINISTRADOR (D-046) y solo mientras el turno siga `OPEN` — anular una venta de un turno ya arqueado movería el efectivo esperado de una caja que alguien firmó, y ahí el camino es la nota de crédito desde `/comprobantes`. **Exige un comprobante `ACCEPTED`**: ni la baja ni la NC existen sobre uno que el PSE todavía no resolvió, así que con producción sin credenciales (D-080) una venta de mostrador no se anula hasta que el comprobante se resuelva, y el mensaje lo dice. Aparte, se corrige un hueco de Fase 5b que esto destapó: la reversa de un despacho se bloqueaba por **cualquier** comprobante vigente del pedido, y una boleta no se da de baja de forma individual —su único camino es la NC, que la deja `ACCEPTED` para siempre—, así que el despacho de toda venta con boleta era irreversible y el propio mensaje de error ofrecía un camino que no desbloqueaba nada. Ahora bloquea solo lo que **todavía** factura: una línea acreditada por completo por notas vivas dejó de estarlo. | Encadenar las reversas existentes en vez de escribir una "anulación de venta" propia es la decisión de D-099 aplicada al camino de vuelta: cada eslabón ya tiene su guardrail, su auditoría y su prueba, y una anulación propia los tendría que repetir. El hueco de la boleta no apareció antes porque ninguna prueba había necesitado revertir el despacho de una venta con boleta: Fase 5b probó la NC y probó la reversa, nunca las dos sobre la misma línea. Exigir la aceptación en vez de inventar una "anulación interna" es la parte incómoda y la correcta: un correlativo gastado existe, y deshacerlo por dentro dejaría al job de D-073 mandando después el comprobante de una venta que ya no existe. |
| D-101 | 2026-09-05 | **Caja v1: un turno abierto por usuario, arqueo solo del efectivo, esperado congelado al cerrar.** Decisión del dueño. Apertura con monto inicial → ventas del turno con su medio (`EFECTIVO | TARJETA | YAPE_PLIN | TRANSFERENCIA`, que son `CASH`/`CARD`/`WALLET`/`TRANSFER` sobre el `PaymentMethod` de D-039/D-075, no un enum nuevo) → cierre con arqueo: esperado contra contado, diferencia registrada y **motivo obligatorio** si difiere. La diferencia la calcula el API con `expectedCash` de `@ayr/shared`, la misma función que la pantalla usa mientras el cajero cuenta. **Solo el efectivo entra al arqueo**: una venta con tarjeta o Yape no pone billetes en el cajón. Cerrar una caja **cuadrada** es del cajero; cerrarla **con diferencia** exige ADMINISTRADOR. "Un turno abierto por usuario" lo garantiza la base con la columna `open_user_id` única, que vale `user_id` mientras el turno está `OPEN` y `null` en cuanto se cierra. | El turno existe porque **nada más en el sistema sabe a qué caja entró un cobro**: `customer_payments` guarda cuánto y con qué medio, no dónde. Congelar el esperado al cerrar en vez de recalcularlo es lo que hace que una anulación posterior no mueva el número contra el que el cajero firmó — el mismo criterio con el que `sales_order_items.reserve_*` conserva lo prometido aunque la reserva viva cambie. El candado en la base y no solo en el servicio es porque dos aperturas simultáneas del mismo cajero (dos pestañas, un doble toque) partirían la caja en dos turnos que nadie sabría cuadrar, y esa es justo la carrera que un mostrador provoca. Exigir ADMINISTRADOR solo con diferencia deja al cajero la operación que más hace y reserva el rol para la decisión que de verdad compromete dinero. |
| D-102 | 2026-09-05 | **Impresión v1: el PDF de Fase 5b alcanza; el ticket de 80 mm queda diferido, y la contingencia se avisa en pantalla.** Decisión del dueño. Cerrar una venta muestra el número del comprobante y su PDF (`GET /invoicing/documents/:id/pdf`), sin plantilla nueva. Mientras producción corra sin credenciales del PSE (D-080) o con el interruptor manual levantado (D-073), `/pos` muestra un aviso permanente de **"comprobantes en contingencia"** —`GET /pos/context` devuelve `providerConfigured` y `providerOffline`— y el diálogo de venta cerrada repite que ese comprobante quedó pendiente de envío. | El ticket térmico exige una plantilla propia, un ancho fijo y una impresora que la operación todavía no tiene; el PDF ya está probado y se imprime en cualquier lado. El aviso, en cambio, no es cosmético: en un mostrador hay alguien esperando su boleta, y la diferencia entre una operación conocida y un cliente preguntando por un papel que no llega es decirlo **antes** de cobrar. El hook para el ticket es el propio endpoint del PDF: cambiar la plantilla no cambia ninguna llamada. |
| D-103 | 2026-09-05 | **`TransferMode.PICKUP` — recojo en mostrador: sin transporte, sin peso y sin guía de remisión.** Una venta de mostrador crea un despacho de verdad (mueve kardex y cierra el pedido, D-074) pero el traslado lo hace el **comprador** con su propio medio, así que el remitente no emite guía. La modalidad nueva no lleva vehículo, ni conductor, ni transportista —lo obliga el `CHECK` reescrito de `dispatches`— ni peso bruto, y **no tiene código en el catálogo 18 de SUNAT** porque nunca llega a un documento: `issueDispatchNote` la rechaza, y el tipo del puerto (`Exclude<TransferMode, PICKUP>`) hace que ni siquiera compile mandarla al PSE. | Las dos modalidades de Fase 5b describen quién transporta **por nosotros**, y ninguna es cierta en un mostrador: rellenar una placa y un conductor inventados para pasar el `CHECK` habría metido datos falsos en una tabla que alimenta guías reales, y elegir "transporte público" con el cliente como transportista habría puesto su documento en el campo del transportista contratado. Modelar la tercera situación real cuesta un valor de enum y una rama del `CHECK`, y además le sirve a cualquier recojo en tienda fuera del POS. Que el tipo del puerto excluya `PICKUP` convierte "esto no va al PSE" en algo que el compilador sostiene y no en un comentario. |
| D-104 | 2026-09-05 | **Una venta de mostrador es de una sola línea de negocio.** El carrito se ata a la línea del primer producto; el buscador marca los que no caben y el API rechaza el carrito mezclado nombrando los dos productos que chocan. Vender drywall y UPVC al mismo cliente en el mismo momento son dos ventas de mostrador. | `sales_orders.business_line_id` es uno solo desde D-054, y el POS no abre un camino paralelo (D-099). Las dos salidas eran cambiar el modelo del pedido —para un caso de mostrador, tocando la fase entera— o partir la venta en dos pedidos, que daría **dos comprobantes por una sola compra**, y eso es peor para el cliente que la restricción. Se elige lo tercero: decirlo antes, en el buscador, donde cuesta un toque corregirlo. |
| D-105 | 2026-09-05 | **Un comprobante importado es un comprobante de primera clase con un origen distinto, y sin contraparte del otro lado.** `fiscal_documents.origin` (`ISSUED_HERE` \| `IMPORTED`) separa lo que el ERP emitió de lo que entró ya emitido por planilla (RF-71): histórico anterior al sistema, o una emisión de contingencia hecha en el portal de SUNAT durante una caída. Vive en la misma tabla porque es el mismo documento para todo lo que importa —la cuenta por cobrar, el estado de cuenta del cliente, los reportes, la búsqueda—, y **nace `ACCEPTED`** porque SUNAT ya lo recibió. Lo que no puede hacer es hablar con el PSE: ni enviarse, ni reintentarse, ni consultarse, ni darse de baja, ni recibir una nota de crédito emitida acá. Los estados frenan la mitad (un `ACCEPTED` no lo toca `send`, `retry` ni `correct`); la otra mitad la frena `assertIssuedHere`, y el web apaga esos botones en vez de ofrecerlos para que el API los rechace. La baja y la nota de crédito de un importado se hacen donde se emitió, y el resultado se vuelve a importar. | Guardarlo aparte habría obligado a que cada lector del módulo —cobranzas, reportes, el estado de cuenta— hiciera una unión, y la primera consulta que se olvidara de hacerla habría dejado una deuda invisible. El corte va en el **origen** y no en el estado porque es lo único cierto: "aceptado" quiere decir dos cosas distintas según de dónde salió el documento, y en un importado el ERP no vio esa aceptación, la afirma la planilla. Mandarle una nota de crédito propia habría sido peor que inútil: el PSE no lo conoce como nuestro, así que la nota nacería referida a un documento que para el proveedor no existe. |
| D-106 | 2026-09-05 | **Un importado no toma correlativo, pero sí empuja el de su serie; y la serie que no existe se crea inactiva.** Trae su número del papel, así que `allocateNumber` no interviene. A cambio, si su correlativo viene **más arriba** que el de `fiscal_series`, la serie se adelanta hasta él. Si la serie no está registrada, se crea con `isActive = false`: el histórico cuelga de ella y el asignador de correlativos, que solo mira series activas, no la ve. Una serie ya registrada para otro tipo de documento rechaza la fila nombrando los dos tipos. | Sin el empujón, el ERP habría vuelto a entregar un número que SUNAT ya tiene emitido: dos comprobantes con el mismo número no son un choque de índice, son un problema fiscal. Crearla **inactiva** es lo que evita el efecto colateral opuesto —que importar el histórico habilite a emitir por una serie que nadie configuró ni confirmó con el contador—, y deja el alta explícita donde corresponde, en `/configuracion/series`. |
| D-107 | 2026-09-05 | **El importador genérico aprende a agrupar: N filas de planilla pueden ser una sola entidad.** `ImportAdapter` se parte en `RowImportAdapter` (el de siempre: una fila, una entidad) y `GroupedImportAdapter`, que agrega `groupKey`, `validateGroup` y `createGroup`. El comprobante entra por el segundo, con una fila por línea y la cabecera repetida en todas —la forma en la que cualquier sistema de facturación exporta lo emitido—. Un grupo se valida entero (que sus líneas sumen su total dentro de un céntimo por línea, que la cabecera diga lo mismo en cada fila, que ninguna línea esté rota) y se confirma entero, en una transacción: o entran todas sus líneas o no entra el documento. Corregir una fila en la previsualización **revalida su grupo completo** —y también el que abandonó, si la edición le cambió el número—, porque si no el veredicto quedaba pegado en las otras filas. Aparece además `import_rows.warnings`: lo que hay que ver antes de confirmar sin que bloquee. | La alternativa era una fila por comprobante con las líneas embutidas en una celda, que ninguna planilla real tiene y que nadie puede corregir renglón por renglón en el preview. Validar el grupo no es un lujo: un comprobante cuyas líneas no suman su propio total entra como una deuda equivocada en la cuenta del cliente, y no hay forma de verlo mirando una línea sola. Y la revalidación del grupo entero al editar es la parte que la primera versión no tenía: sin ella, arreglar el precio de una línea dejaba a sus hermanas mostrando un error que ya no era cierto y al documento sin confirmar. |
| D-108 | 2026-09-05 | **Reimportar archiva la versión anterior; no la pisa (RF-72), y solo alcanza a lo importado.** La fila anterior se marca (`archived_at`) y conserva sus líneas y su historial; la nueva nace apuntándola (`supersedes_document_id`, único, misma forma que la reemisión de D-072). La unicidad de `number` pasa a ser **parcial** (`WHERE archived_at IS NULL`): pueden convivir el archivado y el vigente con el mismo `F001-00000123`, lo que no puede haber es dos vigentes. Tres puertas cerradas: un comprobante que **emitió el ERP** no se reemplaza importando; uno con **cobros vigentes** tampoco (primero se revierte el cobro); uno con **notas de crédito vivas** encima, tampoco. El listado muestra solo la vigente; a la archivada se llega desde el detalle de la que la reemplazó. | Es la misma regla que el proyecto aplica a todo lo que ya ocurrió: se marca, no se borra. Pisar la fila habría destruido la única prueba de qué decía el archivo anterior, que es justo lo que se quiere auditar cuando alguien reimporta. Las tres puertas son la misma idea desde tres lados —**solo se reimporta lo que se importó, y solo mientras nada se apoye todavía en la versión anterior**—: mover un cobro de un total a otro, o dejar una nota de crédito acreditando un documento que cambió de importe, son dos formas de romper un saldo en silencio. Un `CHECK` no puede exigir que las dos puntas aparezcan juntas porque viven en filas distintas; eso lo sostiene la transacción, y el `CHECK` ata lo que sí es por fila: que solo un importado archive o quede archivado. |
| D-109 | 2026-09-05 | **El comprobante importado nace con su saldo entero por cobrar; lo ya cobrado se registra con el flujo de cobranzas de siempre.** La planilla no trae columna de "pagado" y el importador no crea cobros. | Es lo que se quiere en el caso que de verdad importa: la emisión de contingencia hecha en el portal durante una caída del PSE es **nueva y sin cobrar**, y tiene que aparecer como deuda el mismo día. El histórico ya cobrado es el caso raro —D-001 dejó dicho que no hay data real que migrar— y admitirlo habría exigido inventar el medio de pago y la fecha de cada cobro viejo, o dejar entrar cobros sin ninguno de los dos. Registrar el cobro a mano después de importar cuesta un formulario y no miente. |
| D-110 | 2026-09-05 | **Un comprobante importado se anula por dentro, con un estado propio y sin SUNAT.** `POST /invoicing/documents/:id/annul` lleva un `fiscal_document` con `origin = IMPORTED` de `ACCEPTED` a **`ANNULLED`**, con motivo obligatorio, solo ADMINISTRADOR y sin tocar el PSE; su saldo pasa a cero (`documentBalance`) y sale de cuentas por cobrar. Guardrails: los mismos dos de la baja —cobro vigente y nota de crédito viva—, más el rechazo de una versión ya archivada por reimportación (RF-72), idempotencia con 409 (D-052) y rechazo explícito sobre un comprobante que el ERP emitió, cuyo camino sigue siendo el fiscal de D-072. Tres `CHECK` lo atan en la base: las tres columnas de la constancia van juntas, `ANNULLED` es privativo de `origin = IMPORTED`, y un anulado siempre tiene fecha. | RF-71 dejó un importado naciendo `ACCEPTED` con su cuenta por cobrar y **sin ningún camino de vuelta**: el PSE no lo conoce como nuestro (D-105), así que ni la baja ni la nota de crédito lo alcanzaban, y un importado equivocado era deuda falsa permanente. Es la cuarta aparición de la misma lección —D-061 con el pago a proveedor, D-088 y D-097 con la reserva—: **la reversa se construye en la misma fase que lo que revierte, o no se construye.** Y el estado es propio y no `VOIDED` por lo mismo que D-105 separó el origen del estado: `VOIDED` afirma que SUNAT aceptó una baja, un hecho que sobre un importado nunca ocurrió, así que reusarlo habría hecho que el papel dijera algo falso ante una auditoría. El corte por `CHECK` y no solo por servicio es lo que impide que un `UPDATE` a mano haga desaparecer del balance una deuda **real** sin que SUNAT se entere, que es justo lo que la anulación interna no es. |
| D-111 | 2026-09-05 | **La rama `dev` de Neon se repone reseteándola desde `production` (`pnpm db:reset-dev`), nunca con SQL a mano.** El guion usa `neonctl branches reset dev --parent`: **resetea, no borra** —la regla dura del proyecto prohíbe borrar ramas—, conserva id, nombre y endpoint (así ninguna cadena de conexión queda inservible) y exige `--yes` porque pierde todo lo que viva solo en `dev`. `--preserve-under-name` deja el estado anterior a mano si hace falta volver. | Hay residuo de pruebas que **la aplicación no puede borrar por diseño**: un comprobante importado no se elimina —desde D-110 se anula, y la fila y su serie inactiva quedan igual—, y lo mismo vale para todo lo append-only del proyecto. Limpiar eso con `DELETE` a mano sobre una base con historial fiscal es exactamente lo que este proyecto no hace en ningún otro lado, y hacerlo "solo en dev" entrena la mano equivocada para el día que alguien se confunda de rama. Reponer la rama entera es la operación honesta: no discrimina qué borrar, no deja media transacción deshecha, y deja `dev` diciendo lo mismo que producción, que es lo que uno quiere de un entorno de desarrollo. |
| D-112 | 2026-09-05 | **Toda fecha de un timestamp en el web pasa por `formatTimestampDate` (zona de Lima), nunca por `slice(0, 10)` sobre el ISO.** Cortar un `Timestamptz` con `.slice(0, 10)` lo lee en UTC; Lima va cinco horas detrás, así que todo lo ocurrido después de las 19:00 locales se mostraba fechado al día siguiente. `no-restricted-syntax` en `packages/eslint-config/next.mjs` lo bloquea en dos formas —`x.algoAt.slice(0, 10)` y `toISOString().slice(0, 10)`— con el mensaje que dice qué usar en su lugar. | Encontrado en la Sesión M-4 (nueve pantallas) y corregido de una pasada en la Fase 7d, junto con el mismo defecto en el formulario de tipo de cambio (`tipo-cambio-view.tsx`), que no estaba en el listado de M-4 porque ahí el timestamp cortado no se mostraba: se usaba como valor por defecto de un formulario, y el síntoma era ofrecer registrar el tipo de cambio de mañana en vez del de hoy después de las 19:00. El lint es la forma de que esta clase de defecto no vuelva a aparecer pantalla por pantalla: sin él, cada corrección es un parche puntual y el próximo desarrollo reintroduce el mismo corte en un lugar nuevo. |
| D-113 | 2026-09-05 | **Paginación server-side con `page`/`pageSize` (1-based), envuelta en `PaginatedResult<T>` (`{ items, total, page, pageSize }`), en las 10 tablas de la Fase 7d que crecen sin techo.** `packages/shared/src/schemas/pagination.ts` centraliza el contrato: `paginationQuerySchema`, `toSkipTake`, `paginate`, y `paginateInMemory` + `DERIVED_FILTER_FETCH_CAP` (5000) para el caso híbrido de un filtro **derivado** (`pendingOnly` en comprobantes, `onlyWithBalance` en compras, `receivables()` entero) que no se puede expresar en SQL sin duplicar la fórmula del saldo (D-075). Un selector que necesita "todo" para un autocompletado (clientes, pedidos facturables/despachables, compras vinculables) usa `fetchAllForPicker` — pide `pageSize=MAX_PAGE_SIZE` (200) y no pagina de verdad. El kardex de un solo ítem (`itemId` en la query) tampoco pagina: necesita el historial completo para el saldo corrido, y se devuelve como una única página que ya lo trae entero. `page` tiene tope (`MAX_PAGE=10_000`, hallazgo de la auditoría de seguridad de la fase): sin él, `pageSize` topado no alcanzaba a acotar `skip` — un `page` arbitrariamente grande seguía siendo un `OFFSET` arbitrariamente grande para Postgres. Detalle en `DECISIONES.md`. | Necesario para UAT con datos reales: sin techo, una tabla de clientes o comprobantes con miles de filas volvía la vista inutilizable. Se descartó `page`/`limit` sin envoltura (no hay dónde poner `total`) y cursor-based (la UI pide "página N de M" y tamaño de página, que un cursor no da gratis). No se adoptó `@tanstack/react-table` pese a estar declarado como dependencia: reescribir diez tablas ya probadas y muy personalizadas justo antes de la entrega al cliente era más riesgo que valor frente a un hook + componente livianos. |
| D-114 | 2026-09-05 | **La afordancia de "esto navega" vive en una sola constante (`LINK_CLASSNAME` en `apps/web/src/lib/utils.ts`): `text-primary underline-offset-4 hover:underline`, la misma combinación que ya trae `variant="link"` de `Button`/`Badge`.** Los ~50 `<Link>` internos de tablas y detalles que antes escribían su className a mano quedaron en tres variantes distintas —subrayado solo al pasar el mouse, subrayado fijo sin color, o ninguna de las dos cosas— y se unificaron a esta. | Auditoría de Fase 7d (Tarea 3): en una tabla con texto plano, una celda que solo se subraya al pasar el mouse no se distingue de texto normal hasta que el usuario ya está encima, y una que se subraya siempre pero sin color tampoco separa "esto es un link" de "esto es una etiqueta". Definirlo una vez es lo que evita que la próxima pantalla invente una cuarta variante. |
| D-115 | 2026-09-06 | **El contenedor de scroll interno de tabla (`<TableScrollArea>`, max-height + `overflow-y-auto`) se revirtió a pedido del dueño.** Las tablas de listado vuelven a `<div className="rounded-lg border">` sin tope de alto: la página vuelve a scrollear entera, como antes de D-113. El encabezado `sticky top-0 z-10 bg-background` de `<TableHeader>` se mantiene — sigue funcionando sin el contenedor (se pega al techo del viewport en vez de al techo de un contenedor propio, porque no hay ningún ancestro entre la tabla y el documento con su propio scroll). Paginación (D-113) y columnas responsive (`hidden md:table-cell`/`lg:table-cell`) quedan intactas: no dependían del contenedor. El componente `TableScrollArea` se borró por no tener más uso. | El dueño encontró que el scroll interno desaprovechaba espacio de pantalla. Mantener el encabezado fijo sin el contenedor no tiene contraindicación real —sigue siendo la misma propiedad CSS, solo cambia contra qué ancestro se pega— así que se conserva esa parte de D-113 sin necesidad de decidir entre las dos cosas. |
| D-116 | 2026-09-06 | **Venta de bobina completa (RF-73, Fase 7e, parte A).** Decisión del dueño. Una bobina `kind=COIL` OPEN o CLOSED (nunca `IN_THIRD_PARTY`/`CANCELLED`) se vende **siempre por su saldo vivo completo**, nunca una fracción, en Drywall o Metallic Roofing por igual. La línea nueva (`salesItemInputSchema.saleCoilId`) no manda producto ni cantidad: el API resuelve el producto (el SKU `trading` de D-037, uno por `typeKey`) y la cantidad (el disponible real al momento de resolver la línea, `resolveSaleCoils` en `sales-lines.ts`) — nunca lo que el formulario tipee. Reusa el ledger de reservas tal cual (D-054/D-066: confirmar reserva = custodia, cancelar libera, despachar consume al costo real vía `resolveDispatchTarget`, ya preparado desde Fase 6 para "la reserva es sobre la bobina y ahí sí es correcta"); el único cambio de guardrail es que `createReservations` acepta `CLOSED` además de `OPEN` cuando el producto reservado no tiene receta activa (venta directa), y sigue exigiendo `OPEN` cuando sí la tiene (materia prima para producir, D-086 sin cambios). Selector nuevo `GET /sales/sellable-coils` (bobinas DISPONIBLES: sin custodia de corte ni de OP, con saldo, de Drywall/Metallic Roofing). La descripción de línea por defecto es `Bobina {código} × {kg} kg`, que es lo que ve dispatch y facturación (ambos ya leían `salesOrderItem.description` tal cual). | La reserva por bobina y el despacho que lee `reserveItemType=COIL` sin producir nada ya existían desde Fase 5a/6 para RF-73 — el hueco real era que nada armaba esa línea sin pedirle al vendedor que adivinara el SKU y tipeara a mano un saldo que tenía que coincidir exacto con el kardex. Separar `saleCoilId` de `reserveFromCoilId` (en vez de reusarlo) evita mezclar dos guardrails distintos bajo el mismo campo: uno reserva un **trozo** de una bobina que después se monta en una OP (exige `OPEN`), el otro **vende la bobina entera** y no la vuelve a tocar nadie (admite `CLOSED`, que además es el nuevo default de alta, C). Confundir los dos habría dejado vender una bobina a medio reservar como materia prima, o habría bloqueado la venta de exactamente las bobinas que C recomienda cerrar para protegerlas de un partido o merma accidental mientras esperan comprador. |

| D-117 | 2026-09-06 | **Bobinas (Fase 7e, parte C): CERRADA por defecto al alta, línea de negocio restringida y metro lineal equivalente expuesto.** Decisión del dueño. (a) Toda bobina nueva (compra `COIL` o planilla) nace `CLOSED` salvo que quien la registra elija `OPEN` en el propio formulario (`purchase_items.coil_status`, columna nueva, nullable, solo `COIL`); una hija de partido o un fleje de corte **no cambian** — siguen naciendo `OPEN`, porque D-060 los necesita disponibles para producción de inmediato. (b) El selector de línea de negocio de una compra `COIL` y de la planilla de bobinas ofrece solo `drywall`/`metallic-roofing` (`COIL_BUSINESS_LINES` en `@ayr/shared`); `trading` vende la bobina como producto (D-037) pero nunca la compra, y `roofing` (UPVC)/`services` no tienen bobina. (c) `CoilDto.equivalentMeters` expone `saldo_kg / (ancho_m × espesor_mm × densityFactor del acabado)` — la misma aritmética que `roofing-math.ts` ya usaba para las coberturas a medida, ahora compartida como `kgPerMeter`/`equivalentMeters` en `packages/shared` para que no diverjan. El formato de código de RF-13 (`{proveedor}-{acabado}-{espesor}-{peso}-{correlativo}`) y el `densityFactor` por acabado (`finishes.density_factor`) ya estaban implementados desde fases anteriores — no había nada que cambiar ahí. | (a) Una bobina que se sabe que se va a vender entera (A, D-116) no debería quedar abierta esperando que alguien la parta o le saque merma por accidente mientras espera comprador; `CLOSED` ya bloqueaba producción y partido (RF-19) desde Fase 2b, así que el único cambio real es el default y no la semántica. (b) Sin el filtro, el formulario ofrecía una compra de bobinas en una línea que nunca las usa, y solo se descubría al fallar la recepción. (c) El metro lineal equivalente es lo que Fase 7f va a necesitar para mostrarlo en la tabla de bobinas; exponerlo ahora en el DTO evita que esa fase tenga que tocar el servicio de nuevo. |

| D-118 | 2026-09-06 | **Catálogo estructurado (Fase 7e, parte B).** Decisión del dueño. `products` gana cuatro columnas nullable: `thicknessMm`/`widthMm` (obligatorios en Metallic Roofing, validado en `CatalogService` según la línea — no en la base) y `widthMm`/`lengthMm`/`pieceWeightKg` (obligatorios en Drywall). El discriminador PLANCHA-vs-A-MEDIDA sigue siendo `product.unit` (`NIU`/`MTR`, D-083) y el largo fijo de una plancha sigue viviendo en `product_boms.piece_length_mm` (D-083/D-086) — no se duplica en `products`, porque en coberturas ese campo ya distingue el subtipo y moverlo habría creado dos fuentes de verdad. `ProductDto.theoreticalKgPerUnit` (kg/pieza o kg/ml, según `unit`) sale de `thicknessMm`×`widthMm`×`densityFactor` del acabado de la receta (`kgPerMeter`/`theoreticalKgPerPiece`, ya compartidos en `@ayr/shared` desde D-117) — `null` sin receta o sin los dos campos. Drywall nunca calcula este valor: declara `pieceWeightKg` directo, porque la sección de un perfil no es un prisma simple y una fórmula por geometría mentiría. El filtro de bobina de la OP de coberturas (D-086) ya leía campos estructurados (`product_boms.input_thickness_mm`, `products.color_id`) sin tocar el SKU como texto — verificado, sin cambios. | El ancho de Metallic Roofing es dato **nuevo**: antes de esta fase no vivía en ningún lado (D-086 filtra bobina por espesor y color, nunca por ancho, porque la producción real monta el rollo que haya). Sirve para cotizar y mostrar kg estimados **antes** de que exista una bobina montada. El ancho y el largo de Drywall, en cambio, coexisten con campos de `product_boms` que ya existían (`input_width_mm` del fleje, `piece_length_mm`/`kg_per_piece` de la pieza) pero describen algo distinto: la pieza terminada tiene su propia geometría comercial, separada de la fórmula de fabricación — un perfil de 35 mm final puede salir de un fleje de 100 mm de ancho, y el catálogo necesita poder mostrar la primera cifra sin depender de que la receta ya esté configurada. |

| D-119 | 2026-09-06 | **Cotización y pedido multi-línea de negocio, y duplicar (Fase 7e, parte D).** Decisión del dueño. `quotations`/`sales_orders` pierden su columna `business_line_id`: cada línea trae la suya, la de su propio producto (`resolveSalesLines` ya no recibe ni valida contra una línea compartida del documento; solo exige que la bobina de materia prima de una línea sea de la **misma** línea que su producto, que nunca fue una regla del documento sino física). `QuotationDto`/`SalesOrderDto.businessLines` la exponen como arreglo derivado de los ítems (list/detalle sin ítems la omiten, mismo motivo que D-113: cargar ítems solo para esto sería más caro que lo que muestra). Un pedido **directo** (sin cotización) sigue exigiendo que ninguna línea venga de una línea con `quotationRequired` (RF-31), ahora comprobado línea por línea; el mostrador (D-104) sigue exigiendo una sola línea de negocio en el carrito, movida a `PosService` porque la tabla ya no la sostiene. **Duplicar** (`POST /sales/quotations/:id/duplicate`) crea un BORRADOR nuevo (correlativo propio) desde una cotización en cualquier estado: mismo cliente, mismas líneas, revalidadas contra el catálogo y el kardex de hoy (no una copia literal de fila) — un producto desactivado o una bobina ya vendida se rechazan igual que en un alta nueva. **Editar solo aplica a BORRADOR**: ya era así desde D-065/RF-66, verificado sin cambios. | El hueco real de A (D-116) fue el disparador: `createReservations` y el despacho asumían `businessLineId` del **pedido** para bloquear el saldo de cada ítem, y eso es falso en cuanto el producto (`trading`) y el ítem físico que reserva (una bobina de Drywall o Metallic Roofing) son de líneas distintas — `lockBalance` habría rechazado toda venta de bobina completa con "el ítem ya tiene saldo en otra línea de negocio". Corregirlo exigía que cada operación de kardex resolviera la línea **del ítem**, no la "del documento" — y una vez ahí, la restricción de una sola línea por documento dejó de tener ninguna base real que defender, así que quitarla (el pedido del dueño) fue completar el mismo arreglo, no uno aparte. Duplicar revalida en vez de copiar por la misma razón de fondo que ya aplicó D-097/D-088: una promesa vieja (un producto, una bobina) puede no seguir siendo cierta hoy, y una copia literal la daría por buena en silencio. |

| D-120 | 2026-09-06 | **Corte tercerizado solo Drywall, y el plan de envío se arma por SKU de perfil (Fase 7e, parte E).** Decisión del dueño. `CuttingService.send` rechaza una bobina que no sea de la línea Drywall (Metallic Roofing rola la bobina entera sin cortarla en flejes, D-086; trading/UPVC no fabrican). El formulario web (`/corte/nueva`) deja de pedir el ancho de cada fila a mano: se elige el SKU del perfil de drywall que va a consumir ese fleje (con receta activa, D-059) y el ancho sale de `product_boms.input_width_mm` — ya estructurado desde Fase 4, no hizo falta agregar nada. La fila muestra además los kg teóricos del plan (proporción por ancho sobre el disponible de la bobina, la misma cuenta que `validateWidthBudget`), informativo: la recepción (RF-41) sigue ajustando contra el peso real, flujo intacto. | El ancho de un fleje nunca fue un dato libre: siempre fue "lo que la receta de tal perfil necesita" (D-059 ya lo exige al dar de alta la receta), así que dejar que el formulario de envío lo pidiera a mano abría la puerta a un ancho que ninguna receta consume — un fleje que nadie va a poder montar en producción (D-059 empareja fleje y receta por ancho exacto, no por tolerancia). Restringir el corte a Drywall es la misma idea que ya aplicó D-086 del otro lado: coberturas no tiene fleje, tiene bobina montada entera. |
| D-121 | 2026-09-06 | **Dos ajustes de alcance de Fase 7e pedidos en la revisión del dueño, solo UI/filtro.** (a) `/bobinas` gana pestañas (`Tabs` de shadcn): **Disponibles** (default: `statusNe=IN_THIRD_PARTY&availability=available`), **En corte** (`status=IN_THIRD_PARTY`), **Agotadas** (`statusNe=IN_THIRD_PARTY&availability=depleted`), **Todas** (sin esos filtros; el `<select>` de Estado que ya existía queda solo en esta pestaña). `coilQuerySchema` gana `statusNe` (`{ equals, not }` de Prisma) y `availability` (subconsulta a `inventory_balances`, `qty > 0` / `qty ≤ 0`, filtrada por `id: {in}`); nada se borra, una bobina anulada simplemente cae en "Agotadas" porque su reversa (RF-21) ya la deja en cero. (b) La terminal de planta (`OrderTerminal` en `planta-view.tsx`) y el detalle de OP (`produccion-detalle-view.tsx`) muestran un stat "Piezas teóricas" = `assignedKg / kgPerPiece` sin redondear, con un hint contra `piecesReported` — dato derivado, ya estaba todo en el DTO. Solo Drywall (coberturas no tiene `kgPerPiece`). | (a) El default de antes (sin filtro) mezclaba lo operable a diario con lo agotado y lo que está fuera de planta, y planta necesita ver primero lo que puede tocar hoy. (b) El operario veía cuánto **alcanza** el fleje pendiente ("Alcanza para") pero no cuánto **debería** haber salido de todo lo montado hasta ahora — el primer número mira hacia adelante, el segundo sirve para notar un desvío acumulado antes de cerrar la orden. |
| D-123 | 2026-09-06 | **Un cambio de regla transversal (default de estado, campo obligatorio, restricción de línea) se corre contra la suite E2E completa antes del push, no contra "casos nuevos + spots de fases viejas".** El primer push de Fase 7e (A-E, D-116..D-121) llegó verde en local con esa muestra y CI encontró 25 fallas reales en specs de fases anteriores (fase1, 2b, 3, 3b, 5a, 5a-bordes, 6-bordes, 7b-bordes, 7d): D-118 (campos de catálogo obligatorios) rompió una alta de producto que no los llenaba, D-117 (bobina nace `CLOSED`) rompió todo test que compraba una bobina y la operaba enseguida asumiendo `OPEN`, D-120 (corte solo Drywall) rompió casos de borde que enviaban una bobina de Metallic Roofing a corte, y la restricción de línea de compra de bobinas (D-117) rompió un test que compraba en `trading`. Ninguna de las 9 specs rotas era nueva de esta fase; todas asumían el comportamiento anterior al cambio de regla. | La corrida completa (`pnpm e2e`, 24+ archivos) se había cortado a mitad de camino en la misma sesión por tardar más de 2 horas, y se sustituyó por los specs nuevos de la fase más una muestra de fases pasadas — exactamente la combinación que no iba a tocar ninguno de estos 9 archivos. La lección no es "correr siempre la suite completa a cualquier costo" (2+ horas localmente es real), sino que **un cambio de regla que no es aditivo —quita un estado por defecto, agrega una validación obligatoria, restringe un enum que antes aceptaba más— es population-wide sobre datos existentes**, y la única muestra que lo detecta con certeza es la corrida completa; los specs de la fase actual no la ejercitan (no existían cuando la regla vieja regía) y una muestra a mano solo detecta lo que a alguien se le ocurrió mirar. Correrla en CI (que sí la corre completa, D-006) es la red de segunda mano — más cara en tiempo de vuelta, pero real. |
| D-122 | 2026-09-06 (planeado, no implementado) | **Sacar el `ProductBom` de coberturas por completo — pendiente para el tramo 7e-ii.** Decisión del dueño, revirtiendo la mitad de D-118: hoy el vínculo bobina↔producto de Metallic Roofing (`coilOptions`/`mountCoil` en `roofing-production.service.ts`) sigue comparando contra `bom.inputThicknessMm`, y el `theoreticalKgPerUnit` del catálogo exige `p.bom?.isActive` y lee `p.bom.finish.densityFactor` — dos fuentes del mismo dato (la lección de D-088/D-097: divergen). Diseño acordado: `products` gana `finishId` (nuevo, nullable, FK a `finishes`) — el maestro de acabados de esta fase pasa a vivir también ahí para coberturas; `thicknessMm`/`widthMm`/`colorId` ya existen (D-118) y `lengthMm` (ya existe, hoy documentado como "solo Drywall") pasa a usarse también para el largo fijo de una plancha de coberturas. `coilOptions`/`mountCoil` filtran/validan contra `product.thicknessMm` (± tolerancia) y `product.colorId`, nunca contra un bom. El kg teórico del catálogo sale de `product.thicknessMm × product.widthMm × product.finish.densityFactor`, sin exigir ningún `ProductBom`. `ProductBom` queda **exclusivo de Drywall** (la receta real, `kgPerPiece`): implica `production_orders.bom_id` nullable (hoy `NOT NULL`) y sacar `order.bom` de todo lector de una OP `ROOFING` — `create()`/`mountCoil()`/`report()` en `roofing-production.service.ts` (los dos primeros por `bom.inputThicknessMm`; `report()` ya lee la geometría de la bobina montada, solo `bom.pieceLengthMm` migra a `product.lengthMm`), el DTO (`ProductionOrderDto.bom` pasa a nullable, agregar los campos de geometría de coberturas al propio DTO de la orden) y el frontend (`produccion-detalle-view.tsx` línea "bobina de X mm" lee hoy `o.bom.inputThicknessMm`). Migración: agregar `products.finish_id`, backfillear desde el `ProductBom` `ROOFING` activo de cada producto (mismo `finishId`) antes de cortar la dependencia; las filas de `ProductBom` `ROOFING` existentes quedan huérfanas de uso pero **no se borran** (siguen de FK en órdenes de producción históricas — borrarlas rompería esas filas). `boms.service.ts` deja de ofrecer/crear BOM `ROOFING` hacia adelante. Pendiente además: reescribir el e2e `fase7e.spec.ts` ("plancha de catálogo con receta…", que hoy prueba justo el camino con BOM) y el flujo de alta de un producto de coberturas nuevo en el catálogo web (pedir `finishId` ahí). | Encontrado en la revisión local del dueño de Fase 7e: le señalé que hoy coberturas SÍ depende de un `ProductBom` en dos puntos (coincidencia de espesor de la bobina candidata, y el teórico del catálogo) pese a que D-118 ya dejó `thicknessMm`/`widthMm`/`colorId` en `products` — persistir el mismo dato en dos tablas es la clase de defecto que D-088/D-097 ya nombraron para la reserva. Se documenta el plan completo acá (en vez de solo la intención) porque tocar `production_orders.bom_id` (columna `NOT NULL` hoy, con lectores en al menos cuatro archivos de backend y dos de frontend) es un cambio de forma, no de filtro, y de este tramo no alcanzaba el contexto para cerrarlo con la misma verificación (lint/typecheck/test/build + E2E + revisión) que el resto de Fase 7e sin arriesgar dejarlo a medio migrar. |
| D-124 | 2026-09-06 | **Fecha de operación: el día de negocio de un hecho, separado del instante en que se grabó.** Pedido del dueño para poder cargar el histórico de agosto después de haber entregado el sistema. Toda operación fechada del dominio gana `operationDate` (`DATE`, día calendario de **Lima**): movimiento de kardex, alta de bobina, envío y recepción de corte, arranque y cierre de una OP, reporte de piezas. Las que ya tenían su fecha de negocio no ganan columna, se les exige la misma disciplina: `dispatches.dispatch_date`, `customer_payments.date`, `supplier_payments.date`. Los timestamps de auditoría (`at`, `created_at`, `sent_at`, `closed_at`) **no se tocan**: siguen diciendo cuándo se tipeó. Por defecto `operationDate` es hoy, así que **el flujo de todos los días no cambia en nada**; retrofecharla la valida un único punto (`OperationDateService.resolve`): solo ADMINISTRADOR (403 a cualquier otro rol), nunca futura, nunca anterior a `HISTORICAL_LOAD_START` (env, default `2026-08-01`). En el web es un campo colapsado detrás de un enlace y solo se le muestra a un administrador. **Todo lector de fechas se auditó**: el kardex ordena y corta por `operationDate` (con el `id` bigserial desempatando dentro del día), el saldo de apertura de un kardex filtrado se calcula por `operationDate`, y los listados de bobinas, comprobantes, despachos y órdenes de corte ordenan por su fecha de negocio y no por `created_at`. Una **reversa tiene fecha propia** —por defecto hoy— y jamás hereda la del hecho que anula; la única excepción es el recosteo de una bobina (D-045), que es reversa + reingreso del **mismo** hecho y por eso los dos movimientos llevan la fecha del ingreso original. Guardrail en vez de recálculo: con producción vacía no se construye un recálculo retroactivo del promedio ponderado; si un movimiento retrofechado cae por detrás de movimientos que el ítem ya tiene, `InventoryService.record` corta con el detalle (ítem, fecha pedida, fecha del más reciente, cuántos quedan por delante) y exige `confirmBackdate`. La regla escrita es que **la carga histórica va en orden cronológico**. | Sin esta separación el histórico era incargable: todo lo que se registrara hoy quedaba fechado hoy, y ningún reporte de agosto lo veía. Poner el guardrail en `InventoryService.record` —el único escritor del kardex (§3.2)— y no repartido por los diez servicios que registran movimientos es lo que garantiza que ningún camino se lo saltee; repartirlo era repetir el hueco de D-088, que se coló entre dos módulos que creían tener la regla puesta. El costo en el flujo normal es cero: si la fecha es hoy no puede haber nada posterior (retrofechar al futuro está prohibido), así que el guardrail ni consulta. El backfill de la migración convierte los timestamps a **Lima** y no a UTC por el mismo desfase de cinco horas que D-112 ya costó nueve pantallas. Y para escribir la columna nueva en `inventory_movements` hubo que apagar el trigger de append-only durante la migración y volver a encenderlo: es la única forma de agregarle una columna a una tabla inmutable, y es seguro porque escribe un campo que hasta esa migración no existía. |
| D-125 | 2026-09-06 | **Entorno `demo`: una cuarta rama de Neon para ensayar, capacitar y cargar datos de prueba.** Creada desde `production` el 2026-09-06, antes de que producción tuviera datos reales. `pnpm env:demo` escribe `.env.demo` (cubierto por el `.env.*` del `.gitignore`), `pnpm db:demo` le aplica migraciones y seed, y `pnpm dev:demo` levanta api+web contra ella **sin tocar** `apps/api/.env`: el override viaja por variables de entorno al proceso, así que `pnpm dev` sigue apuntando a `dev` y las dos cosas conviven. Documentado en `docs/ENTORNOS.md`. | Hasta hoy, ensayar el flujo completo o capacitar a alguien se hacía contra producción, porque era el único entorno con datos parecidos a los reales. Desde mañana eso deja de ser aceptable. Una rama de Neon cuesta lo mismo que las otras tres y separa por construcción "probar" de "trabajar", que es una frontera que ninguna disciplina sostiene sola. |
| D-126 | 2026-09-06 | **La suite E2E no corre nunca contra producción. `pnpm e2e:prod` queda prohibido como rutina; la verificación post-deploy es `pnpm smoke:prod`, de solo lectura.** El smoke hace `/health` sin sesión, login con el ADMINISTRADOR efímero de D-024 y cinco GET (líneas, catálogo, inventario, bobinas, reporte mensual); lo único que escribe es ese usuario efímero, y lo borra en `finally`. La suite completa vive en local y en CI, contra la rama `ci`. `pnpm prod:purge-e2e` queda solo para emergencias, y cada uso se documenta en `PROGRESO.md`. | Desde el 2026-09-07 producción lleva inventario, comprobantes y cuentas reales. La suite completa crea compras, bobinas, órdenes de producción y despachos, y el kardex es append-only (§3.2): parte de lo que deja **no se puede deshacer** sin una reversa de dominio — las fases 7d y 7e documentan el residuo exacto que quedó las dos veces que se corrió. Contra una base vacía eso era un rastro molesto; contra inventario real es contaminación del stock del cliente, y ninguna purga posterior lo arregla. |
| D-127 | 2026-09-06 | **Subtipo de cobertura explícito (`products.roofing_kind`: PLANCHA / A_MEDIDA), y la rama de confirmación que faltaba.** Encontrado por el dueño en producción: la cotización COT-000240 (línea compuesta a medida, 61 ml en subítems de 5×5 m y 6×6 m) fallaba al confirmarse con "0.000 MTR disponibles… necesita 61.000". Estaba validando stock de **producto terminado** —el camino de una plancha— sobre un producto que no existe hasta que planta lo rola. Dos cambios: (a) el subtipo pasa a ser un campo **declarado** del catálogo, obligatorio en Metallic Roofing, visible y corregible en el diálogo de producto y en la lista (una cobertura nueva nace A_MEDIDA, que es el caso habitual del rubro); el largo fijo es obligatorio solo en PLANCHA y está prohibido en A_MEDIDA, donde el largo lo traen los subítems de cada línea (D-083); subtipo y unidad no pueden discrepar (A_MEDIDA implica `MTR`), y lo sostienen la validación del servicio y un `CHECK` en la base. (b) `resolveSalesLines` ramifica por el subtipo: una línea A_MEDIDA sin bobina elegida a mano calcula los kilos teóricos (`ml × espesor × ancho × densityFactor`, la misma aritmética de D-047 y D-118) y **reserva materia prima** — el API elige la bobina con el **mismo filtro** que el selector de la OP (`roofingCoilWhere`, D-086: abierta, color exacto, espesor de la receta ± tolerancia), la más antigua por `operationDate` que alcance, descontando lo ya reservado y lo montado en otra OP (D-060). Una línea PLANCHA sigue exigiendo stock de producto terminado, sin cambios. La composición por subítems queda restringida a A_MEDIDA. | El subtipo existía de hecho pero no como dato: "a medida" se deducía de `unit = MTR` y "plancha" de tener largo, dos convenciones que ninguna pantalla mostraba y que nadie podía corregir. Con la regla escondida, la rama equivocada no se veía venir y solo apareció como un error de stock sin relación aparente con el problema real. Que el API elija la bobina y no el vendedor es deliberado: quien cotiza sabe cuántos metros y de qué color, pero qué rollo los da es una decisión de planta — pedírsela al vendedor fue justamente lo que dejó cotizaciones sin materia prima asignada. El filtro se extrajo a `roofing-coil-match.ts` en vez de copiarse: dos definiciones de "qué bobina sirve" divergen, y una cotización que promete material que la OP después no puede montar es el mismo defecto que D-088 y D-097 ya costaron. La densidad sale todavía del acabado de la **receta**; moverla a `products.finish_id` es lo que D-122 tiene pendiente. |
| D-128 | 2026-09-06 | **Una credencial jamás viaja por `argv`; siempre por el entorno del proceso hijo.** Regla nueva en `CLAUDE.md` (dentro de la regla dura 5), escrita a partir de un incidente de esta sesión: `scripts/db-demo.mjs` le pasaba la conexión a `prisma db execute` como `--url <cadena>`, el comando falló por otro motivo (el `&` de la query string, que el shell de Windows parte en dos) y el manejador de error del propio guion —que componía el mensaje con `args.join(' ')`— **imprimió la cadena completa, con la contraseña**, en la terminal. Se verificó comparando huellas SHA-256, sin imprimir ninguna, que la contraseña del rol `neondb_owner` es **la misma en las cuatro ramas** del proyecto Neon: lo expuesto no era la credencial de `demo` sino la de `production`. El dueño la rotó y propagó a `.env` locales, Secret Manager y GitHub. Correcciones de código: `db-demo.mjs` pasa toda conexión por el objeto `env` de `spawnSync` (y `prisma db execute` la toma del `DIRECT_URL` del entorno), y su mensaje de error dejó de repetir los argumentos. | Dos cosas fallaron a la vez y ninguna sola habría bastado. La primera: un argumento es visible en el título del proceso **y** en cualquier mensaje de error que lo repita — el entorno no. La segunda, la que de verdad explica el escape: `scripts/lib.mjs#run`, el helper compartido, ya filtraba `secret|password|token` de sus mensajes de error desde la Fase 0; el helper local de tres líneas que `db-demo.mjs` se escribió por comodidad, no. Un guion nuevo que reimplementa en chico algo que el proyecto ya resolvió en grande pierde, en silencio, las protecciones que ese grande había acumulado. La regla se escribe como propiedad del dato (una credencial no se pasa por argv) y no como advertencia sobre un helper, porque la próxima vez el helper va a ser otro. |
| D-129 | 2026-09-06 | **La rama `production` se vacía y se rehace desde el seed para el go-live, en vez de purgarse.** Decisión del dueño, tomada sobre el inventario de `scripts/go-live-inventory.mjs` (solo lectura, nuevo): producción **no tenía un solo dato real de negocio** — 289 de 291 clientes con prefijo `E2E` (los otros dos, el maestro `PÚBLICO EN GENERAL` y el cliente con el que el dueño ensayó), **los 1070 proveedores** de prueba, 3 productos activos no-E2E que también eran ensayos suyos, 62 saldos con stock y 6836 movimientos de kardex, todos de prueba. Se descartó la purga manual: deshacer eso exige revertir movimientos append-only a través de reversas de dominio, y las fases 7d y 7e ya habían demostrado dos veces que **no puede completarse** (piezas ya vendidas y flejes ya mermados bloquean sus propias reversas), así que el día del go-live habría empezado con residuo garantizado. El reset es `DROP SCHEMA public CASCADE` + `migrate deploy` + seed, detrás de una compuerta explícita por variable de entorno. El dueño eligió además **no recrear nada**: producción arranca solo con líneas de negocio, márgenes y su usuario, y él carga acabados, colores y catálogo con la carga histórica de agosto (D-124). La rama **no se borra** —eso sigue prohibido—: se vacía y se vuelve a construir con el mismo historial de migraciones que las demás. | Es la única vez en la vida del proyecto en que esto es correcto, y por eso vale escribir por qué: el kardex append-only (§3.2) está diseñado para que el pasado no se pueda reescribir, y eso es exactamente lo que hace imposible limpiar una base que se usó como campo de pruebas. La ventana para vaciarla se cierra el día que entra el primer dato real; de ahí en adelante la respuesta a "esto quedó sucio" vuelve a ser una reversa de dominio, nunca un reset. Que la decisión se haya podido tomar con datos y no con impresiones es mérito del inventario: `prod-e2e-leftovers` respondía "qué dejó la suite" filtrando por prefijo, y la pregunta del go-live era otra —"qué hay acá que no debería existir mañana"— que incluye los ensayos hechos a mano, que no llevan ningún prefijo y que ninguna purga automática reconoce. |
| D-130 | 2026-09-06 | **Un ciclo de imports en `@ayr/shared` borraba campos de schema en silencio; el reloj del negocio pasa a un módulo hoja.** Encontrado por el agente `qa` con un E2E, no por el compilador. `schemas/operation.ts` importaba `businessToday` de `schemas/sales.ts`; `sales.ts` importa `reasonSchema` de `coil.ts` y `roofingPiecesSchema` de `roofing.ts`; los dos importan `backdatableFields` de `operation.ts` → ciclo. En la salida CommonJS de `tsc`, un módulo a medio inicializar no lanza: devuelve `undefined`, y `{ ...undefined }` **tampoco lanza** — esparce nada. Resultado: `createRoofingOrderSchema`, `reportRoofingPiecesSchema`, `closeRoofingOrderSchema`, `reverseMovementSchema`, `createCoilScrapSchema` y `createCoilSplitSchema` perdían `operationDate` y `confirmBackdate` sin error, sin aviso y con typecheck en verde. Toda la rama de coberturas y **todas** las anulaciones ignoraban la retrofecha, y —peor— como Zod descartaba el campo antes de que llegara a `OperationDateService`, **un VENDEDOR no recibía el 403** en esas rutas y una fecha inválida devolvía 201. Corrección: `BUSINESS_TIME_ZONE` y `businessToday` se mudan a `packages/shared/src/business-date.ts`, un módulo que **no importa nada** del paquete, y `operation.ts` deja de importar cualquier schema. Se agregó al mismo tiempo el guard que faltaba en la validación de calendario: `2026-08-32` y `2026-13-01` dan `Invalid Date` y `toISOString()` lanzaba `RangeError` dentro del `refine` — un 500 donde correspondía un 400. | Las dos mitades de este defecto comparten una forma: **una falla que no falla**. El ciclo no rompe el build, no rompe el typecheck y no lanza en runtime; simplemente los campos no están. La única razón por la que se encontró es que había un E2E que mandaba `operationDate` a una ruta de coberturas y miraba qué había quedado guardado — ninguna prueba unitaria del schema lo habría visto, porque importar el schema aislado no reproduce el orden de inicialización del índice. De ahí la regla que queda: **un módulo del que otros esparcen constantes (`{ ...algo }`) no puede importar nada que pueda volver a él**, y la forma barata de garantizarlo es que sea una hoja. El corolario operativo es el de D-123 otra vez, por otro camino: la verificación que encuentra esta clase de cosas es la que ejercita el contrato entero, no la que mira una pieza. |
| D-131 | 2026-09-07 | **"Se vende por metro lineal" y "se fabrica a medida" son dos preguntas distintas, y el código tiene que responderlas con dos predicados distintos.** Encontrado por CI sobre el push de D-124/D-127. Al hacer explícito el subtipo (D-127) reemplacé `product.unit === MTR` por `roofingKind === A_MEDIDA` **en los dos lugares**, y eran preguntas diferentes: (a) _¿esta línea necesita subítems de largo?_ la decide la **unidad** y vale para cualquier línea de negocio (D-083); (b) _¿se fabrica contra pedido desde materia prima?_ la decide el **subtipo** y es exclusiva de Metallic Roofing. Como el subtipo es `null` fuera de coberturas, responder (a) con él dejó de exigir subítems a todo producto en `MTR` de otra línea — y con eso **el mostrador pasó a poder vender material a medida**, que es justo lo que D-098 prohíbe: el guardrail de la lista filtra por unidad, pero el que de verdad corta es el de `resolveSalesLines`, y ese había dejado de mirar. Corregido con `sellsByLength` (unidad) y `isMadeToMeasure` (subtipo) como predicados separados, cada uno en su chequeo. En el mismo hallazgo: `todayIso()` del web calculaba "hoy" con los componentes **locales del navegador** en vez del día de Lima, y como D-124 pasó a validar las fechas de negocio contra Lima, un cliente en un huso por delante prellenaba mañana y recibía "la fecha de operación no puede ser futura" en una operación normal. Los E2E, además, armaban fechas con `new Date().toISOString().slice(0, 10)` — el corte en UTC que D-112 prohibió en el web con una regla de ESLint que **no cubre `e2e/`**. | Las tres mitades del hallazgo son la misma lección desde ángulos distintos: **cuando un dato pasa de implícito a explícito, hay que revisar cada lugar que lo leía, porque puede haber estado respondiendo más de una pregunta**. `unit === MTR` significaba a la vez "se mide por metro" y "es una cobertura a medida" — dos cosas que coincidían mientras coberturas fuera lo único que se vendiera por metro. Reemplazarlo por el subtipo respondió bien la segunda y rompió la primera, en silencio y en un módulo que ningún test de coberturas toca. Que lo haya encontrado CI y no la verificación local es exactamente D-123: el smoke acotado que corrí (fase5a, fase6, fase7e) no incluía `fase7b-bordes`, porque el mostrador no parecía tener nada que ver con el subtipo de una cobertura. La única muestra que lo veía era la completa. |
| D-132 | 2026-09-07 | **La unicidad del comprobante de compra cuenta solo las compras vivas.** Lo encontró el dueño cargando: una compra registrada con datos errados se anula y hay que volver a registrarla con el mismo número corregido, y la unicidad total lo rechazaba como duplicado. El número de una factura del proveedor no se puede inventar —es el que dice el papel—, así que el workaround fue registrarla con un sufijo `-R`, y un número inventado es el que después no cuadra con el proveedor. El índice único `(supplier_id, doc_type, series, number)` pasa a ser **parcial** (`WHERE status <> 'CANCELLED'`), y se agrega `PATCH /purchases/:id/document` (solo ADMINISTRADOR, auditado) para corregir el número ya cargado sin anular la compra: es dato de cáscara, no entra en ningún cálculo. | Es **RF-72 al revés, y esa simetría es la regla**: allá la versión archivada convive con la vigente porque las dos existieron; acá lo anulado **libera** el recurso porque dejó de ser un comprobante registrado. La auditoría del mismo patrón en el resto del modelo no encontró otro caso: `fiscal_documents.number` retiene su correlativo a propósito (SUNAT no reutiliza números), los códigos de bobina, corte, OP y despacho son correlativos generados y no dictados por un papel, los maestros se desactivan (y reactivar es la ruta, no re-crear) y la caja de mostrador ya libera su `open_user_id` al cerrarse. Lo que distingue a compras es que la referencia única **la escribe un tercero**: cuando el sistema no controla el identificador, anular tiene que devolverlo. |
| D-133 | 2026-09-07 | **La fecha de emisión de un comprobante: VENDEDOR solo hoy, retrofechar es de ADMINISTRADOR.** Hallazgo del auditor de la sesión anterior, que quedó anotado y sin corregir. `assertIssueDateWindow` (D-072) admite hasta 7 días de atraso **a cualquier rol**, que es la ventana que SUNAT acepta; en los primeros días de un mes eso alcanza para cruzar al mes anterior, y mover un hecho de mes es privilegio de ADMINISTRADOR desde D-124. `OperationDateService.assertIssueDate` cierra el hueco en `createInTx` y en `createCreditNote`. | Es deliberadamente un **estrechamiento** y no una regla nueva de emisión: la ventana de SUNAT no se toca —sigue siendo la del schema, y es más estrecha que el piso histórico de D-124—, solo se le pone el mismo control de rol que ya tiene cualquier otro hecho fechado. La lección es de forma: un control que depende del **rol** no puede vivir en un schema de Zod, que no lo conoce; separarlo (la ventana en el schema, quién puede usarla en el servicio) es lo que dejó el hueco abierto un mes sin que ninguna prueba lo viera. |
| D-134 | 2026-09-07 | **La reserva de materia prima es genérica: kilos contra el agregado compatible (línea + color + espesor ± tolerancia), no contra una bobina concreta.** Lo encontró el dueño en producción: la cotización de coberturas le pedía "Reserva desde bobina", y elegir el rollo físico semanas antes de rolarlo es una decisión que no le toca al vendedor — la toma planta al montar la OP (D-086). El agregado es una fila de `raw_material_specs` y la reserva la nombra con `item_type = 'RAW_MATERIAL'`; el campo `reserveFromCoilId` desaparece del schema, del API y del formulario. La invariante `disponible ≥ reservado` de D-066 sigue igual, pero el disponible pasa a ser una **suma** sobre las bobinas que cumplen la spec, y eso obliga a un guardrail nuevo (`assertRawMaterialInvariant`) en cada punto que le quita kilos al agregado: salida de kardex sobre una bobina, envío a corte, montaje en una OP ajena, cierre de una bobina, cambio de su color y venta de una bobina entera. **Seguimiento (continuación 7-final):** el mensaje de rechazo al confirmar un pedido sumaba "físicos" y "comprometidos" sin contar los kilos que una OP tiene montados (D-060 los saca del físico libre sin dejarlos en ningún otro lado); ahora `RawMaterialAvailability` trae `mountedKg`/`mountedOrderCodes` y el mensaje nombra la orden. | Lo importante no es la reserva sino **cuántos lectores tuvo que recorrer**, que es D-088 otra vez: la cola de producción filtraba por `itemType = COIL` y dejó de ver los pedidos; el traslado al producto terminado y el despacho leen las mismas coordenadas; la etiqueta de la línea las traducía a un código de bobina. Ninguno de esos lo habría encontrado el compilador —el enum nuevo es aditivo y no rompe ningún `switch`—, así que el censo se hizo a mano y está anotado en el handoff. El cambio además **borró** una regla que existía solo por el modelo viejo: había que preguntar si el rollo que se roló era el reservado antes de descontar la promesa, porque nada obligaba a montar el reservado. Con el agregado la pregunta no tiene sentido: toda bobina que `mountCoil` admite cumple la spec, así que cualquier kilo que la orden role es un kilo del agregado prometido. |
| D-135 | 2026-09-07 | **El filtro de material compatible es espesor ± tolerancia + color, y el acabado solo aporta la densidad.** Ya era así en el selector de la OP desde D-127 (`roofingCoilWhere`); lo que seguía filtrando por acabado era el selector de bobina de la cotización, que D-134 eliminó. Queda registrado como decisión y no como consecuencia para que no vuelva por otra puerta: el acabado entra en el kilo teórico (su `densityFactor`) y **no** en qué rollo sirve. | Un producto y un rollo pueden compartir acabado y no servir el uno para el otro (otro espesor), y pueden tener acabados distintos y servir perfectamente. Lo que decide si un rollo se puede rolar en un producto es la **geometría y el color**; el acabado decide cuánto pesa lo que salga. Confundirlos dejaba fuera rollos válidos sin decir por qué. |
| D-136 | 2026-09-07 | **Panel de stock en vivo en el formulario de cotización, de solo lectura.** Reemplaza al selector de bobina que D-134 eliminó y cambia de pregunta: aquel pedía _cuál rollo_, este responde _cuánto hay_. Dos mitades: el agregado de materia prima agrupado por espesor + color (kg disponibles y metros lineales teóricos, sumados rollo por rollo con su propio ancho) y el disponible por SKU de los productos que el vendedor tiene puestos. En la línea a medida, además, los kilos teóricos que va a comprometer y el disponible del agregado contra el que se van a comparar. Ruta propia de `sales` y sin ningún campo de costo, por lo mismo que `reservable-coils`: VENDEDOR no llega a `/coils` (§3.4). | Sacarle al vendedor una decisión que no le toca deja un hueco de **información**, no de control: sigue necesitando saber si alcanza. La columna "comprometido" del panel puede pasarse de lo realmente prometido cuando dos grupos de espesor vecino comparten una promesa que la tolerancia alcanza a los dos; es a propósito, y está escrito en el DTO: un panel que guía una promesa tiene que errar mostrando **menos** disponible, nunca más. |
| D-137 | 2026-09-07 | **Importador de bobinas desde el Excel real del negocio, con proveedor auto-creado y dos modos de carga.** No reemplaza al de RF-12: son dos contratos distintos —una plantilla que el sistema define y un export que el negocio ya tiene— y hacer que uno se disfrace del otro obligaba a retipear el archivo, que es el trabajo que la importación viene a ahorrar. El proveedor que falta se crea por RUC consultando el padrón (D-067) y, si el padrón no responde, se crea igual con el nombre del archivo y queda con `needs_review = true`; el acabado que no mapea **no se auto-crea**: la fila queda inválida y el preview la resuelve con un desplegable del maestro. Dos modos por lote (`REPLAY`: entra el peso de compra y el stock actual queda como objetivo comprobable; `ADJUST`: entra el peso de compra y sale la diferencia como consumo pre-sistema retrofechado). El número de factura, la moneda original y el tipo de cambio van a las observaciones de la bobina: la carga histórica no reconstruye compras. | Tres reglas que valen más allá de este importador. **Una importación nunca se traba por un tercero**: un padrón caído no puede impedir cargar la historia del negocio, así que la consulta es una comodidad y el fallback es el camino, no la excepción. **Lo que se auto-crea es lo que no hace daño si sale mal**: un cliente con la dirección incompleta se corrige; un acabado con un `densityFactor` inventado le mete un error a cada kilo teórico que se calcule con él, para siempre y sin aviso. Y **dos pesos sin historia en el medio no tienen una sola respuesta correcta**: el modo es del dueño porque depende de para qué quiere el dato, y el reporte de saldo vs objetivo existe para que la respuesta elegida se pueda comprobar en vez de creer. |
| D-138 | 2026-09-07 | **Importador de ventas desde el export real del negocio, agrupado por SERIE-NÚMERO.** Reusa `FiscalImportService` entero —el documento nace `IMPORTED` y `ACCEPTED`, con su cuenta por cobrar, sin tocar el PSE, y reimportarlo archiva la versión anterior (D-109)— y cambia solo la lectura del archivo: `SERIE - NÚMERO` en un campo, el cliente como texto libre del que se extrae el RUC o el DNI, y el precio unitario derivado de `VALOR DE VENTA / CANTIDAD`. Una fila con `DOCUMENTO AJUSTADO` **no se importa** y dice por qué. El cliente que falta se auto-crea con el patrón de D-137; el SKU que falta **no**. Las columnas ignoradas están listadas en el código con el motivo de cada una. | La regla que ordena las tres decisiones es la misma: **adivinar es peor que rechazar cuando lo adivinado no se puede desandar**. Una nota de crédito necesita a qué comprobante afecta y con qué motivo del catálogo 09, y este export no trae ninguno de los dos: una NC mal apuntada descuadra la cobranza del comprobante equivocado y no hay reversa que lo arregle. Un cliente mal completado se corrige en un minuto. Un SKU inventado entra al catálogo con unidad y línea de negocio adivinadas y desde ahí ensucia stock, precios y kilos teóricos. La condición de pago entra como **crédito** por la misma razón: un cobro que ya ocurrió se registra después y deja rastro; una venta que nace cobrada sin haberlo estado no se descubre nunca. |
| D-139 | 2026-09-07 | **El peso por pieza vive en el SKU: `product_boms.kg_per_piece` desaparece.** Pedido del dueño junto con D-122 ("dedup drywall: el peso por pieza vive en el SKU, la receta queda solo como vínculo fleje→perfil"). Hasta hoy convivían `products.piece_weight_kg` (peso de la pieza terminada, D-118) y `product_boms.kg_per_piece` (kilos de fleje que la pieza consume), documentados como cantidades distintas. **No lo son**: rolar un fleje en un perfil no le saca material, y el despunte se reporta aparte al cerrar la corrida (D-057). La migración copia el valor de la receta al SKU donde el SKU no lo tenía y borra la columna; `product_boms.piece_length_mm` se va por el mismo camino (el largo ya vive en `products.length_mm`). Lo que queda en la receta es exactamente el vínculo fleje → perfil: qué acabado, qué espesor y qué ancho de fleje consume el producto. **Ratificada por el dueño (2026-09-07, continuación 7-final):** en su operación el peso de la pieza terminada es el único dato; la pérdida de rendimiento del rolado la captura la merma por diferencia (D-057) al reportar, no un segundo número en la receta. | El síntoma de que eran el mismo número era el propio maestro: `kg_per_piece` se **calculaba** desde la geometría de la receta y el maestro podía sobreescribirlo, así que había un tercer valor (el sugerido) contra el que compararlo, con un enlace de "usar el teórico" para volver. Tres números para una cantidad es la señal de que ninguno es la fuente. La regla que queda: **si dos columnas describen la misma cantidad física, la que sobrevive es la del objeto al que la cantidad pertenece** — el peso de una pieza es del producto, no de la instrucción de cómo fabricarlo. |
| D-140 | 2026-09-07 | **Producir una plancha de catálogo nunca se liga a un pedido: se produce a stock, y el pedido espera ese saldo.** Resuelta por el dueño (continuación 7-final) entre las dos opciones que quedaron anotadas: la elegida es "dejar crear una OP de coberturas sin reserva, con una cantidad objetivo" — el mismo patrón que drywall ya usaba para una corrida sin pedido detrás. `POST /production/roofing` acepta ahora `reservationId` (contra pedido, cobertura a medida) **o** `productId` + `targetPieces` (a stock, solo para una plancha de catálogo con largo fijo — `RoofingProductionService.createToStock`). Una cobertura a medida no tiene camino a stock: sin pedido no hay largo que fabricar. `/planta` suma la tarjeta "Nueva orden de coberturas a stock" junto al selector de pedidos pendientes. | La otra opción —que una plancha reserve materia prima como una cobertura a medida— contradecía a D-127 (decisión del dueño de un día antes: el pedido de catálogo reserva producto terminado, no materia prima). Con la opción elegida, producción y venta quedan desacopladas para el catálogo: el pedido nunca sabe ni le importa si el stock que reserva ya existía o si una corrida a stock lo acaba de fabricar — es exactamente el mismo patrón que un producto de trading comprado y no fabricado. |
| D-141 | 2026-09-07 | **Cada comprobante importado crea un pedido enlazado 1:1, y un toggle por documento decide si ese pedido es una cáscara o uno vivo.** Decisión del dueño (sesión 7-final-B). En la previsualización de la importación de ventas (D-138), cada documento se marca **ENTREGADO** (por defecto) o **PENDIENTE**. Entregado ⇒ pedido `FULFILLED` con `origin = IMPORTED`, líneas espejo del comprobante y fecha de emisión del papel, con **cero efectos de inventario**: ni reserva, ni despacho, ni kardex, ni cola. El bypass es estructural —un método propio (`createImportedShellInTx`) que no tiene código capaz de escribir una reserva— y además se comprueba: al terminar se cuentan reservas, órdenes de producción, despachos y movimientos del pedido, y si hay alguno la importación entera se cae. Pendiente ⇒ pedido `CONFIRMED` por el flujo normal (`createDirectInTx`, que crea las reservas y comprueba la invariante), con reserva **genérica** de materia prima en una línea a medida (D-134) o de producto terminado en una de catálogo (D-054/D-127), **y una OP en cola (`DRAFT`) por cada línea a medida, sin bobina montada** — montar es de planta (D-086). Una línea de catálogo sin stock **no** genera una OP a stock automática (D-140): la fila se marca y solo entra como entregada. El enlace bidireccional no agrega columna: `fiscal_documents.sales_order_id` ya existía. Reimportar (D-109) archiva el documento y **anula el pedido cáscara**; si ese pedido está vivo con reservas, producción o despachos, el reimport del documento se **bloquea con el motivo** en vez de dejar promesas sin dueño. Parcial (media línea entregada) queda fuera de v1. | El histórico entraba como comprobantes sin pedido, así que la mitad de la operación real —lo facturado y **no entregado**— no existía en el ERP: no aparecía en la cola de planta, no reservaba material y competía por bobinas que el sistema creía libres. El toggle es del **documento** y no de la línea porque el archivo no trae la información (dice qué se vendió, no qué falta entregar) y porque media venta entregada no es un estado que el modelo pueda representar. Que el default sea "entregado" es la elección conservadora: equivocarse hacia ese lado no compromete material de nadie. Y que el pedido cáscara tenga un estado **existente** en vez de uno nuevo evita el costo que D-105 ya midió con `origin`: un estado más obliga a cada lista de estados del módulo —despacho, cobranza, cola— a acordarse de él. |
| D-142 | 2026-09-07 | **CLI de importación de ventas (dry-run/`--execute`) sobre el mismo servicio de D-138/D-141, con OP a stock consolidada por déficit de catálogo y reversa por lote.** `pnpm import:ventas` reusa `ImportsService`/`SalesHistoryImportAdapter` a través de un contexto de Nest standalone (nunca SQL directo para una entidad de negocio); dry-run por defecto, y escribe un esqueleto de decisiones (SKU faltante → crear/mapear, unidad sin mapear, documentos pendientes/a excluir) con lo que ya detectó del archivo. `--execute` **se niega** si el JSON de decisiones no cubre todo lo que el dry-run marcó, aplica las correcciones fila por fila con `updateRow`/`updateGroup` (nunca tocando `data` a mano) y confirma. Enmienda operativa a D-140: cuando una línea de catálogo pendiente no alcanza stock, el documento sigue entrando **solo como ENTREGADO** (la reserva nunca se crea por encima del disponible — la invariante no se toca), pero el CLI además suma el déficit de todas las líneas pendientes de ese producto en el lote entero y crea **una** `createToStock` consolidada, sin ligarla a ninguna reserva: es una reposición adelantada para la próxima carga, no un desbloqueo de esta. Cada entidad que crea un `--execute` (documento, pedido, reserva, OP, cliente y SKU auto-creados) queda estampada con `import_batch_id` (columna nueva, nullable, en las seis tablas — migración aditiva), y `purge-imported-sales.ts --batch=<id>` la usa para revertir **un solo lote**, entero o nada, conservando (y reportando) cualquier cliente o SKU que algo fuera del lote haya vuelto a usar. | El botón de `/comprobantes` sirve para un archivo a la vez con supervisión humana fila por fila; una carga de 141 filas / 71 documentos necesita poder auditarse y revertirse como unidad, y necesita que el catálogo real (con SKUs y unidades en texto libre, no en el código UN/EDI del sistema) entre sin reescribir el archivo a mano. La alternativa de reservar por encima del disponible "porque ya viene una OP a stock en camino" se descartó explícitamente: una OP `DRAFT` no mueve kardex (D-060), así que esa reserva sería una promesa sin nada detrás hasta que planta la produzca — exactamente la invariante que esta sesión no podía tocar. Separar "qué se puede prometer hoy" de "cuánto hay que producir para la próxima" es el mismo desacople que D-140 ya eligió entre venta y producción de catálogo. |
| D-143 | 2026-09-08 | **`purge-imported-sales.ts --batch` acepta `--exclude-touched` (D-142-ii): un pedido cuya OP llegó a montar una bobina real o reportar planchas reales, aunque la reversa de dominio ya lo haya dejado consistente (reporte revertido, OP anulada), no se borra nunca — la guarda "bobina montada alguna vez" es incondicional. Sin el flag, eso sigue abortando el lote entero. Con él, se **excluye del borrado físico al pedido dueño de esa OP** (vía su reserva) y a su comprobante 1:1, y se purga el resto del lote igual; el pedido excluido no se toca de ninguna forma, solo queda fuera del `DELETE`.** Usado una vez, en producción: el lote `d4282f5c-...` (ensayo del CLI de importación) tenía 3 OP que el dueño alcanzó a montar y reportar antes de decidir revertir todo; revertidas por flujo normal (`reverseReport` + `cancel` de `RoofingProductionService`), pero igual bloqueaban el purge por diseño. Con `--exclude-touched`: 60 de 62 pedidos/comprobantes del lote se borraron físicamente; los pedidos 80 y 131 (dueños de esas OP) quedaron tal cual, anulados en su producción pero existiendo como fila, documentados acá y en `docs/PROGRESO.md`. | Sin el flag, un lote de ensayo que alcanzó a tocar producción real quedaba **sin ninguna reversa posible**: ni el purge (bloqueado a propósito) ni el dueño (no hay un "borrar 59 de 62" manual sin arriesgar una FK a medio pisar). La alternativa de debilitar la guarda 2/3 para que ignorase el historial de montaje se descartó: el punto de esa guarda es justo que un `DELETE` no puede deshacer el hecho de que una bobina real se movió, y aflojarla para este caso la aflojaría para siempre. Excluir el pedido entero (no solo la OP) evita dejar un comprobante o una reserva huérfana de su pedido, y usar la reserva para encontrar al dueño en vez de asumir 1 OP = 1 pedido cubre el caso real que apareció: dos OP de un mismo pedido (seq 80) cayeron en la exclusión juntas. |
| D-144 | 2026-09-08 | **`purge-imported-sales.ts --batch --exclude-touched` acepta además `--include-reverted`: promueve al borrado físico un pedido excluido por D-143 si, y solo si, cumple las cinco condiciones a la vez — pedido `CANCELLED`, su(s) comprobante(s) `ANNULLED`/`VOIDED`/`REJECTED` (nunca vigente), sus reservas `RELEASED`, sus OP tocadas `CANCELLED`, y cada movimiento de kardex de sus reportes con su reversa presente, **releído directo de `inventory_movements`** — no se confía en `production_reports.status`.** Usado una vez, en producción: con los pedidos 80 y 131 ya anulados por flujo normal (`FiscalImportService.annulImported` sobre sus facturas IMPORTED, D-110; `SalesOrdersService.cancel` sobre los pedidos), las cinco condiciones se cumplieron y el purge borró los 2 pedidos/comprobantes, 6 reservas, 3 OP y sus reportes/consumos — dejando `origin = IMPORTED` en exactamente 0 en toda producción. | D-143 excluye a propósito y para siempre a un pedido que tocó producción real — es la guarda correcta cuando la reversa de dominio **todavía no pasó**. Pero una vez que sí pasó (pedido anulado, factura dada de baja, kardex con cada movimiento revertido), seguir excluyéndolo para siempre convertiría una guarda de seguridad en una imposibilidad permanente de terminar una reversa completa. La alternativa de aflojar la guarda de D-143 en sí (borrar aunque la OP esté montada) se descartó otra vez, por el mismo motivo que la primera vez: el hecho histórico de haber movido una bobina real no se borra. Lo que sí cambia es exigir, además, una prueba positiva de que ese movimiento ya volvió a cero — y esa prueba se lee del propio kardex, no del estado cacheado de un reporte, porque es la única fuente que no puede mentir sobre si el saldo neto es cero. |
| D-145 | 2026-09-08 | **Una OP de coberturas nace de una reserva _o_ de una meta a stock, y el `CHECK` de la base lo dice así.** El constraint `production_orders_roofing_contra_pedido` (Fase 6, D-084) exigía `reservation_id` en toda OP `ROOFING`. D-140 cambió esa regla —una plancha de catálogo se produce **a stock**, sin reserva— y nadie tocó la base, así que `RoofingProductionService.createToStock` chocaba contra el constraint con un `23514` que salía al usuario como `500 Internal server error`, sin ningún mensaje. Pasa a llamarse `production_orders_roofing_contra_pedido_o_a_stock` y su forma es `kind <> 'ROOFING' OR reservation_id IS NOT NULL OR target_pieces IS NOT NULL`. En el mismo defecto viajaba un segundo bug que el constraint destapó: `createToStock` **validaba** `targetPieces` pero no lo guardaba (drywall sí lo hace desde D-048), así que la meta —lo único que explica por qué existe una orden sin pedido detrás— llegaba `null` a la base. | Borrar el constraint era la otra opción y se descartó: lo que D-084 quería garantizar —que una OP de coberturas nunca sea huérfana— sigue siendo verdad y sigue valiendo la pena tenerlo en la base y no solo en el schema Zod del endpoint. Lo que cambió con D-140 no es que la orden pueda no tener dueño, es que hay **dos** maneras legítimas de tenerlo: el pedido que viene a cumplir, o la meta que declara. La forma nueva admite las dos y sigue prohibiendo la tercera, que nunca fue válida. Que el defecto llegara desplegado tiene una causa concreta y anotada: los tests unitarios de `production` son de aritmética (Prisma mockeado, no ven un `CHECK`) y el E2E de Fase 6 cubría el **rechazo** de D-140 —"producí una orden a stock desde planta"— y se detenía justo en la frase que nombra la salida, sin recorrerla nunca. `e2e/tests/fase7final-op-a-stock.spec.ts` la recorre entera. |
| D-146 | 2026-09-08 | **El plan de corte de una OP de coberturas es un tope duro, y el kg que planta declara por reporte es dato, no consumo.** Pedido del dueño. Hasta hoy la única cota de un reporte era el **material montado**: una orden de 100 ML con un rollo entero encima podía reportar 300 ML sin que nada se quejara, y esos metros de más nacían reservados a nombre del pedido (D-088) o entraban al almacén como stock que nadie encargó. Ahora `report` compara el acumulado de sus reportes vigentes contra `Σ cantidad × largo` del plan y rechaza el exceso **sin tolerancia** — cambiar lo que hay que producir es `updatePlan`, no reportar de más. En el mismo cambio, `consumedKg` opcional por reporte: se guarda en `production_reports.consumed_kg` (columna nueva, nullable) y se topa contra el kilo teórico del **plan completo** con la geometría del rollo montado, pero **no toca el kardex** — la salida de la bobina sigue siendo el kilo teórico de los largos (D-047) y el consumo real se reconcilia al cerrar (D-089). Los datos históricos que ya se pasaron del plan no se tocan ni se bloquean para lectura: la regla mira hacia adelante, y lo único que hace un acumulado excedido es dejar el restante en cero y rechazar el reporte siguiente. La terminal de `/planta` muestra ML del plan, ML reportado, ML restante y kg teórico antes de que nadie tipee. | Elegir el tope **por metros y no por planchas** es lo que lo hace aplicable: los largos reales difieren del plan (D-084 lo dice explícitamente, y por eso el plan es editable), así que contar planchas habría rechazado corridas legítimas. El metro lineal es la cantidad que el pedido compró y la que entra al kardex del producto a medida (D-083), o sea la única que significa lo mismo en las dos puntas. Y que el kg declarado **no** mueva kardex es la misma disciplina de D-060: un número que planta observa no es un hecho de inventario hasta que alguien lo emite. Meterlo al kardex por reporte habría partido el costeo en dos fuentes, y la diferencia contra lo teórico —que es la merma de despunte— ya tiene su lugar en el cierre. |
| D-147 | 2026-09-08 | **Reportar producción en tanda: una fila por orden, un número de metros por fila, todo o nada.** Pedido del dueño: el encargado no vuelve de la planta con una orden, vuelve con **una hoja** que trae el turno entero, e ir orden por orden por `/planta` obliga a buscar y abrir cada una. `/planta/tanda` lista las órdenes de coberturas abiertas (con filtro por pedido) y captura por fila los ML nuevos y los kg consumidos opcionales. **Los largos no se tipean**: se derivan del plan de la propia orden con una búsqueda exacta (`piecesFromPlanMeters`, en `@ayr/shared`, la misma que corre el API al guardar), que falla en vez de redondear cuando los metros no salen de un número entero de planchas. `POST /production/roofing/batch` escribe las N filas en **una** transacción reusando `reportInTx` —no una segunda copia de la lógica de reporte— y, cuando alguna fila no valida, no aborta en la primera: anota el error, sigue evaluando y devuelve un 400 con el error de **cada** fila mientras deshace todo lo que las filas buenas alcanzaron a escribir. Un error que no sea de dominio (una violación de constraint) sí corta en el acto: a partir de ahí Postgres aborta la transacción y seguir juntando errores sería inventarlos. | Dos decisiones de forma que valen más allá de esta pantalla. **Los errores de una captura por lotes se devuelven todos juntos**: quien transcribe una hoja de papel necesita corregirla de una vez, y descubrir un error por intento convierte una tanda de doce filas en doce viajes. **La búsqueda de los largos es exacta y no glotona**, y eso no es refinamiento: con un plan de `2 × 4.20 m` más `1 × 6.00 m`, el reparto glotón por orden de plan no encuentra los 6.00 m —se lleva una plancha de 4.20 y se queda con 1.80 que no cierran— aunque la respuesta exista, y el operario habría visto "no cierra" en un caso perfectamente válido. Lo que **no** se hizo, a propósito: redondear la última plancha. Media plancha no existe, y elegir por el operario cuál largo recortar es inventarle un producto que después no coincide con lo que el cliente recibe. |
| D-148 | 2026-09-08 | **"Generar todas las órdenes" del pedido: una OP por línea pendiente, en una transacción.** Pedido del dueño. No cambia el modelo —sigue siendo **1 ítem = 1 OP** naciendo de su reserva (D-084), con las mismas validaciones y el mismo `CHECK` de D-145—, solo evita que un pedido de ocho líneas quede con cinco órdenes en cola y tres olvidadas porque alguien se distrajo a mitad de la lista. `POST /production/roofing/from-sales-order/:id` recorre las reservas `ACTIVE` de materia prima sin OP viva y llama a `createFromReservationInTx` por cada una; las líneas de catálogo (que reservan producto terminado, D-127/D-140) se saltan en silencio, porque su camino es la corrida a stock y no este botón. Sin reversa propia: anular una orden por separado ya existe (RF-33). En el mismo cambio, `planMeters` entra al DTO de la orden y la tarjeta de `/planta` muestra los ML a producir sin abrir el detalle. | El botón no agrega ninguna capacidad nueva: agrega **atomicidad** sobre una que ya existía, que es la misma razón por la que D-141 crea el pedido, su reserva y su OP en la misma transacción. La regla que queda: cuando una operación manual repetitiva tiene que ocurrir N veces para que el estado sea correcto, el botón que la agrupa vale menos por el ahorro de clics que por quitar del medio la posibilidad de hacerla N−1 veces. Que las líneas de catálogo se salten **en silencio** y no como error es deliberado: no son un caso que el usuario deba corregir, son un camino distinto (D-140), y convertirlas en error obligaría a leer un mensaje en cada pedido mixto. |
| D-149 | 2026-09-08 | **Hoja de planta del pedido: un PDF sin importes, armado al vuelo y nunca guardado.** Pedido del dueño. `GET /sales/orders/:id/pdf-planta` imprime número de pedido, cliente, fecha prometida y, por ítem, el producto, cuánto hay que producir, los largos (`10 × 4.20 m`) y las medidas que deciden qué bobina se monta (espesor, ancho, color — D-086), más un pie para firmar. **Ningún precio**: no es la cotización sin la columna de importes, es otro documento. Se descarga desde el pedido y, en el teléfono, se comparte con la Web Share API — el botón de compartir solo aparece si el navegador declara poder compartir **archivos** (`navigator.canShare({ files })`), porque `share` sin esa comprobación falla recién al apretarlo. | Dos diferencias deliberadas con el PDF de la cotización (D-068), y las dos tienen la misma raíz: **quién es el lector**. Aquel se le manda al cliente y por eso queda congelado en R2 tal como se envió; este es una copia de trabajo del estado actual, y una versión vieja guardada sería exactamente lo que no se quiere que baje al taller — así que se arma al vuelo y no se persiste. Y los importes no se omiten por prolijidad: una hoja con precios circulando por la planta es la forma más barata de que se entere todo el mundo del margen de cada pedido. |
| D-150 | 2026-09-08 | **Los importadores directos se eliminan: la carga histórica entra por el flujo real (cotización → pedido → orden → venta).** Decisión del dueño, tomada sobre las dos opciones que se le plantearon y eligiendo la amplia: se va **todo** el módulo de importaciones, no solo los dos de 7-final. Con él se van los cinco adaptadores (`PRODUCTS`, `CUSTOMERS`, `COILS` de RF-52/RF-12; `FISCAL_DOCUMENTS` de RF-71/RF-72; `COILS_HISTORY` de D-137 y `SALES_HISTORY` de D-138/D-141), el ciclo de lote `import_batches`/`import_rows` con su previsualización fila por fila, el `ImportDialog` del web y las cuatro puertas que lo abrían, el CLI `pnpm import:ventas` (D-142) y sus dos herramientas de reversa —`purge-imported-sales` (D-143/D-144) y la auditoría de importados—. **Se conservan tres piezas y cada una por su motivo:** `parse-spreadsheet.ts` con los lectores de celda por encabezado (tolerantes a tildes y mayúsculas, con el tope de 512 caracteres y la fecha como día calendario), porque leer una planilla que llenó un humano es la parte aburrida y ya probada que cualquier importador vuelve a necesitar; `document-lookup.service.ts` (el padrón de apis.net), porque no es del importador —lo usa el alta de cliente y lo va a usar la de proveedor—; y `FiscalImportService.annulImported` (D-110), que no es una vía de ingreso sino el remedio de las filas que ya entraron por una. **Las tablas y las columnas `import_batch_id` no se tocan**: borrarlas es irreversible y son el único rastro de las cargas que sí ocurrieron. | El importador genérico era una **segunda puerta al dominio**, y esa es la razón de fondo: cada adaptador tenía que reimplementar, contra la tabla, las reglas que el flujo real ya sabía —y por eso D-141 necesitó un método propio "sin código capaz de escribir una reserva", y por eso cada carga trajo su decisión de reversa (D-142, D-143, D-144), su guarda y su incidente. Una puerta que entra por atrás no hereda ninguna invariante: las copia, y las copias envejecen. El costo de la decisión es explícito y el dueño lo aceptó: cargar el histórico deja de ser un archivo y una confirmación, y pasa a ser el mismo trabajo que registrar la operación del día. Lo que se gana es que no haya dos definiciones de qué es un pedido válido. Lo que se pierde en cobertura queda anotado: la anulación de un importado conserva un solo caso E2E —que no alcanza a un comprobante emitido por el ERP—, porque los otros cinco empezaban importando. |
| D-151 | 2026-09-08 | **El autocompletado del padrón se extiende al alta de proveedor** (`GET /suppliers/lookup`), reusando el `DocumentLookupService` de D-067 en vez de duplicarlo: `SuppliersModule` importa `CustomersModule`, que es donde el servicio nació. Mismo throttle (20/min), mismo fallback silencioso —si el padrón no responde, no está configurado o el documento no existe, se avisa y la captura manual sigue disponible— y el rol acotado a **ADMINISTRADOR**, que es quien puede dar de alta un proveedor. Es la pieza que D-150 conservó explícitamente al borrar los importadores. | Un proveedor se identifica por RUC exactamente igual que un cliente, y tipear la razón social a mano es de donde salen los nombres que después no coinciden con la factura — el mismo problema que D-067 resolvió del otro lado del mostrador. Dos detalles que no son cosméticos: el rol es **más estrecho** que el de clientes (ADMINISTRADOR, no ADMINISTRADOR+VENDEDOR) porque el token de apis.net.pe es el mismo que sirve el tipo de cambio (D-029) y su cuota es una sola, así que cada rol nuevo con acceso es una forma más de quedarse sin ella; y el servicio se **importa** en vez de proveerse de nuevo, porque dos clientes del mismo servicio externo son dos contadores de cuota que no se ven entre sí. |
| D-152 | 2026-09-08 | **El importador masivo de cotizaciones: dos pasos sin estado en medio, y todo lo que crea pasa por el alta normal.** Es lo que reemplaza a los importadores directos que D-150 eliminó, y la diferencia es la idea entera: **no escribe contra la tabla, escribe una cotización**. `POST /imports/quotations/preview` lee el export real de ventas detalladas —una fila por línea, agrupadas por `SERIE - NÚMERO`—, resuelve el cliente por su documento y el producto por su SKU contra el maestro y devuelve las filas; el navegador las muestra en una tabla editable, deja quitar filas y manda de vuelta **lo revisado**. `POST /imports/quotations` crea una cotización **en BORRADOR** por comprobante, llamando a `QuotationsService.createInTx` (extraída con el patrón `*InTx`, D-099), en una transacción con un `SAVEPOINT` por documento: todo o nada, con el error de cada uno (mismo contrato que D-147). El número del comprobante externo viaja a las observaciones como `Factura externa: FFA1-1349`, la fecha del papel es la de la cotización (D-124) y el precio unitario sale de `VALOR DE VENTA ÷ CANTIDAD` con el tipo de cambio **del propio archivo** cuando el documento vino en dólares. Notas de crédito y filas con `DOCUMENTO AJUSTADO` se excluyen diciendo por qué. **Entre el preview y la confirmación no se guarda ni un byte**: no hay lote, ni filas persistidas, ni archivo en storage. | Tres reglas, y las tres son la lección de los importadores que se fueron. **Cero creación silenciosa**: un cliente o un SKU que no está en el maestro **detiene su fila**; D-138 los auto-creaba y por eso terminó necesitando un purge por lote (D-142..D-144). **Nada se adivina**: el archivo trae metros lineales pero no el plan de corte, así que el plan por defecto es `1 × los ML de la línea` y —cuando no cabe en una plancha, que es el caso de 23 de las 27 líneas a medida del archivo de agosto, con una de 1 832 m— la fila **pide el plan real** en vez de repartirlo en planchas de 6 m que el Excel no menciona; ese plan es después el tope duro de lo que planta puede reportar (D-146), o sea que inventarlo se paga dos veces. Y **el importador para en la cotización**: emitir y confirmar comprometen inventario, y eso se mira documento por documento. El defecto que el E2E destapó de paso vale por sí solo: SheetJS lee las fechas de un csv como M/D/Y, así que el 3 de agosto entraba como el 8 de marzo, sin error y sin señal — el csv pasa a leerse con `raw: true` y la fecha la interpreta quien conoce el formato del archivo. |
| D-153 | 2026-09-08 | **Un borrador de comprobante tiene dos terminales excluyentes: emitirlo por el PSE o registrarlo manual.** Pedido del dueño: la empresa sigue emitiendo desde otra app mientras dura la migración, y esos comprobantes tienen que quedar registrados acá con su cuenta por cobrar. El modo es un **tercer valor de `FiscalDocumentOrigin`** (`MANUAL`) y no un campo ortogonal, y el motivo es medible: las siete guardas que impiden mandar algo al PSE ya preguntaban por `origin` con la forma `!== IMPORTED`, así que un campo aparte las habría dejado **dejando pasar un manual a Nubefact**; con un valor nuevo se dan vuelta a `=== ISSUED_HERE` en una pasada y todo el comportamiento cae solo. `POST /documents/:id/register-manual` cierra el borrador con la serie y el correlativo del papel: `seriesId = null` —la serie del talonario no es una fila de `fiscal_series` y adelantar esa tabla quemaría rango de las series con las que se factura de verdad—, `status = ACCEPTED` por el mismo motivo que un importado (D-105), y sin CDR ni XML. Que los dos terminales partan del **mismo** borrador es lo que garantiza que un manual pase por las mismas validaciones que un electrónico —cliente, líneas contra el pedido, tope de la boleta genérica, detracción, fecha (D-124/D-133)—, porque son literalmente las de `createInTx`. Una **nota de crédito hereda el modo de su afectado**, con una guarda en cada terminal. La anulación interna de D-110 se generaliza (`annulExternal`): alcanza a todo lo que el ERP no emitió y a nada de lo que sí. Un ajuste global (`invoicing_settings.manual_by_default`) solo decide cuál de los dos botones viene destacado; los dos siguen siempre a la vista. | Lo que **no** hubo que hacer es la mitad de la decisión: el dueño pidió que "la salida de stock sea idéntica en ambos modos, misma ruta `InventoryService.record()`", y resultó que **el comprobante no mueve kardex en ningún modo** — lo mueve el despacho (D-074), por un solo camino. La garantía ya existía por construcción, así que no se tocó nada; vale anotarlo porque la pregunta va a volver. Lo que sí costó: **dos `CHECK` de la base escritos cuando la regla era más angosta**, `fiscal_documents_number_ck` (el número solo podía existir colgado de una serie del ERP) y `fiscal_documents_annulled_origin_ck` (la anulación interna era exclusiva de lo importado). Es **D-145 exactamente otra vez** —código que ensancha una regla y una base que sigue diciendo la anterior, con un `500` sin mensaje como único síntoma— y la única diferencia es que esta vez lo encontró el E2E antes del deploy, porque la sesión escribió el caso feliz **y** la reversa. La lección que queda: al agregar un valor a un enum que la base conoce, hay que ir a leer sus `CHECK`, que el compilador no ve. |
| D-154 | 2026-09-09 | **En producción de coberturas, el faltante de materia prima avisa; nunca bloquea. Y un pedido no se bloquea a sí mismo.** Amendment de D-084/D-134, pedido del dueño después de que planta reportara "la operación dejaría 0.000 kg libres… y hay 150.000 kg prometidos a PED-000003" **sobre el pedido que estaba fabricando**, con la bobina llena de material. El rediseño son tres capas y las tres hacían falta. **(a) Alcance de la excepción.** `exceptReservationIds` exceptuaba la reserva de la línea, pero un pedido de coberturas reserva **una vez por línea** y genera una OP por reserva (D-084/D-148), así que la promesa viva de la línea 2 bloqueaba el montaje de la línea 1. Ahora existe `exceptSalesOrderIds` y se exceptúa el **pedido entero**. **(b) Lo que una OP retiene.** Montar sacaba del agregado el **rollo completo**: planta monta los 5 000 kg del rollo para cortar los 200 que el pedido prometió, y el pool quedaba en cero sobre un almacén con 4 800 kg libres. La causa era un **doble descuento**: una OP que nace de un pedido ya tiene su compromiso contado como reserva genérica sobre la spec, y sumarle además la custodia contaba dos veces el mismo kilo. Ahora `heldKg` es **cero** cuando la orden tiene reserva —lo que va a consumir baja las dos cifras a la vez, con `consumeReservationQty` y la salida de kardex— y sigue siendo `assignedKg − consumedKg` solo para las corridas **a stock** (D-140), que no tienen reserva que las represente. **(c) Severidad.** En `mountCoil`, `report` y `close` la invariante **devuelve** los faltantes en vez de lanzarlos (`findRawMaterialShortfalls`): viajan en la respuesta (`ProductionOrderDto.rawMaterialWarnings`), quedan en `audit_log` y se persisten en `production_reports.raw_material_warning`. Fuera de producción —ventas, corte, bobinas, kardex— sigue siendo un rechazo, sin cambios. De paso, el tope duro de D-146 sobre el **kg declarado** pasa a aviso de desviación (`roofingConsumptionDeviation`, banda de ±10 %); el tope de **metros** del plan sigue siendo duro. | La severidad estaba mal elegida, y es el error que vale anotar: **la persona a la que el sistema le corta la operación no es la que puede resolverla**. Quien está en la roladora no anula el pedido de otro cliente ni libera su reserva —ni debería poder—, así que el 400 no protegía nada: el material se rolaba igual y lo único que cambiaba era que no quedara registrado, que es exactamente el escenario que la invariante existía para evitar. Un aviso con el pedido nombrado y los kilos que faltan sí llega a quien puede hacer algo. Las capas (a) y (b) son otra cosa: eran **defectos de la cuenta**, no de la severidad, y bloqueaban sin que hubiera ningún riesgo real. Vale ver que las tres se escondían detrás del mismo síntoma —un rechazo— y que solo una de las tres era el rechazo estando mal: cambiando únicamente la severidad, los dos falsos positivos habrían seguido ahí, ahora como avisos que nadie podría distinguir de los verdaderos. El tope del kg declarado cae por lo mismo que D-146 lo puso: es un **dato observado**, y un dato que hay que falsear para poder guardarlo deja de ser un dato. |
| D-155 | 2026-09-09 | **El espacio de producción del pedido: una pestaña por orden, con el ciclo completo adentro, y guardado por orden.** Retira la tanda de D-147. Pedido del dueño. La tanda dejaba transcribir los metros de N órdenes de una sentada pero **no montaba la bobina**, así que el encargado entraba orden por orden a la terminal para montar y recién después volvía a la tanda a reportar: dos pantallas para una sola tarea. `/planta/producir` (con `?pedido=`) muestra una pestaña por orden —lista lateral master-detail por encima de `MAX_ORDER_TABS`— con indicador **sin bobina / lista / reportada**, y cada pestaña monta o cambia su bobina con el mismo selector y los mismos endpoints que el detalle de la orden, muestra ML plan/reportado/restante y el kg teórico, y reporta. **El guardado es por orden**: montar ya lo era, y el todo-o-nada global de D-147 obligaba a rehacer ocho filas cuando la séptima tenía un dígito de más. Cada guardado individual sigue siendo atómico (reporte + kardex en una transacción vía `InventoryService.record()`). `POST /production/roofing/batch` se elimina; `GET /production/roofing/batch` queda y suma `reservationId` y, por bobina, `consumptionId` y `consumedKg`. `/planta/tanda` redirige a la vista nueva conservando el pedido. El sidebar pasa a **Órdenes de producción** (gestión), **Producir un pedido** (este espacio) y **Terminal de planta**. | La tanda no estaba mal pensada, estaba **cortada por la mitad**: modelaba la transcripción de la hoja de papel y no la corrida, y la corrida empieza montando el rollo. Sirve como criterio general — cuando una pantalla nueva obliga a volver a la vieja para completar la tarea, no es una pantalla nueva, es medio flujo. Lo del todo-o-nada es más fino y va contra la intuición: el contrato de D-147 ("la hoja entra entera") era correcto **para una transacción** y equivocado **para una persona**, porque el error de una fila no invalida las otras siete y rehacerlas es trabajo que el sistema inventa. La atomicidad que sí importa es la de cada reporte con su kardex, y esa no se toca. Sobre los nombres del menú: "Producción" y "Terminal de planta" eran dos rótulos que sonaban al mismo lugar y nadie podía decir cuál era cuál sin entrar; el sustantivo tiene que decir el **objeto** —una orden se gestiona, un pedido se produce—, no el sitio. |
| D-156 | 2026-09-09 | **Ningún campo obligatorio es un callejón: si falta la opción, se crea desde ahí con el alta de siempre.** Pedido del dueño sobre el importador de cotizaciones (D-152), pero la regla es general. El preview pasa a un **acordeón por comprobante** —número, cliente, total y estado de validación en la cabecera; las líneas al expandir—, porque el archivo de un mes son decenas de facturas de tres líneas y en una tabla plana no se veía de qué factura era una fila ni cuánto sumaba. Junto a cada campo que exige elegir de un maestro hay un botón **crear** que abre **el mismo formulario de alta** (`CustomerDialog` y `ProductDialog`, movidos a `components/`, con `initial` para prellenar lo que el archivo ya trae y `onCreated` para devolver el registro): mismas validaciones, mismo endpoint, y **el estado del preview no se pierde**, que es la mitad que importa. Los desplegables de más de `SEARCH_SELECT_THRESHOLD` (50) opciones pasan a un modal de búsqueda con tabla, filtro de texto y columna «Seleccionar» (`SearchSelectModal` / `SearchSelectField`). | No contradice a D-152, la completa. Lo que D-152 prohibió fue la **creación silenciosa** —D-138 daba de alta clientes y SKUs por su cuenta y por eso terminó necesitando un purge por lote (D-142..D-144)—, no que se pudiera crear algo desde el importador: acá sigue habiendo un acto explícito de una persona, con el formulario completo del maestro delante. La diferencia entre las dos cosas es **quién decide**, no dónde está el botón. Y el callejón tenía un costo medible: la única salida era irse al maestro y volver, con las 141 filas revisadas perdidas en el camino, así que la conducta que el diseño premiaba era **quitar la fila** —o sea, perder el dato— en vez de resolverla. Un `<select>` de 900 productos es la misma clase de callejón por otro lado: no se filtra, no se busca, y hay que reconocer el SKU de memoria mientras se recorre la lista con la rueda del mouse. |
| D-157 | 2026-09-09 | **Una cotización puede no tener fecha de vencimiento, y `NULL` significa «no vence».** Pedido del dueño sobre el importador (D-152). `quotations.valid_until` pasa a nullable, `createQuotationSchema.validityDays` acepta `null` y el importador crea con `null`. Todo lo que preguntaba por el vencimiento pasa por `isQuotationExpired(validUntil, hoy)` de `@ayr/shared`: la confirmación del pedido, el estado efectivo del listado y del PDF, el job diario de vencimiento (que ya las excluía solo, porque `valid_until < hoy` es `NULL` en SQL y `NULL` no es verdadero) y la reapertura de la cotización al anular su pedido. **El `null` no viaja por HTTP**: `createQuotationSchema` —que es el cuerpo de `POST`/`PUT /sales/quotations`— sigue exigiendo la vigencia, y el importador pasa `null` por código, con un tipo interno (`CreateQuotationInternalInput`) que solo acepta `createInTx`. El PDF imprime «sin vencimiento». | Las dos alternativas eran peores y las dos mienten. Con la vigencia por defecto, las 71 cotizaciones de agosto **nacían vencidas** —emisión de hace un mes, siete días de validez— y ninguna se podía confirmar: el importador dejaba el archivo cargado y el paso siguiente cerrado. Con una fecha lejana inventada, el sistema afirmaba una vigencia comercial que nadie pactó. La tercera opción es decir la verdad: **ese comprobante ya se vendió, no hay vigencia que respetar**. Lo que importa del cambio no es el `null` sino la función: `validUntil < hoy` escrito suelto convierte la ausencia en `'' < hoy`, o sea en «vencida desde siempre», y el compilador no avisa porque las dos son comparaciones de cadenas válidas. Por eso hay **una sola** función que responde la pregunta, con su test. |
| D-158 | 2026-09-09 | **El importador da de alta al cliente desde el padrón, y el cliente es del comprobante y no de la línea.** Dos partes de la misma pantalla. (a) Cuando el RUC/DNI del archivo no está en el maestro, el preview consulta apis.net.pe (el mismo servicio de D-029/D-067, con tope de `MAX_PADRON_LOOKUPS` consultas y concurrencia acotada) y la cabecera muestra **«Nuevo — se creará desde padrón: <razón social>»**; al confirmar, el cliente se crea con `CustomersService.createInTx` **dentro de la transacción del archivo**. La fila manda al servidor **solo el documento**: el nombre lo trae el padrón del lado del API, y las consultas se resuelven **antes** de abrir la transacción. Si el padrón no responde, no se importa nada y esa fila se resuelve con el alta express de D-156. (b) El campo de cliente se mueve de la línea a la **cabecera del acordeón**: una factura es de un solo cliente y sus diez líneas lo heredan. | Es una **excepción controlada** a la regla de D-152, y la diferencia con la creación silenciosa de D-138 son tres cosas concretas, no una intención: está **a la vista antes de apretar** (con nombre y documento, en la cabecera), la decide **una persona** que puede elegir otro cliente en el mismo campo, y lo que se escribe **no lo elige el navegador** — mandar la razón social en el request habría dejado dar de alta «PROVEEDOR S.A.C.» bajo un RUC ajeno con solo editar el cuerpo. Lo de la cabecera es más simple y más viejo: pedir diez veces el mismo dato no es redundancia, es **la única forma de armar un documento imposible**, y el API tenía que rechazarlo con un error que el usuario no podía atribuir a ninguna fila. |
| D-159 | 2026-09-09 | **El espacio de producción v2: plan editable, reporte por líneas que se autocompleta al montar, selector de bobina con búsqueda, y «Guardar y cerrar» atómico.** Amendment de D-155, pedido del dueño. (a) El **plan de corte se edita desde el panel** —cantidad y largo en una cobertura a medida, solo cantidad en una plancha de catálogo, donde el largo lo trae el SKU (D-118)—. (b) **Montar la bobina rellena el editor del reporte con los largos que el plan todavía debe**, editables y borrables antes de confirmar; el campo único de metros/planchas desaparece. (c) El selector de bobina pasa de lista de tarjetas a **modal de búsqueda con tabla** (código, espesor, color, kg disponibles, «Montar»), reusando el `SearchSelectModal` de D-156 con columnas. (d) Tres acciones por orden: **Guardar** (reporte parcial, la bobina sigue montada), **Cerrar sin reportar más** —que existe siempre que la orden haya producido algo, no solo con el plan cubierto: es el caso de la bobina que se acabó a mitad del plan— y **Guardar y cerrar** (`POST /production/roofing/:id/report-and-close`), que corre `reportInTx` + `closeInTx` en **una transacción** y libera la bobina; cuando lo que se va a reportar cubre el plan, es el botón destacado. | El caso que lo obliga es de dominio, no de comodidad: un pedido de coberturas genera **una OP por línea** (D-084/D-148) y todas se rolan **del mismo rollo**, pero mientras la primera siga abierta con la bobina montada, `assertStripsNotAssigned` no la deja montar en la segunda. Producir un pedido de cuatro líneas obligaba a cerrar cada orden desde otra pantalla antes de seguir. Partido en dos endpoints, además, el cierre podía fallar con el reporte ya escrito. Lo del autocompletado es la misma idea aplicada a la captura: **el caso normal es rolar lo que el pedido pide**, y hacerle transcribir largo por largo lo que la pantalla ya tiene en la mano es trabajo que el sistema inventa — pero las líneas quedan editables y **se pueden borrar**, porque lo primero que hace quien roló la mitad es sacar las que no salieron. |
| D-160 | 2026-09-09 | **Una sola entrada a producción.** La terminal de planta (`/planta`) y el espacio de producción del pedido (`/planta/producir`) se funden en **`/planta`**, con filtro por pedido (`?pedido=`) o todas las órdenes abiertas, y con las dos clases de orden en el mismo selector: la de coberturas abre el panel de D-159 y la de drywall el de piezas. Crear una orden deja de ser el paso 1 de la pantalla y pasa a una sección que se despliega. El sidebar queda con **Producción** (producir) y **Órdenes de producción** (gestionar). El detalle de una orden (`/produccion/:id`) queda de **lectura más corrección** —anular, revertir un reporte, reabrir— y **pierde el cierre**, que era lo único que estaba en los dos sitios; sus enlaces a planta se unifican en «Producir esta orden». `/planta/producir` y `/planta/tanda` quedan como redirecciones. | D-155 había intentado arreglar esto **con los rótulos** —«Producir un pedido» contra «Terminal de planta»—, y no era un problema de nombres: eran dos pantallas que hacían lo mismo con la mitad de las herramientas cada una. La terminal montaba y cerraba pero no mostraba las hermanas del pedido; el espacio mostraba las hermanas pero no cerraba. Quien producía un pedido de cuatro líneas iba y venía entre las dos, y ninguna era la de verdad. El criterio que queda: **cuando dos pantallas comparten el objeto y se reparten los verbos, no son dos pantallas, es una partida al medio** — y el rótulo no la junta. Y el corolario sobre el cierre duplicado: dos botones que hacen lo mismo en dos sitios terminan divergiendo en lo que validan **antes** de llamar al API, que es donde vive la mitad de la lógica de una pantalla. |
| D-161 | 2026-09-09 | **La plancha de catálogo se cotiza por metro lineal.** Un SKU `PLANCHA` con largo en el maestro se negocia por **valor por metro** y el API calcula el valor unitario: `largo del SKU × valor por metro`. La línea sigue midiéndose en `NIU` —se cuenta, se reserva, se produce y se despacha en planchas— y lo único que cambia es de dónde sale su valor unitario. La cotización gana un bloque espejo del plan de corte de D-159: **largo bloqueado con el del SKU y solo la cantidad editable**, sin filas que agregar. Columna nueva `value_per_meter_pen` en `quotation_items` y `sales_order_items`, nullable: guarda el número que el vendedor negoció, para mostrarlo y reabrirlo a editar sin dividir. **Nada se recalcula**: las cotizaciones y pedidos anteriores, y los que carga el importador (D-152), quedan como están. | Se estaba cobrando `cantidad × valor` con un valor que el vendedor pensaba por metro: diez planchas de 3.60 m a S/ 7.00 el metro salían S/ 70.00 en vez de S/ 252.00 — **3.6 veces menos, que es exactamente el largo del SKU**. La alternativa era pasar la línea a metros como una a medida, y se descartó: el kardex, la reserva y el despacho de una plancha están en planchas, y forzarlos a metros obligaba a que un SKU en `NIU` llevara subítems de largo, que es justo lo que la regla dura 14 prohíbe. El criterio que queda: **cuando la unidad de negociación y la unidad de inventario no coinciden, se guardan las dos y se deriva una de la otra en un solo lugar** — nunca se reinterpreta el mismo campo según el producto. |
| D-162 | 2026-09-09 | **«Valor de venta» es sin IGV; «precio de venta» es con IGV**, en toda la aplicación y en los PDF. La fuente de verdad interna no cambia: `unit_price_pen`, `subtotal_pen` y lo que va a SUNAT/Nubefact siguen siendo **valores** sin IGV, y no se migra ni un dato. Lo que cambia es (a) los rótulos de cotización, pedido, comprobantes y PDF, y (b) que en el formulario de cotización y pedido se **tipea el precio con IGV** y el sistema deriva el valor (`precio ÷ 1.18`), mostrando los dos. La traducción vive en un módulo hoja nuevo, `packages/shared/src/tax.ts`, junto con `IGV_RATE_PCT`, que se mudó ahí desde `schemas/sales`. | El vendedor negocia con el cliente en precios con IGV —es el número que el cliente paga— y la pantalla le pedía un número sin IGV bajo el rótulo «P. unitario». Cada vez que tipeaba lo acordado, el documento salía un 18% más caro. Que las dos palabras se usaran como sinónimas es lo que hizo posible el error, así que la corrección es de vocabulario antes que de campos. El módulo es hoja y no vive en `schemas/sales` por D-130: `schemas/pricing` necesita el IGV y se carga **antes** que `sales`, y el ciclo en CommonJS no lanza — deja `undefined` y borra el factor en silencio. |
| D-163 | 2026-09-09 | **Piso duro de precio, y el margen es sobre la venta.** `precio mínimo = costo unitario promedio del kardex ÷ (1 − margen mínimo) × 1.18`, con el **margen mínimo** de `pricing_settings` (el margen a secas queda como el objetivo sugerido). Reemplaza el markup `costo × (1 + margen)` de D-032, que daba otra cosa: con 20% sobre un costo de 100, el markup deja un margen real del 16.67%. Ninguna cotización ni pedido nuevo se guarda por debajo del piso, **para todos los roles incluido el ADMINISTRADOR**; el ajuste legítimo es cambiar el margen mínimo en Configuración → Márgenes, que queda auditado. Alcanza al alta, la edición y el duplicado de cotización y al pedido directo. Quedan **exentos**: lo que importa D-152 —y también su edición, porque una cotización histórica nace exenta y corregirle una línea no la vuelve una oferta nueva— y el **mostrador**, que no tiene dónde mostrar el mínimo y cuyo rechazo tiraría abajo la transacción entera de la venta de caja. Sin costo en el kardex no hay piso. El costo de una cobertura a medida es `kg por metro × costo por kg ponderado del agregado compatible`, porque su producto terminado no tiene saldo hasta que planta lo rola. El margen se acota a menos de 100% en el schema. | D-032 escribía la regla —«un VENDEDOR no puede bajar del margen mínimo»— y **no la aplicaba en ninguna parte**: `minAllowedPrice` no tenía un solo llamador fuera de su propio test, y la página de márgenes era decorativa. Dos detalles que decidieron la forma: la comparación se hace **valor contra valor y no precio contra precio**, porque el valor guardado es el precio dividido por 1.18 y redondeado a cuatro decimales, y comparando precios «exactamente en el mínimo» rebotaba por una diezmilésima; y el piso viaja al formulario en el panel de stock, calculado **por la misma función** que después rechaza, para que la pantalla no pueda prometer un mínimo distinto del que el `POST` exige. La revisión encontró que eso último no alcanzaba: el mínimo se **muestra** con dos decimales y el piso vive con cuatro, así que recortarlo hacia abajo hacía que tipear exactamente el número de la pantalla cayera por debajo y el sistema respondiera «sube el precio» sobre el precio que él mismo pedía —en cerca de la mitad de las combinaciones—. El mínimo que se muestra lo calcula ahora `minTypeablePrice`, que **lo prueba contra la cadena de vuelta** en vez de redondear y confiar, y se expresa en la unidad en la que se tipea (por metro en una plancha). La contrapartida asumida: el costo se puede despejar del piso, y el margen lo lee todo el equipo comercial. |
| D-164 | 2026-09-09 | **El cierre de una bobina liquida su remanente (RF-19).** Cerrar deja de ser solo un cambio de estado: si el saldo teórico de `inventory_balances` no coincide con lo que planta declara que queda físicamente en el rollo (`physicalKg`, obligatorio cuando hay saldo), la diferencia sale como movimiento de kardex **en la misma transacción**, vía `InventoryService.record` y nunca editando el saldo (regla dura 2). Sobra saldo teórico → `OUT`; el conteo da de más → `IN` valorizada al promedio vigente, o al costo del documento si el saldo estaba en cero. `refType` propio **`CLOSE_ADJUSTMENT`**, distinguible de los consumos y de la merma de RF-17 en el kardex de la bobina. Exige motivo escrito en los dos sentidos y lleva `operationDate` (D-124). **Se deshace reabriendo la bobina**, que lo revierte con un movimiento inverso **solo si el ajuste es el último movimiento vivo del rollo**; `cancelScrap` (RF-18) lo rechaza a propósito. El cierre automático del partido (RF-15) y de la recepción de corte (D-052) no cambia: ocurre con saldo cero y no tiene nada que liquidar. | Una bobina cerrada desaparecía de producción y del partido, pero **seguía sumando kilos y valor al inventario valorizado** de material que ya no existe, y nada avisaba: el saldo se quedaba ahí indefinidamente. La única herramienta era la merma de RF-17, un acto aparte, sin relación con el cierre y que nadie recordaba usar. Tres cosas decidieron la forma. **(a)** Se tipea _cuánto queda_, no _cuánto se da de baja_: es lo que planta puede ver mirando el rollo, y con un solo campo quedan cubiertos los dos sentidos. **(b)** El `refType` propio no es cosmético — es el mismo cuidado que ya obligó a separar la merma de proceso del cierre de una OP dentro de `cancelScrap` (D-057), donde dos hechos bajo un mismo `refType` dejaban que la anulación equivocada creara valor de la nada; y es lo que permite **medir la merma anormal**, ahora que D-165 metió la normal en el estándar. **(c)** El corte para "no hay nada que liquidar" es que no se haya declarado nada, **no** que el saldo esté en cero: cortar por el saldo dejaba fuera el sobrante sobre una bobina ya consumida, que es justo el caso para el que existe la rama `IN` (lo encontró el E2E, con el cierre pasando y los kilos sin volver). **(d)** La reversa exige que el ajuste sea el **último movimiento vivo** y no simplemente el último `CLOSE_ADJUSTMENT` vivo, que fue la primera versión y estaba mal: una bobina puede volver a `OPEN` por un camino que no es este —`revertSplit` (RF-16) reabre a la madre con un `update` directo—, y ahí el ajuste queda vivo y sin dueño; el siguiente ciclo cerrar→reabrir lo adoptaba y devolvía kilos y valor que ningún cierre había sacado. Con la condición del último movimiento, el kardex append-only garantiza que un ajuste que dejó de ser el último **nunca vuelve a serlo** y queda inerte para siempre. Lo encontró la revisión. El precio asumido: cerrar con saldo es ahora un cambio **no aditivo** del API — cinco escenarios E2E que cerraban un rollo para guardarlo tuvieron que declarar su saldo entero. |
| D-165 | 2026-09-09 | **La merma normal del 1 % se absorbe en la densidad estándar.** Regla del cliente fijada por el dueño: convertir geometría en kilos rinde siempre un 1 % menos de material del que el prisma perfecto promete. Se aplica **en código y en un solo lugar** —`standardDensityFactor(densidad) = densidad × 1.01` en `@ayr/shared`, dentro de `theoreticalKgPerPiece` y `kgPerMeter`, las dos únicas cuentas que traducen geometría a kilos— y **nunca** editando `finishes.density_factor`. Con eso se mueven juntos el kilo teórico del reporte de planta, el metro equivalente de una bobina, los kilos que reserva una cobertura a medida y el costo por metro del piso de precio (D-163). Centinela en `apps/api/src/production/normal-scrap-factor.spec.ts`. | El 1 % es una política de la empresa y la densidad del acero (7.85 t/m³) es un dato físico: multiplicarlos en el maestro deja un número que no es ninguna de las dos cosas y que **nadie puede volver a separar** —revisar el porcentaje obligaría a dividir cada acabado por 1.01 primero, adivinando cuáles ya lo tenían—, además de que cada acabado nuevo nacería sin el factor. Aplicado en código, entra por un punto y las dos cuentas no pueden divergir: si el kilo por metro y el kilo por pieza llevaran factores distintos, los kilos que un pedido promete por metro y los que el reporte descuenta por metro serían dos números para el mismo hecho. Y es lo que vuelve **medible la merma anormal**: con la normal adentro del estándar, el remanente que D-164 liquida al cerrar la bobina ya no mezcla las dos. Consecuencia visible y buscada: el mismo rollo promete ~1 % menos metros que antes, y el costo estándar por metro sube otro tanto. Pendiente explícito: revisar el 1 % con datos de 2-3 cierres reales, y ver si conviene por acabado. |
| D-166 | 2026-09-09 | **El largo fijo de una plancha tiene que ser un largo posible, y el campo dice en qué unidad va.** `products.length_mm` de un `PLANCHA` queda acotado al mismo rango que el plan de corte ya exigía a cada largo tipeado a mano (`MIN_PIECE_LENGTH_MM`..`MAX_PIECE_LENGTH_MM`, 0.1 a 20 m), con la pregunta en una sola función compartida (`isPlausiblePieceLength` en `@ayr/shared`). Tres cortes, en los tres momentos: **el catálogo** no deja guardarlo (y el diálogo traduce el número a metros mientras se tipea, con el aviso al lado); **la línea de venta** se niega a cotizar un producto ya guardado con el largo roto (`assertUsableFixedLength`, que nombra el SKU y explica la unidad) en vez de calcular; y **`check-roofing-catalog`** los lista para encontrarlos en una base que ya los tiene. No se migra ni un dato: los productos existentes se corrigen a mano. | El campo del catálogo pide **milímetros** y todo el resto de la pantalla de coberturas trabaja en **metros**, así que las **tres** planchas cargadas por el dueño tenían `3.00` y `6.00` donde iban 3 000 y 6 000. Un largo de 3 mm pasa el `> 0` que `sellsByFixedLength` mira, así que D-161 multiplicaba por él y la línea salía **mil veces más barata sin un solo error**: diez planchas a S/ 11 el metro daban **S/ 0.28** en vez de S/ 330, con la pantalla mostrando «Largo (m) 0.00» y «0.030 m lineales» — dos números correctos que había que saber leer. Lo que decidió la forma: **el corte no podía ser que `sellsByFixedLength` devolviera `false`**. Con eso la línea caía en el camino viejo —valor por plancha tipeado a mano— y el vendedor escribiría 11 pensando «por metro»: volvía a estar mal, en silencio y por otro lado. Hace falta **cortar**, no elegir otra rama, y por eso la precondición vive aparte del predicado. El rango tampoco es una cota inventada para este caso: es exactamente el que la rama a medida ya aplicaba a sus subítems desde D-083, y el defecto fue que el largo del **maestro** —el único que nadie tipea al cotizar— era el que no lo cumplía. |
| D-167 | 2026-09-10 | **Una línea de negocio sin inventario se cotiza, se vende y se factura; lo único que no hace es prometer existencias.** La exención la decide el **atributo** `business_lines.inventory_strategy = NOOP` y nunca el código de la línea, en una sola función compartida (`carriesInventory` en `@ayr/shared`) que reemplaza a las tres comparaciones sueltas que había. Cinco puntos: `resolveSalesLines` deja de rechazar la línea; `createReservations` no le abre reserva (abrírsela la mandaba a `lockAvailability`, que crea la fila de saldo de un ítem que nunca va a tener movimientos y rechaza el pedido con «0.000 disponibles»); el **piso de precio** no se le aplica, porque un servicio no tiene costo promedio contra el que medirlo; el **despacho la rechaza** —no sale nada del almacén y no hay peso que declarar en una guía— y `recomputeOrderStatus` no la espera para llegar a `FULFILLED`; y el web deja de filtrar el selector de línea de negocio por `inventoryStrategy`, con `ProductStockDto.carriesInventory` para que el panel diga «Servicio: no lleva inventario» en vez de un cero. El mostrador (POS) **sigue** sin ofrecerlos, y ahora es una decisión escrita: su lista nace del saldo (D-098) y un servicio no tiene saldo del que salir. | «Línea 1: el producto CONFORMADO es de una línea sin inventario: no se cotiza». Un servicio de conformado es de lo que la empresa **más** factura, y no se podía cotizar por ninguna puerta: ni a mano ni por el importador. El rechazo cortaba **antes** de llegar al kardex, que ya trataba el caso como el no-op explícito que es (§2.2) — o sea, la mitad de abajo del sistema estaba lista y la de arriba no dejaba llegar. La forma importa por dos motivos. **Por atributo y no por código**: agregar «Fletes» o «Montaje» como línea `NOOP` no puede obligar a tocar una condición, y la que había se leía como «services está prohibido» en vez de «esta pregunta no aplica». Y **el cero de un servicio no es el cero de un producto agotado**: se ven iguales y significan lo contrario —uno es «hay que reponer», el otro es «esta pregunta no aplica»—, así que la bandera viaja explícita en el DTO en vez de deducirse de `availableQty === '0.000'`. Un pedido de solo servicios no llega a `FULFILLED` y se queda en `CONFIRMADO`: el ERP no modela la ejecución del servicio y fingir que sí sería peor. |
| D-168 | 2026-09-10 | **El SKU de venta directa de una bobina lo calcula una sola función.** `coilSkuFromTypeKey` deja de borrar todos los guiones del `typeKey` y pasa a partirlo por su **último** guion —el separador que `coilTypeKey` puso entre el acabado y el espesor— y a delegar en `coilSku`, que queda como la única fuente. Con un `typeKey` que no se puede partir devuelve el prefijo sobre la cadena entera en vez de inventar un espesor: quien llame va a buscar un producto que no existe y lo va a reportar nombrando el SKU, que es mejor que un 500 sin nombre. `pnpm check:coil-skus` audita, de solo lectura, qué `typeKey` quedó sin producto y qué productos `BOB…` no nombra ninguna bobina; **no migra nada**. | El guion es un **separador**, no un carácter a borrar, y los códigos de acabado del cliente lo llevan adentro: `ALZ-ROJO-3002`. Con eso las dos cuentas que decían calcular el mismo SKU devolvían cosas distintas — el catálogo daba de alta `BOBALZ-ROJO-30020.45` (`coilSku`, que conserva los guiones del acabado) y la venta directa buscaba `BOBALZROJO30020.45` (`coilSkuFromTypeKey`, que los borraba todos)—, así que RF-73 respondía «no existe el producto de venta directa» sobre una bobina que sí tenía el suyo. Es la clase de defecto que solo aparece con los datos reales: con un acabado sin guiones (`GALV`) las dos funciones coinciden y el test que había pasaba. El criterio del último guion no se inventó acá: `describeTypeKey` ya leía el `typeKey` así en el inventario valorizado. Auditado el 2026-09-10 contra `production` (11 tipos de bobina) y `local`: **todos** resuelven a su producto con la función arreglada, así que no hay ningún dato que migrar; los dos `BOB…` huérfanos de cada base son SKU creados a mano, no restos de la forma vieja. |
| D-169 | 2026-09-10 | **El importe de una línea importada es el del papel, no el del recálculo.** El archivo trae `VALOR DE VENTA` por línea; el ERP sigue derivando el valor unitario dividiéndolo entre la cantidad —el comprobante electrónico lo necesita— pero **persiste el importe tal cual**: `netAmountPen` viaja por fila y `resolveSalesLines`, bajo la opción `exactAmounts`, lo guarda como subtotal (el IGV sale de ahí con la cuenta de siempre). Fuera del importador, mandar `netAmountPen` es un 400. La fila cuya cantidad o precio el usuario editó en el preview viaja **sin** importe y se recalcula; editar una cotización importada conserva los importes de las líneas cuyo producto, cantidad y precio no cambiaron. El ajuste de redondeo por línea **se deriva** (`subtotalPen − redondeo(qty × unitPricePen)`) y no se guarda: son dos datos ya persistidos y una columna más sería una segunda fuente para el mismo número. La tolerancia por documento es `Σ\|ajuste\|` contra `max(S/ 0.10, Σ (cantidad + 1) × 0.00005)`, y el rechazo nombra el documento, la línea peor y las dos cifras. Aguas abajo, tres cierres más: el **comprobante** que factura una línea de pedido entera a su propio precio **copia** su importe en vez de recalcularlo; la **cabecera** de ese comprobante y la de una nota de crédito **suman sus propias líneas** (`sumLineTotals`) en vez de volver a derivarlas del unitario; y la **cobranza** compara el saldo en céntimos (`payableBalance`, redondeo hacia arriba), que es la escala en la que se cobra. Acreditar una línea entera acredita su importe entero, por el mismo motivo: con el recálculo, una nota de crédito total sobre un comprobante importado no llegaba al total del afectado y su cuenta por cobrar no cerraba nunca. | Dividir un importe entre la cantidad, redondear a cuatro decimales (D-003) y volver a multiplicar **no devuelve el importe**: el desvío es de hasta `cantidad × 0.00005`, invisible en una línea de tres unidades y de doce céntimos en una de 2 500 kg — el tamaño normal de una línea de acero. Un comprobante ya emitido es un documento legal y sus importes no se recalculan: lo que se deriva es el unitario, que es el dato calculado del papel, no el firmado. Dónde salía a la superficie, reproducido en local: la **cobranza**. Un total de S/ 117.9999 contra un papel de S/ 118.00 rechazaba el cobro real con «El cobro excede el saldo pendiente (S/ 118.00)» — el mensaje redondeaba para mostrar y la comparación no, así que el usuario leía dos cifras idénticas y un 400 sin salida. Por eso hay dos arreglos y no uno: D-169 quita la **causa** de que un total importado tenga cola de diezmilésimas, y `payableBalance` quita la clase entera, que también alcanza a un precio tipeado a mano. Y por eso la tolerancia **escala con la cantidad**: el techo plano de S/ 0.10 que se escribió primero rechazaba justo los documentos que esta decisión venía a poder importar, acusando al archivo de un desvío que había producido el propio ERP al dividir. Lo que queda por encima de la cota es lo único que el redondeo no pudo haber hecho — la columna del precio con IGV donde va el valor, un cero de más en la cantidad—, y eso hay que verlo al importar y no al cobrar. |
| D-170 | 2026-09-10 | **La venta de una bobina entera la cierra (RF-73).** Al despachar, el rollo que quedó con saldo cero y sin ninguna reserva viva pasa a `CLOSED` en la misma transacción, con la auditoría del cierre (`coils.close`) nombrando el despacho; revertir el despacho lo **reabre** (`coils.open`), porque el movimiento inverso le devuelve los kilos y dejarlo cerrado lo pondría con saldo vivo y fuera de producción a la vez. Un despacho parcial que deja remanente **no** lo cierra: ese camino sigue siendo el cierre manual de D-164, el que pide cuántos kilos quedan de verdad y liquida la diferencia como merma anormal. El movimiento de kardex **no** estrena `refType`: sigue siendo `SALE` sobre `itemType = COIL`, que ya es inequívoco. Y `SellableCoilDto` gana `avgCostPen`, el costo promedio del propio rollo. | El resto de RF-73 ya existía desde D-116 —saldo vigente y no nominal, sin fracciones, reserva del rollo entero, `OUT` directo sobre la bobina sin pasar por ningún kardex de producto intermedio, y una bobina reservada que ni se monta en producción ni se manda a corte— y lo que la rompía en la práctica era el SKU de D-168. Lo que faltaba era el cierre: el rollo quedaba `OPEN` con saldo cero y seguía apareciendo en las listas de bobinas abiertas y en los desplegables de planta para siempre, esperando que alguien se acordara. El cierre es parte del hecho, no una tarea posterior. **`refType` propio se evaluó y se descartó**: agregar `COIL_SALE` al enum obliga a una migración y a revisar la reversa, que hoy matchea por `SALE`, a cambio de distinguir algo que `itemType = COIL` ya distingue solo — una venta de producto nunca tiene ese `itemType`. El costo promedio viaja porque una venta de bobina es a precio negociado por kg y a ojo, y hasta acá lo único que le decía al vendedor si se había pasado era el piso de D-163, del que ese costo ya se despejaba: mostrarlo directo no revela nada nuevo. |
| D-171 | 2026-09-10 | **La plancha de catálogo se produce contra el pedido; revierte D-140.** Nace una **cuarta** pregunta, `isMadeToOrder(product)` = `isMadeToMeasure(product) || sellsByFixedLength(product)`: _¿esta línea se fabrica desde bobina contra el pedido?_ La responden que sí la cobertura a medida y la plancha **con largo fijo usable**. Una línea de plancha reserva `RAW_MATERIAL` en kilos del agregado color+espesor, con `orderedMeters` convirtiendo su cantidad —planchas— a los metros que consume: `cantidad × largo del SKU`. El 1 % de merma normal **no** se suma aparte: vive dentro de la densidad estándar desde D-165. La forma de la línea no cambia — se sigue contando en `NIU`, sin detalle de largos, y cotizando por metro contra el largo del SKU (D-161)—, el plan de corte de la OP se sigue derivando de `(largo, cantidad)` y `resolveDispatchTarget` pasa a decidir «se fabrica contra el pedido» por el **subtipo** y no por la receta, que desde D-122 una cobertura ya no tiene. El mostrador deja de ofrecer coberturas (su lista nace del saldo y una plancha ya no tiene salida por ahí) y la tarjeta «Nueva orden de coberturas a stock» de `/planta` se retira con la puerta que cierra en el API, para no dejar el callejón que D-156 prohíbe. **Producir coberturas a stock deja de existir**: `create` sin reserva responde 400 y `createToStock` queda sin llamadores. Ninguna migración: la rama de la reserva se recalcula al **confirmar** el pedido (`resolveRawMaterial`), así que las cotizaciones vivas se confirman solas bajo el modelo nuevo. | Regla de negocio del dueño, que corrige la premisa de D-140: una plancha **no es stock terminado**, es un largo fijo que la roladora corta cuando alguien lo pide. Con el modelo viejo el mostrador exigía tener planchas en el almacén —«0.000 NIU disponibles… necesita 10»— sobre un producto que nunca vive ahí. **Quitar la producción a stock es una decisión de negocio y es reversible**: es la contracara necesaria de lo anterior, porque si además se pudiera producir a stock quedaría un saldo de producto terminado que ningún pedido consume nunca —cada uno reserva materia prima y rola lo suyo— y ese saldo envejecería en el valorizado sin que nadie pudiera venderlo. Para reabrirla hay que responder primero qué hace una línea de pedido cuando ese saldo existe (¿lo toma?, ¿produce igual?, ¿lo toma hasta donde alcanza?), que es la pregunta que hoy no tiene respuesta; por eso la puerta se cierra en vez de dejarse entreabierta, y `createToStock` queda en el archivo para que reabrirla sea volver a enchufarla. La función es **nueva** y no un cambio a `isMadeToMeasure` por la regla dura 14: las cuatro preguntas de la familia devuelven `boolean` y el compilador nunca avisa cuando se responde una con otra — responder «¿se produce?» con `isMadeToMeasure` devuelve la plancha al modelo viejo en silencio. El centinela `sales-lines.spec.ts` ahora cubre las cuatro. **Y no es «tener subtipo», que fue la primera versión y la revisión la encontró mal:** el `CHECK` de la base es más laxo que la app —admite una `PLANCHA` en `KGM` (hay SKU legados) y una sin largo— y en esas dos la cantidad no son planchas de un largo conocido, así que mil kilos se leían como mil planchas y la línea reservaba catorce toneladas de bobina por una venta de una. Esas dos se siguen atendiendo con saldo de producto terminado, exactamente como antes de D-171: lo único que esta decisión cambia es la plancha que de verdad se puede rolar. Que la rama la decida `sellsByFixedLength` —la misma que decide que el precio va por metro— es además lo correcto: el precio y el material salen del mismo largo, así que corregirlo en el catálogo mueve las dos cuentas juntas o ninguna. Censo previo (2026-09-10, `production` y `local`, solo lectura): 19 SKU de plancha en producción, **0** líneas en cotizaciones o pedidos vivos, **0** saldo de producto terminado y **0** OP a stock, así que el cambio no alcanza a ningún dato existente. |
| D-172 | 2026-09-10 | **Trazabilidad (T4): bobina↔OP/pedido por el consumo real, cliente por búsqueda cuando no hay ficha propia.** `GET /coils/:id/consumptions` recorre `ProductionOrderConsumption` por `coilId` y sube por `productionOrder.reservation.salesOrder` — la punta que faltaba del enlace bidireccional bobina↔producción: el detalle de la OP ya lista sus bobinas montadas (`consumptions`, D-060), esto lista qué OP (y qué pedido detrás de ella) montaron una bobina dada, en una tarjeta nueva de `/bobinas/:id`. Sin página de detalle de cliente, "el link a este cliente" es `/clientes?search=<RUC/DNI>` — `clientes-view.tsx` lee `?search=` con `useSearchParams()` solo al montar (mismo patrón que `?pedido=` en despachos/comprobantes nuevos) y `customerSearchHref()` en `@/lib/utils` arma el href; se aplicó donde el nombre del cliente aparecía como texto plano en cotizaciones, pedidos, despachos, comprobantes y cobranzas. Dos DTO ganaron un campo para poder armar el link: `DispatchDto.customerId`/`customerDocNumber` y `PurchaseItemDto.coilId` (ambos ya se resolvían en el `include` de Prisma, solo faltaba devolverlos). Quedan afuera a propósito los renglones dentro de un `<Select>` de un formulario (elegir un pedido para despachar o facturar): ahí un clic ya significa "elegir esta fila", no "ir a verla", y convertirlo en link rompería la selección. | El RUC y no el nombre en `customerSearchHref`: es el dato exacto, sin la ambigüedad de dos clientes con nombres parecidos. Ir por `ProductionOrderConsumption` y no por `Reservation` para la trazabilidad bobina→pedido: una reserva `RAW_MATERIAL` apunta a un agregado color+espesor (D-134), no a una bobina física — la bobina concreta recién se decide al armar la OP, así que `Reservation.itemId` no sirve para esta pregunta y `ProductionOrderConsumption.coilId` sí. |
| D-173 | 2026-09-10 | **Reporte de bobinas en PDF (T6): al vuelo con `pdfkit`, sin persistir en R2.** Mismo criterio que la hoja de planta del pedido (D-149) y no el de la cotización (D-068): es un reporte interno y regenerable, no un documento que sale de la empresa, así que no hay nada que congelar. `GET /coils/:id/pdf` arma identificación, saldo, las órdenes de producción que la montaron (D-172) y el kardex completo de una bobina; `GET /coils/report-pdf` acepta los mismos filtros que `GET /coils` y arma una tabla del conjunto filtrado actual, topada a `MAX_PAGE_SIZE` (200) filas — el mismo techo que ya respeta cualquier otro listado (D-113) — con un aviso en el pie si el filtro trae más. Los dos se descargan con un `<a href="/api/coils/...">` directo, igual que el PDF de planta. `apps/api/src/coils/coil-pdf.ts` concentra los dos armados; la tabla genérica que comparten trunca cada celda a una línea con `ellipsis: true` en vez de dejar que un código sin espacios (no tiene dónde partirse) se escriba encima de la columna siguiente. | Persistir este PDF en R2 habría copiado la forma del PDF de cotización sin la razón que la sostiene: aquel se congela porque es lo que se le mandó al cliente (D-068), y una foto vieja de un reporte de bobinas guardada sería exactamente el riesgo que D-149 ya evitó con la hoja de planta — mejor armarlo con el saldo de hoy cada vez que se pide. El tope de filas en el reporte de lista no es una limitación nueva: es el mismo `MAX_PAGE_SIZE` que ya acota `GET /coils`, aplicado también a su PDF. |
| D-174 | 2026-09-10 | **Nombres de línea en la UI: decisión del dueño, no traducción literal.** `BUSINESS_LINE_LABELS` (`packages/shared/src/enums.ts`) cambia sus **valores**, nunca sus claves: `metallic-roofing` pasa de "Metallic Roofing" a "Coberturas Aluzinc", `roofing` de "Roofing (UPVC)" a "Coberturas (UPVC)", `trading` de "Trading" a "Reventa", `services` de "Services" a "Servicios"; `drywall` no cambia. El código (`BusinessLine`) y el `name` que trae la fila de `business_lines` en la base **no se tocan** — son identifiers y datos, no presentación —, y un censo por grep confirmó que `BUSINESS_LINE_LABELS` es el único punto de la app que renderiza el nombre de una línea (ningún componente tenía el string duplicado). | Cinco nombres de línea llevaban dos años en inglés en toda la UI —tablas, forms, selects, PDFs—, algo que ninguna sesión anterior había señalado porque nadie preguntó "¿en qué idioma tiene que estar esto?" hasta que T5 lo puso en la agenda. Que el mapa ya estuviera centralizado en un solo `Record` (y no repartido por componente) es lo que hizo el cambio de una sola línea por nombre; si hubiera habido strings sueltos, esta decisión habría sido "centralizar primero, renombrar después". |
| D-175 | 2026-09-10 | **Sidebar reagrupado (T5/M2): Comercial, Catálogo, Planta, Administración, sin "General".** "Inicio" pasa a llamarse "Panel" (también su `<h1>` y `metadata.title`) y queda sin grupo — `NavGroup.label` acepta `''`, que `app-sidebar.tsx` no pinta como encabezado —, a la cabeza del menú. "Márgenes" y "Tipo de cambio" (dos ítems, dos rutas) se funden en un solo ítem "Márgenes y tipo de cambio" bajo Administración; las dos rutas (`/configuracion/margenes`, `/configuracion/tipo-cambio`) siguen existiendo sin cambios — lo nuevo es `configuracion/layout.tsx`, una cabecera de pestañas (`Tabs` controlado por `usePathname()`/`router.push`) que las envuelve. `NavItem` gana `activePrefix` opcional para que ese ítem fusionado se marque activo en las dos rutas y no solo en la primera. Ningún `href` existente cambió de destino. | Es reordenar y renombrar, no mover rutas — la restricción que pidió el dueño explícitamente, para que nada que ya apunte a una pantalla (favoritos, otra sesión, un test) se rompa. `activePrefix` y no un `href` compartido a una ruta índice nueva: crear `/configuracion` como página propia para que el ítem fusionado apunte ahí habría sido la ruta nueva que la restricción prohíbe: más simple es que el ítem siga apuntando a una de las dos rutas reales y que el resaltado de "activo" mire un prefijo distinto del destino. |
| D-176 | 2026-09-10 | **Avance de la orden en la fila de bobina/fleje montado (T5/M3), sin ningún cálculo nuevo.** En `/planta`, la fila de cada bobina (coberturas) o fleje (drywall) montado gana una línea con datos que el DTO ya traía sin mostrarlos: en coberturas, `{order.reportedMeters} reportados · {consumption.consumedKg} consumidos`; en drywall, `{order.piecesReported} piezas salidas · {consumption.consumedKg} consumidos`. `reportedMeters`/`piecesReported` son agregados de **toda la orden** (los reportes no se parten por fleje en el modelo de datos); `consumedKg` sí es de esa asignación puntual (`ProductionOrderConsumption.consumedKg`) y ya vivía en el DTO, solo se usaba para decidir si el botón de liberar se apagaba, nunca se mostraba. | Mostrar un agregado de la orden dentro de la fila de un material puntual podría leerse como si fuera exclusivo de ese fleje o esa bobina, y por eso el texto no dice "de esta bobina": dice "reportados"/"salidas" a secas, en la misma línea que el dato que sí es de esa fila (`consumidos`), separados por el mismo `·` que el resto de la pantalla ya usa para yuxtaponer cifras de distinto alcance (ver, por ejemplo, la fila de bobina de D-172). Partir los reportes por fleje para que "reportados" fuera de verdad por fila habría sido la lógica nueva que T5 pidió evitar, y el dato que sí cambia por fila (`consumedKg`) ya alcanza para lo que el encargado necesita ver de un vistazo: cuánto rindió el material que tiene puesto ahora mismo. |
| D-177 | 2026-09-10 | **Sort de columna en las cuatro listas principales (T5/M4), client-side sobre lo ya cargado.** `apps/web/src/lib/use-sort.ts` (`useSort`, `compareBy`, `compareDecimalBy`) y `apps/web/src/components/sortable-table-head.tsx` son el mecanismo único; cotizaciones, pedidos y bobinas (paginadas server-side) ordenan solo la página actual, producción (sin paginar, tope de 500) ordena el conjunto entero. `key: null` es el estado inicial: sin clickear ninguna columna, la lista se ve exactamente en el orden descendente que ya manda el servidor (D-113/D-124), que esta decisión no toca. Columnas clave por vista: código, cliente, fecha/emisión y total/estado en cotizaciones y pedidos; código, disponible y estado en bobinas; código, creada y estado en producción. Dinero (`totalPen`, `availableKg`) compara por `Decimal.comparedTo`, nunca por `<`/`>` de string o `number` (regla dura 1). | Ordenar server-side habría exigido tocar el API de las tres listas paginadas — justo lo que esta sesión (corta, con presupuesto acotado) decidió no hacer: alcanza con lo que el usuario ya tiene en pantalla, y la única pérdida real es que ordenar por una columna no trae de otra página lo que quedó afuera, que es aceptable para una tabla que ya pagina por algo. Un solo hook y un solo componente para las cuatro vistas en vez de repetir el patrón por archivo es lo que evita que la próxima lista que necesite sort invente su propia variante. |
| D-178 | 2026-09-10 | **Cajas info largas → ícono ⓘ con popover (T5/M4).** `apps/web/src/components/ui/popover.tsx` (nuevo, `radix-ui`) y `apps/web/src/components/info-popover.tsx` envuelven el patrón; se aplicó a dos notas estáticas que ocupaban layout permanente — la de D-146 en "Plan de corte" (`roofing-order-panel.tsx`) y la de D-054 en "Reservas de material" (`pedido-detalle-view.tsx`). El criterio que separa un candidato de lo que se queda como estaba: texto **estático** que explica una regla del dominio, no un aviso condicional de negocio (advertencias de D-154, mensajes de validación) — esos siguen siempre visibles, porque ocultarlos detrás de un clic es exactamente el error que M2 no quería cometer. El botón es accesible por teclado y por foco sin nada adicional: lo hereda de Radix `Popover` (foco al abrir, `Escape` para cerrar). | Dos candidatos y no un barrido completo de la app: el brief de la sesión pedía un criterio uniforme antes de tocar la primera pantalla, y separar "texto informativo estático" de "aviso condicional" exige leer cada caso, no un grep — con el presupuesto de esta sesión (~30-45 min), dos candidatos ya identificados en el handoff de S10 alcanzan para fijar el patrón; el resto queda para quien retome el criterio, no para una sesión más. |
| D-179 | 2026-09-11 | **Escritorio compacto: el sistema se diseña para una sola clase de pantalla (S11, Fase 2).** Decisión de negocio del dueño, no técnica: el ERP se opera **solo en PC de escritorio**; el operario de planta no usa la app y el supervisor ingresa todo, incluida producción. De ahí salen tres cosas. (a) **No hay objetivo móvil ni tablet**: no se agregan media queries para pantallas chicas, y las que ya existen se conservan solo por lo que aportan entre 1366 y 1920 px. (b) **Densidad alta**: cabecera de 48→32 px, `main` de `gap-6 p-6` a `gap-3 p-4`, `h1` a 18 px, `h2` unificados a 14 px, `Label` a 12 px, celda de tabla `px-2.5 py-1.5` con encabezado `h-8` atenuado, `Badge` de 20→18 px, `Card` con `--card-spacing` de 12 px, y una tira única `StatStrip`/`Stat` que reemplaza el bloque de cuatro `Card` de una cifra copiado en cinco vistas. En las listas comerciales el documento del cliente pasa al lado del nombre en vez de debajo. Medido en un viewport de 950 px de alto: cotizaciones y pedidos 13.3 → 23.4 filas (+76 %), bobinas 15.5 → 23.1 (+49 %), producción 6.5 → 13.2 (+103 %). (c) **`/planta` deja de estar a escala táctil**: veinte controles `h-12`/`h-16 text-lg` vuelven al tamaño del resto de la app. La fuente, además, nunca había llegado a cargar: `--font-sans` se autorreferenciaba en `@theme inline` y las variables de `next/font` se aplicaban en `<body>` en vez de `<html>`, así que toda la app se renderizaba en Times New Roman (verificado con `getComputedStyle`). | Densificar sin decidir antes para qué pantalla es el sistema habría sido elegir por corazonada. Con "solo escritorio" fijado, cada número tiene un porqué: el operario con guantes era el único argumento para los controles de 64 px de `/planta`, y sin él compiten por el alto de pantalla que el supervisor necesita para ver la cola entera. El objetivo se puso **medible** (≥50 % más filas por pantalla) justamente para no confundir "se ve más apretado" con "entra más": tres de las cuatro listas lo superan cómodo y bobinas queda en +49 %, que es lo que había para ganar ahí — sus filas ya eran de una sola línea, así que no tenía la grasa que tenían las otras. Lo que **no** se hizo: bajar el tamaño del texto de datos (sigue en 14 px) ni apretar el alto de fila por debajo de `py-1.5`; la densidad sale de sacar andamiaje —cromo de página, tarjetas de una cifra, líneas de más en una celda—, no de achicar el dato. |
| D-180 | 2026-09-11 | **Paleta sobria de escritorio y cuatro tonos de estado de dominio (S11, Fase 3).** Un solo color de marca —azul acero `oklch(0.46 0.105 248)`, con su variante clara para oscuro— sobre una escala de neutros que no cambia; se usa en la acción primaria, el anillo de foco, los links y el estado "en curso", nunca de adorno. Los estados de dominio dejan de elegir su `variant` de `Badge` en cada vista y pasan por `apps/web/src/components/status-tone.ts`: `progress` (vivo, esperando a alguien), `done` (terminó bien), `warning` (hace falta mirarlo, no es error) y `destructive` (error de verdad), más neutro (`outline`) para lo anulado o revertido. Los tokens (`--tone-*`) viven en `globals.css` con las dos versiones de tema; las variantes `progress`/`done`/`warning` se agregan a `Badge`. Los mapas son `Record<Status, StatusTone>` exhaustivos, así que un estado nuevo en `@ayr/shared` no compila hasta que alguien decida qué significa. **Rojo y ámbar quedan reservados para error y aviso**: el único uso decorativo que quedaba —«Vencido S/ 0.00» en rojo fijo en `/cobranzas`— ahora se pinta solo cuando el vencido es mayor que cero (comparado con `Decimal`, regla dura 1). Contrastes medidos en el navegador, claro y oscuro: ninguno baja de 4.7:1. | El problema no era que faltara color, era que el mismo color quería decir cosas distintas: el negro sólido era «Aceptada» en comprobantes (terminó bien), «En producción» en pedidos (sigue en curso) y «Activa» en reservas, mientras «Atendido» —el terminal bueno de un pedido— era el gris más apagado de la paleta. Aprender la convención era imposible porque no había una, y arreglarlo vista por vista la habría dejado igual de frágil. Por eso el mapa es exhaustivo y no tiene `default`: un `default` silencioso es exactamente lo que dejó entrar el desorden. Un estado anulado se pinta neutro y no rojo a propósito — es una decisión tomada, no un problema, y teñir de rojo cada cotización anulada convierte el rojo en ruido justo donde tiene que gritar. |
| D-181 | 2026-09-11 | **El guard de reset de la base de pruebas identifica la rama `ci` por el endpoint de cómputo, verificado, no por el branch id.** `apps/api/prisma/reset-test-db.ts` compara el hostname de `DATABASE_URL` contra `ep-dry-butterfly-` para la rama Neon `ci`. El valor anterior, `ep-misty-band-`, copiaba el **branch id** de `ci` (`br-misty-band-ae9s41t7`) asumiendo que coincidiría con el hostname del endpoint de cómputo; son dos identificadores aleatorios independientes en Neon y nunca coincidieron. El guard, escrito en la sesión Saneamiento E2E (`eb1f15d`), nunca había corrido contra la rama `ci` real hasta la Ventana V-2 (2026-09-11): local prueba contra Docker, así que el primer contacto con Neon fue esa corrida de CI, y falló al primer intento con «Reset bloqueado». | Verificar el prefijo con `neonctl branches get ci --output json` (sin imprimir la cadena de conexión completa, regla dura 5) en vez de asumirlo por el nombre de la rama es lo que habría evitado el error desde el principio. Si el endpoint de `ci` vuelve a cambiar —Neon puede recrear el compute de una rama— el guard se rompe de la misma forma: el valor sale de ese mismo comando, nunca de una cadena de conexión pegada a mano. |

---

### 0.3 Alcance de esta versión (v1 "lista para cliente")

Incluye: auth/roles, bobinas (alta manual/XML/planilla, partido, merma, cierre), producción drywall y coberturas, corte tercerizado, catálogo e inventario valorizado por línea, cotizaciones/ventas con comprobante electrónico (Nubefact sandbox), importación de comprobantes, auditoría inmutable, terminal de planta.
Excluye: app móvil nativa, contabilidad general (asientos), planillas de personal, integración bancaria.

---

## 1. Propósito y alcance

### 1.1 Qué es

AYR Steel ERP es una aplicación web interna de gestión para una empresa peruana de **transformación y comercialización de acero**. Cubre el ciclo desde la compra de materia prima (bobinas de acero) hasta la venta del producto terminado, incluyendo compra y venta de producto terminado y de productos de terceros.

Gestiona: compras afines al rubro (bobina, producto terminado, producto de terceros) y compras que son gastos a crédito, notas de débito, etc.; producción; ventas con facturación electrónica, guías y notas de crédito; y una vista por rol coherente con sus funcionalidades.

### 1.2 Qué cubre

- Registro de materia prima (bobinas) por alta manual, por XML de factura de compra y por importación masiva desde planilla.
- Transformación de bobina a producto terminado en dos líneas de producción físicas distintas (perfilería drywall y coberturas metálicas).

---

## 2. Contexto de negocio

### 2.1 El rubro

La empresa compra **bobinas de acero** (rollos planos, en inglés _coils_) y las transforma. Una bobina se caracteriza por su peso (kg), su ancho (mm), su espesor (mm) y su **acabado** —el recubrimiento superficial—, y se compra por peso a un precio por kilogramo.

De la bobina salen dos caminos productivos físicamente distintos:

- **Corte longitudinal (slitting):** la bobina se parte a lo largo en tiras de menor ancho llamadas **flejes**. Los flejes alimentan una perfiladora que produce perfiles de drywall.

- **Conformado:** la bobina pasa por una máquina conformadora que le da perfil ondulado o trapezoidal y produce coberturas y planchas metálicas.

Además la empresa revende productos que no fabrica.

### 2.2 Las cinco líneas de negocio

| **Línea**        | **Identificador** | **Modelo**     | **Materia prima**       |
| ---------------- | ----------------- | -------------- | ----------------------- |
| Drywall          | drywall           | Transformación | Bobina → fleje → perfil |
| Metallic Roofing | metallic-roofing  | Transformación | Bobina → conformado     |
| Roofing (UPVC)   | roofing           | Compra-venta   | Producto terminado      |
| Trading          | trading           | Compra-venta   | Producto de terceros    |
| Services         | services          | Sin stock      | N/A                     |

La línea services es deliberadamente una operación nula sobre inventario: su estrategia de inventario es `noop` (no crea movimientos de kardex).

---

## 3. Arquitectura técnica

### 3.1 Estructura del repositorio

```
ayr-steel-erp/
  apps/
    api/          NestJS 11 + Prisma + pg-boss      → Cloud Run
    web/          Next.js 15 (App Router) + shadcn  → Vercel
  packages/
    shared/       Zod schemas, tipos, enums, utilidades Decimal
  docs/
    ARQUITECTURA.md   (este archivo)
    DECISIONES.md     (espejo de §0.2, formato ADR largo cuando haga falta)
    PROGRESO.md       (estado por fase, qué falta, bloqueos)
    handoff/          (resúmenes de cierre de sesión)
  .claude/        settings.json, agents/, commands/
  .mcp.json       playwright (headless), context7
  CLAUDE.md       reglas operativas del agente
```

### 3.2 Módulos del API (NestJS)

`auth` · `users` · `business-lines` · `finishes` (acabados) · `coils` (bobinas, partidos, mermas, cierre) · `cutting` (flejes, corte tercerizado) · `production` (recetas, órdenes de producción, `/planta`; coberturas y cola en Fase 5b) · `catalog` (productos por línea) · `inventory` (kardex, movimientos, valorización) · `customers` / `suppliers` (maestros, RF-80..85) · `purchases` (compras, recepción, pagos) · `sales` (cotizaciones, pedidos y ledger de reservas; Fase 5a) · `invoicing` (comprobantes electrónicos, guía de remisión, despacho y cobranza tras el puerto `ElectronicInvoicingProvider`; Fase 5b, D-071) · `imports` (planillas, XML) · `documents` (R2) · `pricing` · `exchange-rates` · `audit` · `jobs` (pg-boss).

Regla transversal: **todo cambio de stock pasa por `inventory`** como movimiento de kardex inmutable; los módulos nunca escriben stock directamente.

Segunda regla transversal (D-066, Fase 5a): **`inventory` es también el único punto donde se comprueba `disponible ≥ reservado`** para las salidas. Lo que compromete material **sin** mover kardex —envío a corte (D-050), fleje asignado a una OP (D-060), reserva de un pedido (D-054)— no lo puede ver esa comprobación, y por eso cada uno de esos tres mecanismos tiene además su guardrail explícito, aplicado a mano en todas las rutas que tocan el ítem.

### 3.3 Modelo de datos — principios

- Toda entidad con stock lleva `businessLineId`. `services` no tiene stock (estrategia `noop`).
- Bobina: `weightKg`, `widthMm`, `thicknessMm`, `finishId`, `currency`, `exchangeRate`, `unitCostPerKg`, `typeKey` (acabado+espesor), `code` (proveedor-acabado-espesor-peso-correlativo). Partido = bobina hija con `parentCoilId`.
- Kardex: tabla `inventory_movements` append-only (`type`, `qty`, `unitCost`, `refType`, `refId`, `reversalOfId`). Anulaciones = movimiento inverso, nunca delete.
- Auditoría: `audit_log` append-only con `actorId`, `action`, `entity`, `entityId`, `before`, `after`, `at`.
- Todos los `Decimal` con escala explícita: dinero 4, kg 3, mm 2.

### 3.4 Roles (RF-02)

| Rol               | Alcance                                                                |
| ----------------- | ---------------------------------------------------------------------- |
| ADMINISTRADOR     | Todo, incl. usuarios, catálogos, anulaciones, auditoría                |
| SUPERVISOR_PLANTA | Bobinas, producción, corte tercerizado, inventario, terminal `/planta` |
| VENDEDOR          | Cotizaciones, ventas, catálogo (lectura), inventario (lectura)         |

### 3.5 Integraciones

| Servicio      | Uso                              | Credencial                       |
| ------------- | -------------------------------- | -------------------------------- |
| Nubefact      | Emisión factura/boleta/NC/ND/GRE | `NUBEFACT_URL`, `NUBEFACT_TOKEN` |
| Cloudflare R2 | Archivos                         | `R2_*`                           |
| Neon          | DB + ramas                       | `DATABASE_URL`, `DIRECT_URL`     |
| UptimeRobot   | Monitores `/health` y web        | `UPTIMEROBOT_API_KEY`            |

### 3.6 Entornos

| Entorno | API                         | Web            | DB                                 |
| ------- | --------------------------- | -------------- | ---------------------------------- |
| local   | `pnpm dev` (localhost:3000) | localhost:3001 | Neon rama `dev`                    |
| ci      | GitHub Actions              | build          | Neon rama `ci` (reset por corrida) |
| prod    | Cloud Run                   | Vercel         | Neon `main`                        |

### 3.7 Fases de construcción

| Fase | Entrega                                                                                                                                                      | Cierre (`/goal`)                                                                          |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------- |
| 0    | Bootstrap: monorepo, CLAUDE.md, CI, auth, deploy vacío, monitores                                                                                            | Login E2E verde en prod, CI verde                                                         |
| 1    | Maestros: líneas, acabados, catálogo, clientes, proveedores, márgenes, tipo de cambio (RF-25, RF-50, RF-80..94) + importación planilla (RF-52)               | RF-25, RF-50, RF-52 E2E                                                                   |
| 2a   | Kardex base (`inventory`) + compras (D-030) + alta de bobinas por 3 vías (RF-10, RF-11, RF-12, RF-13, RF-14)                                                 | E2E compra→recepción→pago parcial; alta de bobina manual/XML/planilla con kardex correcto |
| 2b   | Bobinas: partido (RF-15, RF-16), merma (RF-17, RF-18), cierre (RF-19), edición (RF-20), anulación (RF-21, RF-22), inventario por línea (RF-23)               | E2E partido→reversa, merma→anulación, cierre bloquea producción                           |
| 3    | Corte tercerizado + flejes (RF-40..42, RF-22, D-049/D-050)                                                                                                   | E2E envío/recepción/prorrateo                                                             |
| 3b   | Reversa de recepción de corte tercerizado (D-051), simétrica a RF-16                                                                                         | E2E reversa→saldo original→cancelar→anular; reversa bloqueada con fleje consumido         |
| 4    | Producción drywall + terminal `/planta` (RF-32, RF-34, RF-35, RF-39, D-047, D-055..D-060). RF-38 va con las cotizaciones, en Fase 5                          | E2E corrida y anulación de drywall                                                        |
| 5a   | Cotización → confirmación → pedido + reserva (RF-61, RF-62, RF-65, RF-66, RF-69, RF-80..85; D-064..D-069)                                                    | E2E cotizar→confirmar→reserva→OP consume→anular; invariante bloquea merma/corte           |
| 5b   | Facturación electrónica (factura, boleta, NC), GRE remitente, despacho y cobranza (RF-70, RF-74..RF-79, RF-86..RF-89; D-070..D-080)                          | E2E pedido→despacho→GRE→factura aceptada→cobranza→saldo 0, y sus reversas                 |
| 6    | Producción de coberturas metálicas contra pedido + maestro de colores (RF-30..RF-33, RF-36; D-082..D-091)                                                    | E2E cotización con subítems→OP→bobina filtrada→cerrar con merma→despacho→factura          |
| 7    | Cola de producción (RF-37, RF-38), punto de venta de mostrador (RF-60) e importación de comprobantes ya emitidos (RF-71, RF-72; D-105..D-109) — **completa** | Reimportación que archiva la versión anterior                                             |
| 8    | Auditoría, reportes, hardening, UAT (RF-90..96)                                                                                                              | Checklist cliente                                                                         |

> D-034 (2026-09-03): reordenado desde el plan de `docs/handoff/fase-0.md`. Fase 1 crece para incluir clientes, proveedores, precios y tipo de cambio; Fase 2 absorbe el módulo de compras porque comparte kardex y cuentas por pagar con bobinas.
> D-041 (2026-09-03): la Fase 2 se parte en 2a y 2b. 2b depende de 2a (necesita el kardex y bobinas ya dadas de alta); las fases 3 en adelante no cambian.
> D-048 (2026-09-02): Fase 4 y 5 se reordenan por RF-31 (producción de coberturas exige cotización, que es de Fase 5). Fase 4 queda solo con drywall (sin esa dependencia) + terminal `/planta`; Fase 5 absorbe cotizaciones, `production_orders`, producción de coberturas y ventas.
> D-065 (2026-09-03): la Fase 5 se parte en **5a** y **5b**. 5a entrega el ciclo comercial hasta la reserva (el ledger de D-054, del que cuelga todo lo demás) y deja construidas sus tres reversas; 5b sigue con producción de coberturas, venta con descuento de stock, despacho y cobranza. El corte va justo ahí porque la reserva es la pieza de la que dependen tanto la producción de coberturas (RF-31) como la venta (RF-64), y construirla junto con cualquiera de las dos habría mezclado dos guardrails transversales en una sola sesión.
> Fase 4 (2026-09-03): **RF-38 pasa a Fase 5** por la misma dependencia. El indicador del menú cuenta "cotizaciones que esperan producción", y en Fase 4 todavía no existen cotizaciones: sale junto con RF-37 (la cola) en la fase que las crea. RF-32 (no mezclar acabados en una corrida) sí queda cubierto para drywall: la receta (D-059) fija acabado, espesor y ancho, y la OP rechaza cualquier fleje que no coincida.
> D-070 (2026-09-04): **la Fase 5b se reasigna** a facturación electrónica, guía de remisión, despacho y cobranza, y aparece una **5c** con lo que 5b traía. El motivo está en D-070: el pedido de 5a no tenía forma de salir del almacén, de facturarse ni de cobrarse, y ese tramo vale más que un tipo de producto más. La Fase 6 original queda absorbida —la emisión Nubefact es de 5b— y se reduce a la importación de comprobantes ya emitidos (RF-71, RF-72).
> D-082 (2026-09-04): **§3.7 se renumera.** La `5c` que D-070 había creado pasa a ser la **Fase 6** (el dueño abrió la sesión llamándola así), la importación de comprobantes pasa a **7** y auditoría/reportes/UAT a **8**. La Fase 6 entrega solo producción de coberturas y el maestro de colores: la cola de producción (RF-37, RF-38) y el punto de venta directo (RF-60) salen de su alcance y se suman a la 7, porque no dependen de coberturas y sí de decisiones de UI que esta fase no toca. RF-73 (venta directa de bobina) ya lo cubre D-037 desde Fase 2a.
> D-098..D-104 (2026-09-05): **segundo tramo de la Fase 7 — el punto de venta de mostrador (RF-60)**. Queda en la fila de la Fase 7, sin construir, solo la importación de comprobantes ya emitidos (RF-11, RF-71, RF-72). El POS reusa los servicios de 5a y 5b dentro de una transacción en vez de abrir un camino propio (D-099), agrega el turno de caja con arqueo (D-101) y la modalidad de traslado `PICKUP` (D-103), que le sirve a cualquier recojo en tienda.
>
> D-105..D-109 (2026-09-05): **tercer tramo de la Fase 7 — importación de comprobantes ya emitidos (RF-71, RF-72), y con él la fila queda completa.** RF-11 (alta de bobina desde el XML del proveedor) ya estaba entregado desde la Fase 2a y aparecía en esta fila por arrastre: lo que faltaba eran los otros dos. El comprobante importado entra a la misma tabla con `origin = IMPORTED` (D-105), no habla nunca con el PSE, y reimportarlo archiva la versión anterior en vez de pisarla (D-108). El importador genérico de RF-52 aprende a agrupar N filas en una entidad (D-107), que es lo que cualquier importación futura de documentos con líneas va a necesitar.
>
> D-092..D-096 (2026-09-04): **la Fase 7 se ataca en dos tramos; esta sesión solo construye el primero.** El dueño abrió la sesión pidiendo únicamente la cola de producción (RF-37, RF-38) sobre coberturas contra pedido. El punto de venta directo (RF-60) y la importación de comprobantes (RF-11, RF-71, RF-72) siguen en la fila de la Fase 7 sin construir, para una sesión futura con ese mismo número — no ameritan una fase propia porque ninguno depende de la cola ni la cola de ellos.

---

## 4. Requisitos funcionales

Cada requisito está trazado a la ruta, el callable o el módulo que lo implementa. Si un requisito no tiene una referencia de código al lado, no está en esta lista.

### 4.1 Autenticación y usuarios

| **#** | **Requisito**                                                                      |
| ----- | ---------------------------------------------------------------------------------- |
| RF-01 | El usuario inicia sesión con correo y contraseña.                                  |
| RF-02 | Cada usuario tiene exactamente un rol: ADMINISTRADOR, SUPERVISOR PLANTA, VENDEDOR. |
| RF-03 | Bajar el rol de un usuario o desactivarlo invalida sus sesiones abiertas.          |
| RF-04 | Un administrador gestiona el alta, edición y baja de usuarios.                     |

### 4.2 Materia prima (bobinas)

| **#** | **Requisito**                                                                                                                                                                                                                                                                                                                                               |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RF-10 | Alta individual de bobina con datos de factura de compra.                                                                                                                                                                                                                                                                                                   |
| RF-11 | Alta de bobina a partir del XML de la factura electrónica del proveedor.                                                                                                                                                                                                                                                                                    |
| RF-12 | Alta masiva desde planilla, con revisión previa fila por fila. **Retirado con D-150** (2026-09-08): el importador de planilla se eliminó junto con el resto del módulo. Una bobina se da de alta por compra, por XML de factura o a mano.                                                                                                                   |
| RF-13 | El identificador de bobina se genera con formato compuesto proveedor-acabado-espesor-peso-correlativo.                                                                                                                                                                                                                                                      |
| RF-14 | Cada bobina lleva una clave de tipo que agrupa por acabado y espesor, ignorando el ancho.                                                                                                                                                                                                                                                                   |
| RF-15 | Partir una bobina en hijas por ancho, conservando trazabilidad a la madre.                                                                                                                                                                                                                                                                                  |
| RF-16 | Revertir un partido, devolviendo peso y ancho a la madre.                                                                                                                                                                                                                                                                                                   |
| RF-17 | Registrar merma sobre una bobina. Sigue siendo un acto **propio** y distinto del ajuste de cierre de D-164: cubre la pérdida puntual durante la vida del rollo (borde oxidado, empalme fallado) y se anula por RF-18, mientras que el ajuste del cierre se deshace reabriendo la bobina.                                                                    |
| RF-18 | Anular una merma mal registrada.                                                                                                                                                                                                                                                                                                                            |
| RF-19 | Abrir y cerrar una bobina; una bobina cerrada no entra a producción. **D-164:** cerrar exige declarar cuántos kilos quedan de verdad y **liquida la diferencia** contra el saldo teórico como movimiento `CLOSE_ADJUSTMENT` en la misma transacción (`OUT` si sobra saldo, `IN` si el conteo da de más), con motivo y `operationDate`; reabrir lo revierte. |
| RF-20 | Editar los datos de una bobina, incluida su moneda y tipo de cambio.                                                                                                                                                                                                                                                                                        |
| RF-21 | Anular una bobina solo si no tiene ningún movimiento.                                                                                                                                                                                                                                                                                                       |
| RF-22 | Cancelar el plan de corte de una bobina. **Fase 3** (D-044): depende del plan de corte de RF-40..42.                                                                                                                                                                                                                                                        |
| RF-23 | Consultar inventario de bobinas separado por línea de negocio.                                                                                                                                                                                                                                                                                              |
| RF-25 | Gestionar el catálogo de acabados con su factor de densidad. **D-165:** el factor que se tipea es el **físico** (acero ≈ 7.85); la merma normal del 1 % la agrega el código (`standardDensityFactor`) y nunca se carga a mano en el maestro.                                                                                                                |

### 4.3 Producción

| **#** | **Requisito**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RF-30 | Producir cobertura o plancha consumiendo una o varias bobinas a la vez. **Fase 6** (D-082..D-090): `production` rama `ROOFING`, `/planta`. Una OP admite hasta `MAX_ORDER_STRIPS` bobinas montadas a la vez, igual que drywall con sus flejes. **D-146:** reportar largos tiene desde ahora un tope duro además del material montado — el acumulado de los reportes vigentes de la orden nunca supera los `Σ cantidad × largo` de su plan de corte, sin tolerancia; producir más exige ajustar antes el plan (`PUT /production/roofing/:id/plan`). Cada reporte admite además un `consumedKg` **opcional** que se guarda como dato de planta y no mueve kardex.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| RF-31 | Toda producción de coberturas debe ir contra una cotización; no se admite producción suelta. **Fase 6** (D-084): `POST /production/roofing` exige `reservationId`, así que la regla vive en el schema del endpoint y no en un chequeo que alguien pueda olvidar. `business_lines.quotation_required` (D-065) ya cierra el otro extremo: en `metallic-roofing` no hay pedido directo. **D-171 revirtió la excepción de D-140:** una **plancha de catálogo** también se fabrica contra el pedido y reserva materia prima (`isMadeToOrder`), y **producir coberturas a stock dejó de existir** — `POST /production/roofing` sin `reservationId` responde 400. Ninguna cobertura tiene camino a stock: la plancha porque no es stock terminado, la de a medida porque sin pedido no hay largo que fabricar. **D-148:** `POST /production/roofing/from-sales-order/:id` crea, en una transacción, la OP de cada línea del pedido que todavía no la tiene. No es un camino nuevo —cada orden nace de su reserva como siempre—, solo impide que un pedido de varias líneas quede a medio encolar. **D-154:** el faltante de materia prima del agregado deja de bloquear en producción —montar, reportar y cerrar avisan con el pedido en riesgo y los kilos que faltan, y siguen adelante— y la promesa del **propio pedido**, incluidas sus otras líneas, no cuenta nunca en contra. El tope de **metros** del plan (D-146) sigue siendo duro; el del **kg declarado** pasó a aviso de desviación. |
| RF-32 | Una misma corrida no puede mezclar bobinas de acabados distintos. **Fase 6** para coberturas (D-086): el filtro exige espesor dentro de tolerancia y `colorId` igual al del producto, y la OP rechaza toda bobina que no lo cumpla. Cubierto para drywall desde Fase 4 por la receta (D-059).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| RF-33 | Anular una producción de coberturas devuelve a cada bobina el peso exacto que consumió. **Fase 6** (D-088, D-089): revertir un reporte de largos devuelve al kardex de la bobina los kilos teóricos que salieron y restaura la reserva; reabrir la OP deshace la merma de despunte y el ajuste de costo; anular la OP —solo sin reportes vigentes— libera las bobinas montadas sin tocar el kardex, porque montar no lo movió (D-060).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| RF-34 | Producir perfiles de drywall desde fleje. **Fase 4** (D-055..D-060): `production`, `/planta`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| RF-35 | Revertir una producción de drywall. **Fase 4**: revertir un reporte de piezas, reabrir una OP cerrada y anular la OP (D-060).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| RF-36 | No se puede anular una producción si el producto resultante ya tiene una venta cerrada posterior. **Fase 4** para drywall y **Fase 6** para coberturas: el guardrail es el mismo y mira el kardex, no la venta — si después del ingreso del producto hay cualquier movimiento vivo que no sea otra entrada (una salida por despacho, una merma, un ajuste de costo de otro cierre), la reversa falla completa.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| RF-37 | La cola muestra los pedidos confirmados de coberturas contra pedido pendientes de fabricar, con su avance. **Fase 7** (D-082, D-092..D-096): vista derivada de `Reservation` + `ProductionOrder` (sin tabla ni estado nuevo), ordenada por prioridad manual, semáforo de fecha prometida y FIFO por confirmación; `/planta` la usa como pantalla de entrada y `/produccion` la expone en vista de administración con edición de prioridad y fecha.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| RF-38 | Un indicador en el menú lateral muestra cuántos pedidos esperan producción. **Fase 7** (D-082, D-093): cuenta las filas de la cola de RF-37.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| RF-39 | Terminal simplificada para el operario de planta. **Fase 4** (drywall) y **Fase 6** (rama de coberturas de `/planta`). **D-146/D-147:** la tarjeta de reporte de coberturas muestra ML del plan, ML reportado, ML restante y kg teórico antes de capturar, y `/planta/tanda` permite transcribir la hoja del turno entera: una fila por orden abierta, los ML nuevos y los kg opcionales, guardada como una sola transacción todo o nada con el error de cada fila cuando alguna no valida. **D-155:** la tanda se reconvierte en `/planta/producir`, el espacio de producción del pedido: una pestaña por orden con su estado (sin bobina / lista / reportada), la bobina se monta y se cambia **desde la misma pestaña**, y cada orden se guarda por separado en vez de todo o nada. `/planta/tanda` queda solo como redirección. La terminal (`/planta`) sigue siendo para operar **una** orden suelta. **D-159/D-160:** `/planta` es la **única** entrada a producir — filtro por pedido o todas las órdenes abiertas, las dos clases de orden en el mismo selector, el plan de corte editable desde el panel, el reporte por líneas que se autocompleta con el plan faltante al montar la bobina, el selector de bobina como modal de búsqueda, y **Guardar** / **Guardar y cerrar** (reporte + cierre + liberación de la bobina en una transacción). `/planta/producir` y `/planta/tanda` quedan como redirecciones.                                                                   |

### 4.4 Corte tercerizado

| **#** | **Requisito**                                                   |
| ----- | --------------------------------------------------------------- |
| RF-40 | Enviar bobinas a un tercero para corte, con plan de anchos.     |
| RF-41 | Recibir los flejes y prorratear el costo del servicio por peso. |
| RF-42 | Consultar el stock de flejes por ancho.                         |

### 4.5 Catálogo e inventario

| **#** | **Requisito**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RF-50 | Cada línea tiene su catálogo propio de productos. **D-166:** el largo fijo de una plancha va en **milímetros** y tiene que estar entre 0.1 y 20 m; el diálogo lo traduce a metros mientras se tipea. **D-171:** ese mismo largo decide, además del precio (D-161), cuántos metros de bobina consume cada plancha. **D-167:** un producto de una línea `NOOP` —un servicio— se cotiza y se vende como cualquier otro; lo único que no hace es prometer existencias.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| RF-51 | Cada línea con stock tiene su vista de inventario valorizado. **Fase 5a** le suma las columnas reservado y disponible (D-066).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| RF-52 | Importación masiva de catálogo desde planilla, con edición fila por fila. **Retirado con D-150** (2026-09-08): el importador genérico —con su ciclo de lote, su previsualización fila por fila y sus cinco adaptadores— se eliminó entero. Sobrevive `parse-spreadsheet.ts` (lectura de planilla y de celdas por encabezado), que es la pieza que cualquier importador futuro vuelve a necesitar. La carga histórica entra por el flujo real. **D-152:** la única importación masiva que queda es la de **cotizaciones** (`/cotizaciones/importar`), y no es un importador genérico: lee el export de ventas del negocio y crea cotizaciones en borrador por el alta normal, sin crear ningún maestro. **D-156:** el preview del importador se agrupa en un acordeón por comprobante y ningún campo obligatorio es un callejón — un cliente o un SKU que falta se da de alta desde la propia fila, con el formulario completo del maestro y sin perder lo revisado, y los desplegables largos pasan a un modal de búsqueda con filtro. **D-157/D-158:** el importador crea las cotizaciones **sin fecha de vencimiento**, el cliente se elige una vez por comprobante en la cabecera del acordeón, y un RUC/DNI que no está en el maestro se da de alta desde el padrón —visible en el preview antes de confirmar, con los datos que devuelve SUNAT y nunca con los del archivo—. |
| RF-53 | Consultar el kardex de un producto o de una bobina.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| RF-54 | Maestro de colores con muestra visual, y color estructurado en el producto de cobertura y en la bobina. **Fase 6** (D-085): `colors` (CRUD de ADMINISTRADOR, baja lógica), `products.color_id` y `coils.color_id`. El SKU refleja el color para leerlo a simple vista; el filtrado y el emparejamiento son siempre por `colorId`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |

### 4.6 Ventas y cotizaciones

| **#** | **Requisito**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RF-60 | Emitir una venta desde el punto de venta interno. **Fase 7b** (D-098..D-104): `/pos` crea pedido + despacho + comprobante + cobro en una transacción reusando los servicios de 5a y 5b; solo stock del propio producto, contado y entrega inmediata, con turno de caja y arqueo. `/pos/caja` para abrir, cerrar y anular la venta del turno.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| RF-61 | Emitir una cotización. **Fase 5a** (D-064..D-069): `sales`, `/cotizaciones`. **D-161:** un SKU `PLANCHA` con largo se cotiza **por metro lineal** (`valuePerMeterPen`), con el largo bloqueado del SKU y solo la cantidad editable; la línea sigue en `NIU`. **D-162:** se tipea el **precio con IGV** y el sistema deriva el **valor** sin IGV, que es lo que se guarda. **D-163:** ninguna línea se guarda por debajo de `costo promedio ÷ (1 − margen mínimo) × 1.18`, para todos los roles. **D-167:** una línea de negocio sin inventario (un servicio) se cotiza igual que cualquier otra, sin reserva y sin piso. **D-169:** la línea que trae el importador de comprobantes (D-152) persiste el **importe del papel** y no su recálculo; el ajuste de redondeo se deriva y se muestra. **D-171:** una línea de plancha reserva materia prima, no producto terminado. |
| RF-62 | Confirmar una cotización para producción. **Fase 5a**: confirmar crea pedido + reserva en una transacción (D-054, D-066).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| RF-63 | Registrar que el cliente aceptó una cotización y con ello mandarla a producción. **Fase 5a** cubre el pedido y la reserva; la OP de coberturas es **Fase 6** (D-026: orden separada, `production_orders.reservation_id`; D-084: la OP la exige para poder nacer). **D-149:** el pedido baja al taller como **hoja de planta** (`GET /sales/orders/:id/pdf-planta`): producto, metros a producir, largos y medidas por ítem, sin ningún importe; se arma al vuelo y no se guarda.                                                                                                                                                                                                                                                                                                                                                                                             |
| RF-64 | Convertir una cotización en venta, descontando stock. **Fase 5b** (D-074): el despacho es lo que mueve el kardex y cierra el pedido; la factura no descuenta stock.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| RF-65 | Cancelar una cotización. **Fase 5a**: cualquier estado no confirmado; una confirmada exige anular antes su pedido.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| RF-66 | Editar una cotización propia que no tenga producción viva. **Fase 5a**: mientras siga en borrador.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| RF-67 | Anular una venta, con cascada sobre su cotización gemela y reversa de stock.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| RF-68 | Listar ventas con búsqueda, filtros y totales agregados del conjunto filtrado.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| RF-69 | Listar cotizaciones por separado de las ventas. **Fase 5a**: `/cotizaciones` y `/pedidos` son listas distintas.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| RF-71 | Importar comprobantes ya emitidos desde planilla, incluidas notas de crédito. **Fase 7** (D-105..D-107, D-109): entidad `FISCAL_DOCUMENTS` del importador de RF-52, una fila por línea; el comprobante nace `ACCEPTED` con `origin = IMPORTED`, con su cuenta por cobrar y sin contacto con el PSE. Uno que no debió entrar se **anula por dentro** (Sesión M-4, D-110): estado `ANNULLED`, sin SUNAT, y su cuenta por cobrar desaparece. **Retirado con D-150** (2026-09-08): la entrada de comprobantes ya emitidos se eliminó con el módulo de importaciones. Lo único que queda del ciclo es `POST /invoicing/documents/:id/annul` (D-110), que no da de alta nada: es el remedio de las filas que ya entraron.                                                                                                                                                          |
| RF-72 | Reimportar un comprobante ya importado archiva la versión anterior en vez de pisarla. **Fase 7** (D-108): `archived_at` + `supersedes_document_id`, unicidad parcial de `number`, y tres puertas cerradas (lo emitido acá, lo que tiene cobros vigentes y lo que tiene notas de crédito vivas no se reimporta). **Retirado con D-150** (2026-09-08): sin reimportación no hay versión que archivar. `archived_at`, `supersedes_document_id` y la unicidad parcial de `number` **se conservan en la base** —describen filas que existen— pero ya no las escribe nadie.                                                                                                                                                                                                                                                                                                        |
| RF-73 | Venta directa de bobina (sin transformar). **Fase 2a** (D-037): la bobina se vende como producto de la línea `trading`, y la línea de venta reserva los kilos de la bobina concreta (D-066). **D-116:** siempre el saldo vivo completo, nunca una fracción. **D-168:** el SKU `BOB…` lo calcula una sola función — con un acabado con guiones adentro las dos que había divergían y la venta respondía «no existe el producto de venta directa». **D-170:** el despacho **cierra** el rollo que quedó en cero y sin reservas, y la reversa lo reabre.                                                                                                                                                                                                                                                                                                                        |
| RF-70 | Emitir comprobante electrónico (factura, boleta, nota de crédito) desde un pedido o como venta directa, con serie y correlativo del punto de emisión. **Fase 5b** (D-070..D-073): `invoicing`, `/comprobantes`. **D-153:** el borrador tiene desde ahora **dos terminales excluyentes** — `send` lo emite por el PSE, `register-manual` lo cierra con la serie y el correlativo del comprobante que la empresa emitió en otra app mientras dura la migración (`origin = MANUAL`, nace `ACCEPTED`, sin CDR ni XML y sin tocar `fiscal_series`). Los dos parten del mismo borrador, así que las validaciones son las mismas.                                                                                                                                                                                                                                                   |
| RF-74 | Corregir un comprobante rechazado por el PSE y reemitirlo con un correlativo nuevo; el rechazado queda en el historial. **Fase 5b** (D-072).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| RF-75 | Anular un comprobante aceptado por comunicación de baja dentro del plazo, o por nota de crédito fuera de él. **Fase 5b** (D-072): el módulo documenta qué camino aplica según tipo y plazo. **D-153:** un comprobante `MANUAL` no se da de baja ante SUNAT desde el ERP —eso se hace donde se emitió—; el ERP lo saca de sus cuentas con la anulación interna de D-110, que pasa a cubrir todo lo que el ERP no emitió.                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| RF-76 | Emitir nota de crédito total o parcial sobre un comprobante aceptado, ajustando el saldo por cobrar. **Fase 5b** (D-075). **D-153:** una nota de crédito **hereda el modo de su afectado**: la de un comprobante manual se registra manual y la de uno electrónico se emite, con una guarda en cada terminal. Sin eso, SUNAT recibiría una nota de crédito sobre un comprobante que este ERP nunca le mandó.                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| RF-77 | Despachar un pedido total o parcialmente, moviendo kardex y cerrando el pedido al cubrir el 100 % de sus líneas. **Fase 5b** (D-074): `/despachos`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| RF-78 | Emitir la guía de remisión electrónica remitente del despacho, con modalidad de traslado privado o público. **Fase 5b** (D-078).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| RF-79 | Revertir un despacho: devuelve el stock y el estado del pedido. Bloqueado si un comprobante aceptado lo referencia. **Fase 5b** (D-074).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |

### 4.7 Clientes y proveedores (D-031)

| **#** | **Requisito**                                                                                                                                                                                                                                                                                                                                                             |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RF-80 | Alta, edición y baja lógica de clientes: tipo y número de documento (DNI, RUC o CE), nombre/razón social, dirección, correo, teléfono y días de crédito.                                                                                                                                                                                                                  |
| RF-81 | Alta, edición y baja lógica de proveedores: los mismos datos que un cliente, más si presta servicio de corte tercerizado (`providesCuttingService`, D-033). **D-151:** el alta trae razón social y dirección del padrón con el mismo botón que la de cliente (`GET /suppliers/lookup`), opcional de punta a punta.                                                        |
| RF-82 | No se permite repetir número de documento entre clientes activos.                                                                                                                                                                                                                                                                                                         |
| RF-83 | No se permite repetir número de documento entre proveedores activos.                                                                                                                                                                                                                                                                                                      |
| RF-84 | Buscar cliente o proveedor por nombre o número de documento.                                                                                                                                                                                                                                                                                                              |
| RF-85 | VENDEDOR crea clientes y edita sus datos básicos; la baja lógica, el documento de identidad y los días de crédito son solo de ADMINISTRADOR. Proveedores, solo ADMINISTRADOR. **Fase 5b** (D-076, reemplaza la versión anterior).                                                                                                                                         |
| RF-86 | Registrar el cobro de un comprobante, total o parcial, con medio de pago y referencia. **Fase 5b** (D-075): `/cobranzas`. **D-169:** el saldo se compara **en céntimos** (`payableBalance`), que es la escala en la que se cobra — con cuatro decimales, un total de S/ 117.9999 rechazaba el cobro real de S/ 118.00 con un mensaje que mostraba las dos cifras iguales. |
| RF-87 | Revertir un cobro: el monto vuelve al saldo pendiente y la fila queda marcada, nunca borrada. **Fase 5b** (D-075, patrón de M-2).                                                                                                                                                                                                                                         |
| RF-88 | Consultar las cuentas por cobrar con su saldo y su fecha de vencimiento, y cuáles están vencidas. **Fase 5b** (D-075).                                                                                                                                                                                                                                                    |
| RF-89 | Emitir boleta a "público en general" para venta menor sin documento del cliente, con tope y excepción registrada. **Fase 5b** (D-077).                                                                                                                                                                                                                                    |

### 4.8 Reportes (D-031, alcance fijado por D-036)

Los reportes de v1 son exactamente estos cinco. Cualquier otro reporte queda fuera de alcance.

| **#** | **Requisito**                                          |
| ----- | ------------------------------------------------------ |
| RF-90 | Reporte de inventario valorizado por línea de negocio. |
| RF-91 | Reporte de kardex por producto o por bobina.           |
| RF-92 | Reporte de ventas por período.                         |
| RF-93 | Reporte de cuentas por pagar por proveedor.            |
| RF-94 | Reporte de cola de producción.                         |

> **Fase 5b (D-070..D-078).** El ciclo posterior al pedido: RF-70 y RF-74..RF-79 en `invoicing` (`/comprobantes`, `/despachos`) y RF-86..RF-89 en la cobranza (`/cobranzas`). El puerto `ElectronicInvoicingProvider` es lo que hace que ninguno de esos requisitos dependa del PSE que se use hoy.

### 4.9 Auditoría y configuración

| **#** | **Requisito**                                            | **Implementación** |
| ----- | -------------------------------------------------------- | ------------------ |
| RF-95 | Toda acción crítica queda registrada de forma inmutable. |
| RF-96 | Consultar el registro de auditoría.                      |                    |

---

## 5. Preguntas abiertas (pendientes de grill)

| #    | Pregunta                                                                                                                                         | Recomendación del agente                                                                                                                                                                                                                                    | Estado                                                                                                                                       |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| P-01 | ¿Data real que migrar?                                                                                                                           | —                                                                                                                                                                                                                                                           | **Resuelta → D-001**                                                                                                                         |
| P-02 | ¿El ERP emite comprobantes directo a SUNAT (Nubefact API) o solo registra/importa los emitidos fuera?                                            | Emitir directo desde ventas; importación (RF-71) solo para histórico/contingencia.                                                                                                                                                                          | **Resuelta → D-025**                                                                                                                         |
| P-03 | RF-63: ¿cotización confirmada genera **orden de producción** separada o la cotización misma es la orden?                                         | Orden de producción separada (`production_orders`), con FK a cotización. Permite parciales y reprogramación.                                                                                                                                                | **Resuelta → D-026**                                                                                                                         |
| P-04 | Venta directa de bobina: ¿se genera SKU en catálogo o se vende por código de bobina?                                                             | Vender por código de bobina, línea `trading`, sin SKU; kardex descuenta la bobina completa.                                                                                                                                                                 | **Resuelta → D-027, luego D-037** (vigente: SKU `BOB{finishCode}{thicknessMm}`, uno por `typeKey`, sin ancho ni guiones)                     |
| P-05 | Método de valorización del kardex: promedio ponderado vs PEPS.                                                                                   | Promedio ponderado por producto/línea (más simple, aceptado por SUNAT).                                                                                                                                                                                     | **Resuelta → D-028**                                                                                                                         |
| P-06 | Moneda: ¿ventas en PEN y USD? ¿Tipo de cambio manual o SUNAT diario?                                                                             | Ambas monedas; TC manual editable con default del último usado.                                                                                                                                                                                             | **Resuelta → D-029** (decisión final: TC SUNAT diario vía apis.net.pe con fallback manual editable, no solo manual)                          |
| P-07 | Compras que son gastos/crédito y notas de débito (§1.1): ¿módulo de compras completo con cuentas por pagar, o solo registro?                     | v1: registro de compras + saldo por proveedor; sin contabilidad.                                                                                                                                                                                            | **Resuelta → D-030** (decisión final: módulo completo con recepción y pagos, no solo registro)                                               |
| P-08 | Secciones faltantes del docx (3, 4.7, 4.8, RF-15, RF-24, RF-70): ¿existían (reportes, clientes, proveedores)?                                    | Asumir 4.7 = clientes/proveedores, 4.8 = reportes; confirmar.                                                                                                                                                                                               | **Resuelta → D-031 y D-036** (§4.8 acotada a cinco reportes; RF-15 recuperado en §4.2)                                                       |
| P-09 | Precio de venta: ¿lista de precios por línea, por cliente, o manual por cotización?                                                              | Lista base por producto + override manual con permiso.                                                                                                                                                                                                      | **Resuelta → D-032, corregida por D-163** (el margen es sobre la venta —`costo ÷ (1 − margen)`— y el mínimo es un bloqueo duro, no una guía) |
| P-10 | Corte tercerizado: ¿un solo proveedor de corte o varios? ¿costo por kg fijo?                                                                     | Varios proveedores; costo por kg ingresado al recibir.                                                                                                                                                                                                      | **Resuelta → D-033**                                                                                                                         |
| P-11 | Costo de producción: ¿solo materia prima, o incluye servicios directos y gastos indirectos de fábrica?                                           | Materia prima + servicios directos + overhead por kg configurable por línea; sin centros de costo en v1.                                                                                                                                                    | **Resuelta → D-035**                                                                                                                         |
| P-12 | Flete, aduana y seguro de una compra de bobinas: ¿son gasto del período o forman parte del costo del material (landed cost)?                     | Landed cost: la compra `SERVICE` se vincula a la compra `COIL` y su costo sin IGV se prorratea por kg entre las bobinas, como `ADJUST` de costo en el kardex.                                                                                               | **Resuelta → D-043** (default por recomendación; el dueño puede revertirlo antes de Fase 3)                                                  |
| P-13 | Consumo de producción (RF-30, RF-34): ¿el kg que se descuenta del kardex es el kg teórico por dimensiones o el kg real pesado por el operario?   | Kg teórico desde dimensiones × `densityFactor` del acabado, con override de kg real; la diferencia contra el teórico (u override) se registra como merma de proceso automática.                                                                             | **Resuelta → D-047**                                                                                                                         |
| P-15 | Cotizaciones y ventas (Fase 5, RF-31, RF-60..69): ¿cotizar reserva stock? ¿cómo se modela la reserva contra el kardex y qué la libera o consume? | Cotizar no reserva; confirmar la cotización crea pedido + reserva atómicamente, en un ledger propio (no en el kardex), con estados ACTIVA/CONSUMIDA/LIBERADA e invariante `disponible ≥ reservado`; sin vencimiento automático, alerta + liberación manual. | **Resuelta → D-054**                                                                                                                         |

---
