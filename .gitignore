# dependencias y builds
node_modules/
dist/
dist-cli/
.next/
out/
coverage/
.turbo/
*.tsbuildinfo

# entornos y credenciales (nunca commitear)
.env
.env.*
.env.setup
!.env.example
.neon
.vercel/
.gcloud/

# tests
test-results/
playwright-report/
blob-report/
playwright/.cache/

# sistema
.DS_Store
Thumbs.db
*.log
npm-debug.log*
pnpm-debug.log*

# Claude Code (local)
.claude/settings.local.json

.vercel

# archivos de trabajo del CLI de importación de ventas (D-142): datos reales del dueño
# (clientes, RUCs, montos), nunca al repo. Viven en local-data/ (ignorada por completo);
# las dos reglas de abajo son red de seguridad si alguno queda suelto en la raíz.
/local-data/
/*.xlsx
*.decisiones.json
